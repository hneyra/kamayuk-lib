/**
 * `@kamayuk/ui` — todavia vacio.
 *
 * El esqueleto existe desde el paso 1 del plan para que el repositorio arranque y para que
 * el `link:` de cada sistema tenga a que apuntar antes de que haya codigo. Lo que entra
 * aqui esta escrito en ADR-0030 §4.
 */
export {};
