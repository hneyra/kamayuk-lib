# Registro de «Verificar antes de afirmar»

Cada issue deja aquí una fila con qué se implementó, **con qué rotura se demostró que la
verificación muerde** y qué rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo por
tercera vez.

La doctrina —qué es una fila y qué tiene que demostrar— vive en
[`CLAUDE.md`](../../CLAUDE.md); la historia, aquí. Que la fila **exista** lo comprueba
`docs/00-gobierno/verificar-fila-del-registro.mjs`; lo que **diga**, la revisión.

**La tabla nace con una fila, y es la del repositorio mismo.** No hereda nada: la historia de los
paquetes que llegan aquí vive en el repositorio del que salieron, y copiarla sería el registro de un
trabajo que aquí no se hizo.

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| **Nacen las tres primeras librerías de frontend (#2).** `@kamayuk/formato` (de `rentas/frontend/src/dominio/`, verbatim), `@kamayuk/api` (**diseñado**, no extraído: los tres clientes tenían clases de error incompatibles) y `@kamayuk/sesion` (`crearIdentidad` parametrizado por realm, retorno, destino y **prefijo de claves**). Más `sin-suponer-un-sistema`, que barre el código de producción de los cinco paquetes que viajan a un navegador, omitiendo comentarios. **134 pruebas, 0 fallos** | **Cuatro roturas.** (1) `const colado = '/rentas/api/v1';` en `paquetes/api/cliente.ts`. (2) `\b` inicial en el patrón de vocabulario tributario, que hacía que `totalDeArbitrios` **no casara** — el límite de palabra no existe dentro de camelCase. (3) La guarda barriendo también `paquetes/verificaciones/`. (4) La guarda en entorno `jsdom` en vez de `node` | (1) `AssertionError: ADR-0030 §2 pone el sistema delante de la ruta… Donde aparece: paquetes/api/cliente.ts:161`. (2) `expected 0 to be greater than 0` en «y sobre la muestra, «vocabulario-tributario» encuentra lo que tiene que encontrar» — **la regla no habría mordido la forma en que el vocabulario se cuela de verdad, que es un identificador**. (3) La guarda **se delataba a sí misma**: el texto de su propia regla cita «arbitrio», porque es el ejemplo con el que ADR-0030 §4 la escribe, y ese texto es código y no comentario. (4) `TypeError: The URL must be of scheme file` en `texto.ts:6` |
| **El verde de los tres paquetes resolvía por una vía que ningún consumidor usa (#4).** Los imports entre paquetes pasan a **relativos**; se retiran el `paths` del `tsconfig` y el `alias` de `vitest`, que enmascaraban el fallo; y las **nueve prohibiciones** de ESLint se mudan a `@kamayuk/verificaciones` con sus nueve muestras, porque el código enlazado no lo lintaba nadie. **160 pruebas en 9 archivos, 0 fallos** | **Cuatro roturas.** (1) El import desnudo `from '@kamayuk/api'` con `node_modules/@kamayuk` apartado. (2) Al revés: apartado `node_modules/@kamayuk` **entero**, con los imports ya relativos. (3) `**/node_modules/**` escrito dentro de un docblock. (4) `includes('muestras')` como comprobación de directorio | (1) `paquetes/sesion/escalera.ts(45,30): error TS2307: Cannot find module '@kamayuk/api'` con **RC=2** — el rojo que sólo sale en CI, dentro de un archivo de otro repositorio. Y la guarda nueva lo caza sobre el árbol de verdad, nombrando `paquetes/sesion/escalera.ts:3` y diciendo qué escribir. (2) `tsc` **RC=0** y **160 pruebas en verde**: la resolución ya no depende de los enlaces de workspace, que es la situación del consumidor. (3) `ReferenceError: node_modules is not defined` en `prohibiciones.mjs:6` — el `*/` del glob **cierra el comentario antes de tiempo**. (4) La guarda salió roja nombrando `otras-muestras.ts`, que lleva la palabra en el nombre y no está en el directorio |
| **UI-2 · `@kamayuk/ui` capa 1 (#6).** Los **42** tokens del artboard —38 colores, 2 radios, 2 sombras— en un `@theme` de Tailwind v4, **derivados del archivo y no transcritos**; `Importe`, `Insignia`, `FechaDeCalculo` e `Icono`; los trazos con nombre GENERICO —el mapeo modulo→icono es negocio de un contexto y la guarda lo prohibe—; y las barreras de tipo. **11 archivos, 175 pruebas** (eran 9 y 160) | **Cuatro roturas.** (1) `fechaCalculo` pasada a opcional. (2) Un quinto tono en `Tono`. (3) `Importe` con tres decimales. (4) El arnes, sin querer: cuatro pruebas rojas nada mas escribirlas | (1) `TS2578: Unused '@ts-expect-error' directive` en dos sitios — la barrera de tipos es una prueba DEL COMPILADOR y falla cuando la propiedad se pierde, sin ejecutar nada. (2) `TS2741: Property 'verde' is missing in type …` — el `Record<Tono, string>` de las clases obliga a exhaustividad, asi que un tono nuevo no entra sin sus dos clases. (3) `Importe con una forma que el backend no sirve` — no redondea: revienta. (4) `Found multiple elements with the text: Un estado`. **Con `globals: false`, Testing Library NO registra su limpieza**, asi que cada `render` se acumulaba en el mismo documento. El rojo hablaba de un selector ambiguo y no de que faltara limpiar: mandaba a mirar la prueba en vez del arnes. Arreglado con un `afterEach(cleanup)` explicito en `vitest.setup.ts` |
| **UI-3 · Los tres temas por dos modos, con el oscuro derivado en OKLCH (#8).** Seis paletas completas —228 declaraciones— **derivadas de la del artboard con reglas** (unos ocho numeros por tema), no escritas a mano; `temas.css` se regenera y la guarda exige que salga igual; contraste WCAG medido en las seis, con las excepciones declaradas CON SU CIFRA; `ProveedorDeTema` con sus dos ejes; y el primer componente de shadcn con el `--radius` atado. **16 archivos, 217 pruebas** (eran 13 y 202) | **Seis roturas, cuatro de ellas defectos mios que solo aparecieron al MIRAR el archivo generado.** (1) El orden del papel invertido. (2) `--azul-oscuro` clasificado como accion. (3) El croma del sepia multiplicado. (4) El tono de la accion del sepia rotado. (5) Un valor tocado a mano en `temas.css`. (6) El renombrado del radio, contra `rentas` | (1) `--superficie` salia **#090909** y `--fondo` **#0f1012**: la tarjeta mas OSCURA que el lienzo. En claro la tarjeta es blanca sobre lienzo gris; en oscuro tiene que seguir siendo la mas clara, porque **la elevacion se lee con luz en los dos modos**. El contraste NO lo caza: las dos parejas pasaban. (2) En oscuro una accion se aclara, asi que **la barra global entera se volvia clara**. No es una accion: es una superficie. (3) `--superficie` es `#ffffff`, croma 0, y 0 × 1.6 sigue siendo 0 — el papel del sepia salia gris. **Un tono no tiñe lo que no tiene color**: hay que anadir croma. (4) Con el tono en 10° el azul del sepia salia **#5f1d2c**, granate, o sea del color del error. El sepia entibia el papel y la tinta; la semantica se queda quieta. (5) `El archivo de temas dejo de salir de las reglas`, con el comando para regenerarlo. (6) El `@theme` paso de `--radius-radio` a `--radius`, y la guarda de `rentas` salio roja: `expected 44 to be 42`. **Nada en la CI de esta libreria lo habria dicho** |
| **UI-4 · Las once piezas de shadcn que el interprete de V8 pide en cada pantalla (#11).** `Tarjeta` —filo AZUL y cabecera azul maciza, que NO es la `Card` de shadcn—, `Etiqueta`, `Campo`/`Area`/`Dato`, `Casilla`, `Desplegable`, `Emergente`, `Calendario`, `Separador`, `Tabla`, `Alerta`, `Avance` y `Formulario` sobre `react-hook-form`+`zod`; mas `campos.ts`, que pone por escrito **que pieza dibuja cada uno de los siete tipos de campo** en vez de esconderlo en un `switch`. La forma es la de shadcn —Radix, `cva`, `cn()`, `data-slot`— y los valores son los del artboard. **21 archivos, 253 pruebas** (eran 16 y 217) | **Seis roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` sin sorpresas al terminar): **(R1)** `Tarjeta` con `rounded-[0.625rem]` escrito a mano; **(R2)** `s: 'Desplegabe'` en la tabla de tipos; **(R3)** `Opcion` como `<div>` en vez de `Select.Item`; **(R4)** `Etiqueta` sin su `aria-describedby`; **(R5)** un archivo de capa fuera de `test:capas`; **(R6)** la indulgencia de `test:capas` colada en `test` | **6 de 6 en rojo.** (R1) `Hay piezas con el radio escrito a mano… tarjeta.tsx: «rounded-[0.625rem]»` — `Boton` ya se vigilaba a si mismo, pero con doce piezas vigilarlo pieza a pieza es como se olvida una. (R2) `La tabla nombra piezas que el paquete no exporta: Desplegabe` — que el nombre este escrito no basta. (R3) `Unable to find an accessible element with the role "option"`: el desplegable se abria y no habia nada que elegir. (R4) DOS rojos a la vez, `expected null to be '_r_4_-ayuda'` y `expected null to be '_r_9_-error'`. (R5) `Hay archivos de capa que NO los corre nadie`. (R6) `expected 'vitest run --exclude…' not to contain 'dangerouslyIgnoreUnhandledErrors'`.<br><br>**El defecto que la prueba caza y la vista no: el mensaje de error se comia el NOMBRE del campo.** La primera `Etiqueta` envolvia al control en el `<label>` para no tener que inventar un identificador por campo. Con ayuda o error puestos, envolver mete **todo** el contenido del `<label>` en el nombre accesible: el campo pasaba a llamarse «Ejercicio El ejercicio son cuatro digitos: 2026.», y el lector de pantalla lo anuncia ENTERO cada vez que se enfoca — el mensaje deja de ser un aviso y se vuelve parte del rotulo, que no se quita ni corrigiendo el campo. En pantalla se veia perfecto. Lo delato `getByLabelText('Ejercicio')` al dejar de encontrarlo. Y el miedo que motivo el envoltorio **no tenia fundamento**: `useId()` da uno unico por instancia montada, asi que un interprete que dibuje la misma definicion cuarenta veces da cuarenta identificadores distintos. Reescrita a `htmlFor` + `aria-describedby` + `Slot`, que es la forma de shadcn — y ahora se sabe por que es esa.<br><br>**Y el otro hallazgo, que es del ARNES y no del producto: montar una capa de Radix bajo jsdom cuesta ~6 s.** Reproducido con `radix-ui` pelado y cero lineas de esta libreria: el mismo `Popover` **cerrado** tarda 63 ms y el archivo 1.8 s; **abierto**, 6 026 ms y 40 s. Es acumulativo dentro del archivo, asi que un archivo con las cuatro pruebas de capa juntas tardaba **137 s** y hacia caducar la llamada del trabajador al proceso principal —`[vitest-worker]: Timeout calling "onTaskUpdate"`—, o sea **`yarn verificar` en ROJO con las 246 pruebas en VERDE**. Descartados uno a uno: los dos motores de Vitest (hilos y procesos), tres reporteros, `ResizeObserver` e `IntersectionObserver` puestos a mano, `pretendToBeVisual: false`, cerrar la capa antes de terminar, y un `requestAnimationFrame` sincrono. El documento quedaba limpio entre pruebas. **Aviso para quien mida esto**: el reloj de jsdom miente aqui —decia 6 ms donde el de Node decia 5 817— y `console.log` desde el trabajador viaja por la misma tuberia congestionada, asi que instrumentar con `console.log` mide la tuberia y no el codigo. Resuelto acotando: `test` los excluye, `test:capas` los corre con la indulgencia, `verificar` encadena los dos, y `las-capas-corren.test.ts` impide el modo de fallo silencioso —un archivo excluido de uno y no nombrado en el otro, que no daria ningun rojo: daria menos pruebas—.<br><br>**Cifras**: `yarn verificar` **19 archivos / 251 pruebas** mas **2 / 2** de capa, rc=0. Y **el consumidor comprobado a mano ANTES de abrir el PR**, que es lo que #10 pide y lo que no se hizo en #8: `rentas@main` con esta rama enlazada, **46 archivos, 1 128 pruebas, 0 fallos** — las tres `peerDependencies` nuevas no lo rompen |
| **UI-6 · `@kamayuk/shell`: el armazón de V8, escrito nuevo y sin saber de qué sistema es (#13).** Las **siete piezas** del armazón entran en `@kamayuk/ui` con la convención de #11 —`Miga`, `Plegable`, `Menu`, `Confirmacion`, `Cajon`, `PaletaDeMando` y `Avisos`, sobre Radix, `cmdk` y `sonner`— y sobre ellas se escribe **el armazón entero**: barra global, carril de módulos plegable con su filtro, paleta de mando, cabecera con miga, acciones al pie y el aviso de cambios sin guardar. **El catálogo entra por parámetro** (`catalogo.ts`) y el enrutado es por hash con `createHashRouter`, **con una ruta por destino ofrecido**. **24 archivos, 306 pruebas** (eran 19 y 251), más **3 / 3** de capa (eran 2 / 2) | **Doce roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` sin sorpresas al terminar): **(R1)** `const PREFIJO = '/rentas/api/v1'` en `shell/catalogo.ts`; **(R2)** «predial del contribuyente» en la barra de instrucción; **(R3)** el árbol pintando `casan.slice(0, 1)`; **(R4)** `destinoDeSlug` devolviendo el slug en vez de `null`; **(R4b)** la ruta `*` rindiendo `<Pantalla />`; **(R5)** `slugDe` ignorando el slug declarado; **(R6)** «Seguir editando» limpiando también la marca; **(R7)** el atajo mirando sólo `ctrlKey`; **(R8)** `accionesDelPie` devolviendo siempre las de escritura; **(R9)** `Cancelar` construido sobre `AlertDialog.Action`; **(R10)** un `capa-*.test.tsx` en `paquetes/shell` que no corre nadie; **(R11)** `capa-del-menu.test.tsx` fuera de `test:capas`; **(R12)** el carril ignorando `enCajon` y el plegado | **12 de 12 en rojo.** (R1) `ADR-0030 §2 pone el sistema delante de la ruta… paquetes/shell/catalogo.ts:71`. (R2) `ADR-0030 §4 lo escribe con este ejemplo exacto… CabeceraDePantalla.tsx:33`. (R3) `Unable to find an accessible element with the role "button" and name /Flota/`. (R4) `expected 'flo-turnos' to be null` y `expected 'alm-entradas' to be null`. (R4b) `Unable to find an element with the text: /no corresponde a ningun destino disponible/`. (R5) `expected '#/alm-entradas' to be '#/entradas'`, y ocho derivados detrás. (R6) `Unable to find an element with the text: esta sucia`. (R7) `Unable to find an element with the placeholder text of: /Un modulo o un destino/`. (R8) `expected [ 'Limpiar', 'Guardar' ] to deeply equal [ 'Exportar', 'Imprimir' ]`. (R9) `expected null to be 'cancelar'`. (R10) y (R11) `Hay archivos de capa que NO los corre nadie: 'test' los excluye y 'test:capas' no los nombra`, nombrando el archivo. (R12) `Unable to find an accessible element with the role "dialog"` y `expected <aside …> to be null`.<br><br>**R4 y R4b son la misma regla por dos puertas, y sólo mirándolas juntas se ve cuál cierra.** Con R4 puesta —`destinoDeSlug` aceptando cualquier slug— las pruebas puras salieron rojas y **la de integración siguió VERDE**: el hash de un destino que el catálogo no trae seguía sin abrir nada. El motivo es que el cierre de verdad no es esa función, sino **la tabla de rutas**, que se construye recorriendo el catálogo: lo que no está en él no tiene ruta que lo reciba. R4b lo confirma al revés, rindiendo la pantalla desde la ruta `*`. O sea que el AC4 no depende de que alguien se acuerde de comprobar el hash en cada pantalla: depende de la forma de la tabla.<br><br>**El hallazgo del arnés, que corrige la regla de #11 con números: lo caro NO es «una capa», es el POSICIONADOR.** #11 apartó a `test:capas` las pruebas que abren una capa, midiendo `Popover` y `Select`. Las siete del armazón se midieron de nuevo, una por archivo y con las librerías peladas: **`DropdownMenu` 12 409 ms de archivo (39 s de reloj de pared); `AlertDialog` 208 ms; `Dialog` 233 ms; `Command` (cmdk) 227 ms; `Sonner` 151 ms** — y las cuatro baratas **juntas en un archivo, 305 ms**. Lo que separa a unas de otras es `@radix-ui/react-popper`, que `DropdownMenu` lleva dentro igual que `Popover` y `Select`; `AlertDialog`, `Dialog` y el diálogo de `cmdk` se colocan con CSS y no lo cargan. Y el gasto **no está en abrir**: cronometrado por tramos con el reloj de Node, montar 34.8 ms + buscar el disparador 87.6 ms + abrir 79.1 ms + la consulta 8.9 ms = **213 ms dentro del `it`**, con el `it` reportando 4 775 ms. Está en **desmontar el posicionador al limpiar**, fuera del cuerpo de la prueba, que es por lo que no se ve instrumentando lo que uno escribe. Resultado: sólo `capa-del-menu.test.tsx` va apartado, y las otras cuatro piezas caben en `piezas-del-armazon.test.tsx` con el resto.<br><br>**Y la guarda de las capas tenía un agujero, que R10 mide en las dos direcciones.** `las-capas-corren.test.ts` miraba `paquetes/ui/shadcn` mientras el `--exclude` de `test` es **global**: un `capa-*.test.tsx` en cualquier otro paquete quedaba excluido de `test` igual que los demás y la guarda **no lo veía** para exigir que `test:capas` lo nombrara — o sea, no lo corría nadie, que es exactamente el modo de fallo que esa guarda existe para impedir, escondido donde la guarda no miraba. Medido con el archivo puesto: con la guarda acotada, **6 pruebas en verde**; con la de #13, que barre `paquetes/` entero, roja nombrando `paquetes/shell/capa-inventada.test.tsx`.<br><br>**Tres cosas más que se midieron en vez de suponerse.** (a) **jsdom no trae `matchMedia`** —además de `ResizeObserver` y `scrollIntoView`, que `cmdk` pide—: `TypeError: window.matchMedia is not a function` en `sonner/dist/index.mjs:1072`, al resolver el modo `system`. Por eso `useEsEstrecho()` del armazón pregunta por ella antes de usarla en vez de darla por hecha. (b) **El rótulo de la región viva de `sonner` venía en inglés**: sin `containerAriaLabel`, `aria-label="Notifications alt+T"`. No se dibuja en ninguna parte —es el nombre accesible del contenedor— así que una pantalla entera en castellano se anunciaba con una palabra en inglés y nadie lo ve mirándola. (c) **`AlertDialog` NO se cierra pulsando fuera, pero `Esc` SÍ lo cierra** —probado sobre la pieza, y el foco al abrir cae en el `Cancel`—. La primera mitad es la diferencia de verdad con un `Dialog`; la segunda obliga a que **`Esc` signifique la salida inocua**, y por eso el aviso conecta su cierre a «seguir editando» y no a «salir». El javadoc decía lo contrario antes de medirlo.<br><br>**Cifras**: `yarn verificar` **24 archivos / 306 pruebas** más **3 / 3** de capa, rc=0. Y **el consumidor comprobado a mano ANTES de abrir el PR**, que es lo que #10 pide: **`rentas` sale ROJO, y se dice aquí en vez de arreglarlo callando**. `yarn typecheck` da `TS2307: Cannot find module 'sonner'` y `Cannot find module 'cmdk'` dentro de `node_modules/@kamayuk/ui`, más dos derivados que parecen defectos de la librería y no lo son; `yarn test`, **3 archivos fallidos de 53 y 1 prueba de 1 234**, con `Failed to resolve import "cmdk"`. **No es un defecto de este PR: es su consecuencia declarada.** Desde `rentas`#88 el clon enlazado resuelve sus dependencias **desde `rentas`** (`preserveSymlinks: true`), así que las dos `peerDependencies` nuevas de `@kamayuk/ui` hay que instalarlas allí: `cmdk@^1.1.1` y `sonner@^2.0.8` en `frontend/package.json`, dos líneas, y `resolucion.ts` las deduplica sola porque deriva su lista de las `peerDependencies`. Este PR **no toca `rentas`** a propósito |
| **La barra de instruccion no se dibujaba NUNCA (#15).** `CabeceraDePantalla` aceptaba `instruccion?` y sabia dibujar la barra gris de V8; `Armazon` **no se la pasaba**, y no podia: ni `Destino` ni `ModuloDelCatalogo` la llevaban. Asi que la barra no salia en ningun sistema y no habia forma de que saliera sin tocar la libreria. `Destino` gana `instruccion?: string` —dato del SISTEMA, no del marco: son cuarenta frases distintas en `rentas`— y `Armazon` la baja a la cabecera. **24 archivos, 308 pruebas** (eran 24 y 306) | **La rotura es reintroducir el defecto**: `<CabeceraDePantalla hoja={hoja} />` sin su `instruccion`, que es exactamente como estaba | **Roja**: `Unable to find an element with the text: /revise lo que entro y lo que salio hoy/`, 1 de 24.<br><br>**Por que este defecto no se veia, que es lo unico interesante de el.** La cabecera se dibujaba **entera y correcta** —miga, titulo, nota— y solo le faltaba la barra. Una pantalla a la que le falta la instruccion **parece una pantalla**, no un defecto: no hay hueco, ni filo vacio, ni error en consola. Las pruebas del armazon comprobaban la cabecera y pasaban, porque comprobaban lo que si se dibujaba. Lo destapo `rentas`#90 al montar la aplicacion de verdad y exigir de cada una de las 40 pantallas su instruccion: **cuarenta rojos pidiendo un texto que nadie dibujaba**. Es el argumento entero a favor de que el recorrido corra contra la aplicacion montada y no contra el componente suelto.<br><br>**Y se anade la mitad negativa**, que es la que impide que el arreglo sea peor que el defecto: un destino SIN instruccion **no dibuja la barra**, en vez de dibujarla vacia. Un filo que encierra nada se lee como un hueco donde falta algo |
| **La CI mira a sus consumidores, y la lista es dato (#10).** `paquetes.yml` gana un trabajo que clona cada consumidor declarado en `consumidores.json`, le corre su propia orden de verificacion **dos veces sobre el mismo arbol** —una con esta libreria en `main` y otra con la rama del PR— y falla solo si la segunda empeora a la primera. `los-consumidores-se-miran.test.ts` impide que el guardian se quede sin sujeto. **25 archivos, 314 pruebas** (eran 24 y 308) | **Dos mediciones en CI de verdad, en una rama desechable** (`rotura-reintroducir-el-renombrado-de-8`, cerrada y borrada): **(M1)** `--shadow-sombra-1` cambiado de `0 1px 2px rgba(22,35,44,.06)` a `0 1px 3px rgba(22,35,44,.09)`; **(M2)** la orden del consumidor puesta a `yarn verificar && false`, que falla en las dos pasadas | **M1 — el trabajo caza lo que la libreria no puede ver.** `Lint, tipos y pruebas` **VERDE con 314 pruebas**, y `hneyra/rentas sigue en verde con esta rama` **ROJO**: `linea base (libreria en main): success` · `##[error]ESTA RAMA ROMPE A «hneyra/rentas».` · `Con la libreria en «main» su suite sale en VERDE; con esta rama, en ROJO.`<br><br>**M2 — el AC4, que es lo que separa un guardian util de uno que se ignora.** Con las dos pasadas rojas, el trabajo sale **VERDE** con aviso: `linea base (libreria en main): failure` · `::warning::«hneyra/rentas» ya estaba rojo con la libreria en «main». Esta rama NO es la causa, asi que este trabajo no la bloquea.`<br><br>**El primer intento de medida fue el equivocado, y eso es un hallazgo.** Se empezo reintroduciendo el renombrado de #8 —`--radius-radio`— y **la libreria lo caza ella sola**: #11 anadio `radio.test.ts`. Bueno para la libreria y malo como medida, porque el trabajo de consumidores ni llega a correr (`skipping`). Hubo que buscar una rotura que la libreria **no tenga con que comparar**, que es exactamente el hueco que #10 describe: la paleta se compara contra el artboard, y **el artboard vive en `rentas`** — esa direccion es deliberada, porque al reves seria `@kamayuk/ui` dependiendo de un sistema (ADR-0030 §4). O sea que el hueco no es un descuido: es la sombra que proyecta una decision correcta.<br><br>**El coste, medido** (AC5): el trabajo del consumidor tarda **1m14s** y **1m22s** en las dos corridas —clonar los dos repositorios, un `yarn install` y **dos** pasadas de la suite de `rentas`—, y corre en paralelo con el propio de la libreria (1m33s). El PR pasa de ~1m35s a ~3min de reloj de pared.<br><br>**Lo que NO cierra.** El trabajo corre sobre `main` del consumidor, no sobre la rama que a lo mejor lo esta arreglando en paralelo: dos cambios que se necesitan a la vez —una `peerDependency` nueva aqui y su declaracion alli— siguen exigiendo el mismo orden de siempre, mezclar el del consumidor primero. Es lo que se hizo en #14/`rentas`#96 a mano, y esto no lo automatiza |
| **UI-11 · Los textos del armazon entran como DATO, no como `i18next` (#19).** Las **32** palabras que `@kamayuk/shell` decia por su cuenta y las **6** de `@kamayuk/ui` salen a dos sacos —`shell/textos.ts` y `ui/textos.tsx`— y entran por `<Armazon textos={…} />` (un `Partial`, con el castellano por omision) y por `props` en las piezas sueltas. **Cero `peerDependencies` nuevas**, que es el coste que el issue rechaza: `i18next` como `peer` obligaria a los cuatro sistemas a montarlo antes de dibujar un boton. El saco viaja por su propio contexto, y ese contexto tiene los valores por omision **como valor por defecto** —a diferencia de `useArmazon()` y `useHoja()`, que revientan a proposito—: las siete piezas se publican sueltas y una pieza que exigiera proveedor de idioma para dibujar un arbol convertiria la traduccion en requisito. **28 archivos, 343 pruebas** (eran 25 y 314), mas **3 / 3** de capa | **Ocho roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` identico al de partida despues de cada una): **(R1)** `Volver` escrito otra vez dentro de `AccionesAlPie`; **(R2)** `aria-label="Mostrar u ocultar el menu"` escrito otra vez en `BarraGlobal`; **(R3)** `ListaDeLaPaleta` sin su `label`; **(R4)** `Miga` ignorando su `rotulo` y volviendo a `aria-label="Ruta"`; **(R5)** una pieza NUEVA con texto dentro que **nadie monta**; **(R6)** `react-i18next` anadido a las `peerDependencies` de `@kamayuk/ui`; **(R7)** `marcarElSaco` devolviendo el saco sin marcar; **(R7b)** el arnes dejando de recorrer los atributos; **(R8)** `Armazon` dejando de fundir el saco con los valores por omision | **8 de 8 en rojo.** (R1) `El armazon con una hoja de consulta deja llegar texto que NO pasó por el saco: «Volver» (por texto, en button[boton])` y `paquetes/shell/AccionesAlPie.tsx:36 «Volver» (por texto JSX)` — **4 de 21**. (R2) `«Mostrar u ocultar el menu» (por aria-label, en button[boton])` en las OCHO pantallas, mas `paquetes/shell/BarraGlobal.tsx:57 «Mostrar u ocultar el menu» (por atributo «aria-label»)` — **8 de 21**. (R3) `La paleta con resultados deja llegar texto que NO pasó por el saco: «Suggestions» (por aria-label, en div[lista-de-la-paleta])` — **1 de 20**. (R4) `«Miga» deja llegar texto que NO pasó por el saco: «Ruta» (por aria-label, en nav[miga])` — **2 de 17**. (R5) **las dos guardas montadas en VERDE, 20 de 20**, y solo el barrido rojo: `paquetes/ui/shadcn/pieza-inventada.tsx:3 «Sin resultados» (por atributo «aria-label»)` y `«No hay nada que ensenar todavia.» (por texto JSX)`. (R6) `Las «peerDependencies» de «ui» cambiaron…: expected [ 'class-variance-authority', …(11) ] to deeply equal [ …(10) ]` y `«react-i18next» entro por «peerDependencies»` — **2 de 9**. (R7) `«alternarElCarril» no se marco: expected 'Mostrar u ocultar el menu' to be '⟦alternarElCarril⟧'`, y detras las ocho pantallas. (R7b) `el recorrido no mira los atributos anunciados: expected [ 'Un literal que nadie tradujo' ] to include 'Un nombre accesible sin marcar'` — **2 de 20**. (R8) **23 de 36**, con `Unable to find an accessible element with the role "button" and name "Grabar"` y veintidos derivados de `armazon.test.tsx`.<br><br>**R5 es la medicion que justifica que haya DOS guardas y no una, y es lo unico realmente interesante de este issue.** El AC2 pide montar, y montar es lo correcto: un escaner de fuentes da rojos sobre cadenas que nunca se dibujan y se calla sobre las que si. Pero el montaje **solo caza lo que se monta**. En `@kamayuk/shell` eso lo cubre todo, porque el armazon es un solo arbol con una sola entrada: una pieza nueva del marco se dibuja dentro de `<Armazon>` o no se dibuja. En `@kamayuk/ui` NO: sus piezas son hojas que nadie monta hasta que un sistema las usa, asi que una pieza nueva con una palabra dentro pasa en verde — medido, 20 de 20. Por eso entra un segundo guardian que contesta **otra** pregunta, la de forma y no la de contenido: «el texto literal visible solo puede vivir en los dos sacos».<br><br>**Y ese barrido se escribio con el analizador de TypeScript, no con expresiones regulares, tambien por medida.** El primer intento con regex dio **249 aciertos sobre este mismo arbol, de los cuales 2 eran de verdad** —listas de importacion, destructuraciones, tipos `() => void`, cualquier linea suelta con pinta de prosa— y la eleccion era entre ruido y agujeros. Preguntandole a `ts.createSourceFile` que es un `JsxText` y que es un `JsxAttribute`, el barrido da **cero** falsos positivos y sus dos unicos aciertos son las dos frases del saco de la ui.<br><br>**El defecto que la guarda destapo al primer intento: `cmdk` montaba su lista EN INGLES.** Sin `label`, `Command.List` sale con `aria-label="Suggestions"`. Es exactamente el mismo defecto que #13 midio en `sonner` —`Notifications alt+T`— y de la misma familia: **no se dibuja en ninguna parte**, asi que una paleta entera en castellano se anunciaba con una palabra en ingles y mirar la pantalla no lo ensena. Corregido con `rotulo` en `ListaDeLaPaleta`, por omision «Sugerencias». Lo caza el recorrido **porque mira los atributos anunciados y no solo los nodos de texto**, que es la diferencia con el arnes de `rentas`#103 — y no es teorica: **cinco de las 32 palabras del armazon no se dibujan en ninguna parte**, y un recorrido de nodos de texto las da todas por buenas (R7b lo mide).<br><br>**Y una regla de la casa mordio sola, sin que nadie la invocara.** La primera version del saco declaraba `cuantosDestinos: (casan: number, total: number)`, y `yarn lint` salio rojo: `Un importe se declara «string», nunca «number»… (regla 1, RNF-055)` en `paquetes/shell/textos.ts:90`. En este producto `total…` es un nombre de dinero y solo `totalElementos` y `totalPaginas` estan exceptuados. Renombrado a `ofrecidos`, que ademas dice que se cuenta.<br><br>**Cifras**: `yarn verificar` **28 archivos / 343 pruebas** mas **3 / 3** de capa, rc=0. El archivo del armazon cuesta **1 472 ms** con la paleta, el cajon y el `AlertDialog` abiertos, asi que **no va a `test:capas`**: lo caro es el posicionador (#13) y el menu de sesion se queda cerrado a proposito. Y **el consumidor comprobado a mano ANTES de abrir el PR**, y medido DOS veces sobre el mismo arbol: `rentas@227467d` en un `git worktree` limpio, `yarn verificar` da **32 archivos / 507 pruebas, 0 fallos, rc=0** con la libreria en `main` **y exactamente lo mismo con esta rama**. Nada de `rentas` se toco: los textos son opcionales y con valor por omision, asi que su `Armazon` sigue compilando y dibujando lo mismo.<br><br>**El arbol de trabajo tuvo que apartarse para medir, y conviene que conste.** La primera pasada se hizo sobre `/home/jorge/ws/rentas` y salio con uno y luego dos rojos en `verificaciones/la-v6-no-esta.test.ts` —`e2e — el arnes, atado a las pantallas de V6` y `expected … to contain "import '@kamayuk/ui/estilos.css'"`—: **los dos de ficheros sin seguimiento que otra sesion estaba creando ahi en ese momento**, no de esta rama. Medir el consumidor sobre un arbol sucio da una cifra que no dice nada, asi que la buena es la del `worktree` |
| **UI-14 · El armazon sobrevive a que su catalogo cambie despues de montar (#20).** `crearEnrutador()` **deja de recibir el catalogo**: una sola ruta `*` bajo la raiz, y `DestinoDeLaRuta` elige entre `<Pantalla />` y `<DestinoNoOfrecido />` con `useHojaDeLaRuta`, que es el MISMO dato que dibuja el arbol, la paleta y la miga. El enrutador se construye **una vez** —`useState(crearEnrutador)`, inicializador perezoso y no `useMemo`, que es una pista que React puede tirar—. `Cascara` sigue poniendo el proveedor de la hoja solo cuando hay hoja, asi que `useHoja()` sigue reventando fuera de una pantalla, que es a proposito. La propiedad del AC4 de #13 pasa de sostenerla la tabla de rutas a sostenerla el render, y las cuatro pruebas que la vigilan siguen verdes **sin tocar una linea**.<br><br>**La causa NO era «el contexto se pierde», y medirla cambio la prueba que hay que escribir.** Leido en `react-router@7.18.3`: `RouterProvider` siembra el estado en un `useState(router.state)` **una sola vez** (`chunk-BV7QT456.mjs:6801`), asi que con un enrutador nuevo la pintada siguiente usa todavia las coincidencias del viejo; y `useRoutesImpl` las reapunta contra el manifiesto NUEVO **por el identificador de ruta** —`manifest[m.route.id] || m.route`, `:6130`—. Los identificadores son **posicionales** (`0-1`, `0-2`…), de modo que cambiar el catalogo reapunta cada coincidencia a la ruta que por accidente ocupe ese hueco en la tabla nueva. Donde caia una ruta de destino se dibujaba `<Pantalla />` **sin que hubiera hoja** y `useHoja()` reventaba; donde caia la `*`, no pasaba nada.<br><br>**Y de ahi sale un hallazgo que contradice la parafrasis del encargo, no el issue.** Medidas cinco variantes con el defecto puesto: `[]` -> un catalogo que **no** ofrece el destino del hash **revienta** (es el repro del issue, `#/tra-pap` con `SIN_TRANSITO`); `[]` -> uno que **si** lo ofrece **NO revienta** —sale la pantalla correcta, por accidente de los identificadores—; y de un catalogo lleno a uno menor depende de donde caiga el hueco: con «Flota» fuera y `#/flo-turnos` el hueco cae en la `*` y se salva, con «Almacen» fuera y `#/alm-panel` cae en una ruta de destino y revienta. O sea: «montar con `[]` y volver a pintar con uno lleno» **no muerde**. Las dos pruebas nuevas que llevan **EL REPRO** delante estan elegidas para caer del lado que revienta, y el javadoc del `describe` lo dice para que nadie las «simplifique» a un catalogo cualquiera.<br><br>**Cinco pruebas nuevas en `armazon.test.tsx`**, todas con el catalogo **llegando tarde** (AC3): el repro del issue, el repro al reves (AC2 — se pierde el permiso de la pantalla que se esta mirando), el destino que el catalogo si trae, el caso sin hash, y que el marco **no queda congelado** (se navega por el arbol con el catalogo que llego). Las tres ultimas no muerden este defecto y esta escrito cual si.<br><br>**Y midiendo eso salio un SEGUNDO defecto de la misma causa, que no necesita que el catalogo cambie.** Con una ruta por destino, `#/entradas/` —una barra final, que la escribe cualquiera al pegar una direccion— la recogia la ruta `entradas`, porque `react-router` come la barra final al casar; pero `useHojaDeLaRuta` compara el `pathname` en crudo contra el slug, no encontraba `entradas/` y `Cascara` no ponia el proveedor. `<Pantalla />` dibujada sin hoja, y el mismo `Unexpected Application Error!` **en la primera pintada, sin rerender ninguno**. Con una sola ruta no hay dos opiniones que puedan discrepar. Va con su prueba (la 30). | **Una rotura, la del propio defecto**: `crearEnrutador(catalogo)` con una ruta por destino ofrecido —`...catalogo.flatMap(...)` con `element: <Pantalla />`— y `useMemo(() => crearEnrutador(configuracion.catalogo), [configuracion.catalogo])` en `Armazon`. Es exactamente el codigo de `main`, restaurado despues por copia. Y antes de eso, la **medicion de las cinco variantes** en un archivo apartado —A, B, C, D y E— que es lo que dijo cuales de las cinco pruebas muerden y cuales no | **2 de 29 en rojo, y las dos son las del repro.** `AssertionError: expected <h2></h2> to be null` / `Received: <h2>Unexpected Application Error!</h2>` en `armazon.test.tsx:544` («EL REPRO del issue: con un hash puesto, el catalogo llega y no ofrece ese destino») y en `:561` («EL REPRO al reves (AC2)»), con `Error handled by React Router default ErrorBoundary: Error: useHoja() fuera de una pantalla del <Armazon>. Sin hoja abierta no hay nada que marcar sucio…` en la consola — **el mensaje literal del issue**. Y la de la barra final, medida aparte con la misma rotura, sale roja igual —`BARRA: Unexpected Application Error!useHoja() fuera de una pantalla…` en la primera pintada—. Las otras 27 verdes, incluidas las cuatro del AC4 de #13, que es la mitad que importa: el defecto no las tocaba. <br><br>**Cifras**: `yarn verificar` **28 archivos / 349 pruebas** (eran 343) mas **3 / 3** de capa, rc=0; `paquetes/shell` pasa de **60 a 66**. Ninguna prueba nueva abre capa, asi que el archivo se queda fuera de `test:capas` y `las-capas-corren` sigue conforme. <br><br>**Consumidor comprobado A MANO antes de abrir el PR**, sobre `rentas@6874c26` con el arbol limpio y la libreria enlazada por `link:`: `yarn verificar` **32 archivos / 508 pruebas, 0 fallos, rc=0**, y `yarn e2e` **47 pasadas en 50,4 s** en Chromium contra el bundle construido — que es la medicion que de verdad importa aqui, porque abre **los 40 destinos por el hash** y es donde una ruta unica mal resuelta se veria. `rentas` no se toco: su rodeo declarado (`frontend/src/aplicacion.tsx:99-109`) se queda, porque sigue siendo lo correcto por su primer motivo — no ensenar un arbol vacio durante un segundo—; lo que deja de ser es **necesario** |
| **Las seis paletas salen del paquete, y hay una guarda SOBRE EL CSS EMITIDO (#23).** `estilos/temas.css` —228 declaraciones, tres identidades por dos modos, generadas en OKLCH con su contraste medido (#8)— existia, estaba perfecto y **no lo importaba nadie**: `estilos.css` empezaba con `@import "tailwindcss"` y nada mas, el `exports` publicaba `"."`, `"./estilos.css"` y `"./fuentes"`, y la unica referencia al archivo en todo el arbol era la constante de su propia guarda. Medido en el consumidor —`yarn build` de `rentas`, sobre `dist/assets/*.css`—: **`data-tema` 0 · `data-modo` 0 · `prefers-color-scheme` 0**. `ProveedorDeTema` estampaba los dos atributos sobre un documento cuyo CSS no tenia ni una regla que los leyera.<br><br>**El camino elegido es el `@import` desde `estilos.css`, y no una entrada `@kamayuk/ui/temas.css`**, con su motivo escrito en el docblock de la hoja: las dos llegan al navegador y solo una no se puede olvidar — con una entrada propia, el consumidor que escriba un solo `import` se queda **exactamente como estaba** (proveedor montado, atributos puestos, pantalla del mismo color) y nada se lo dice; seria este mismo defecto servido otra vez, una vez por sistema.<br><br>**Y la guarda no lee el archivo: lo compila.** `temas/las-seis-llegan-al-css.test.ts` resuelve la hoja por el `exports` del `package.json` —no por una ruta escrita dentro—, la compila con la API de `tailwindcss` **con la `base` de cada hoja puesta en el directorio de la hoja, que es lo que hace Vite**, y exige en el CSS emitido las seis combinaciones con sus 38 colores y sus valores derivados, los dos ejes contados como los conto el issue (tres `[data-tema]`, los dos `[data-modo]`, tres `@media (prefers-color-scheme: dark)`, uno por identidad) y —la mitad silenciosa— que entren **sin `@layer`**, que es lo que hace que le ganen al `@theme`. Es la prueba que faltaba: las dos de #8 miden **el archivo** y pasan con el archivo perfecto y perfectamente inalcanzable. **353 pruebas en 29 archivos** (eran 349 en 28) mas **3/3** de capa, rc=0 | **Tres roturas, una por eje.** (1) Se borra la linea `@import "./temas.css";` de `paquetes/ui/estilos/estilos.css` — el defecto original, restaurado. (2) Se quita la entrada `"./estilos.css"` del `exports` de `paquetes/ui/package.json` — el otro extremo del mismo camino. (3) `@import "./temas.css" layer(theme);` — la hoja llega entera y **encapada**, que es lo que ninguna de las otras tres pruebas ve | **(1) 2 de 4 en rojo, y el centinela VERDE** (su umbral lo pasa la hoja sola, a proposito: un centinela que se cae con el defecto lo diagnostica mal): `AssertionError: Las seis paletas NO llegan al navegador:` con las nueve lineas —«institucional/claro: el CSS emitido no trae «[data-tema='institucional']»» y las ocho hermanas—, y `no hay ni una regla para [data-tema='institucional']: expected ' @layer theme, base, components, util…' to contain '[data-tema=\'institucional\']'`. **(2) el archivo entero rojo antes de correr una prueba**: `Error: \`paquetes/ui/package.json\` ya no exporta \`./estilos.css\`. Sin esa entrada, el consumidor no tiene como importar la hoja: ni la paleta ni los seis temas llegan al navegador.` — `Test Files 1 failed (1) / Tests no tests`. **(3) 1 de 4 en rojo y LAS OTRAS TRES VERDES**, que es justo lo que se queria demostrar: `AssertionError: Hay paletas emitidas DENTRO de una capa:` con las nueve.<br><br>**CORRECCION MEDIDA, POSTERIOR (PR #30): lo que esta fila y el codigo decian DE esa rotura era falso.** Se afirmaba —en el docblock de `capasAbiertasEn`, en el mensaje de esa asercion y en el comentario de `estilos.css`— que encapadas «el CSS trae las 228 declaraciones y la pantalla no cambia de color». **No es verdad: encapadas siguen pintando las seis.** Medido compilando la misma hoja publicada con `layer(theme)` puesto: las nueve reglas caen en un **SEGUNDO** bloque `@layer theme` que sale **DESPUES** del que Tailwind emite para el `@theme`, y dentro de una misma capa **gana lo que va despues**; ademas `[data-tema='alto-contraste']` y `[data-tema='sepia']` **superan en especificidad** a `:root, :host`. En Chromium, medido por la revision: con `layer(theme)` `data-tema` sigue apareciendo 7 veces y salen **6 paletas distintas de 24 combinaciones**, identico a sin capa (`main` antes de este issue daba 24 con **1**).<br><br>**La exigencia del «sin capa» se queda —es un invariante bueno— pero por el motivo honrado**: sin capa ganan **por la regla de la cascada** (lo no-capado gana a lo capado) y **no dependen** del orden en que Tailwind emita los dos bloques ni de que los selectores de tema pesen mas que `:root, :host`; encapadas pintan **por esas dos casualidades, que no vigila nadie**, y el dia que cambie cualquiera el fallo es silencioso. La rotura (3) sigue siendo valida: demuestra que la guarda **caza el `layer(...)`**, no que el `layer(...)` apague las paletas. Rehecha con el mensaje corregido, el rojo es el mismo —1 de 6 y las otras cinco verdes— con las nueve lineas de «quedo dentro de un @layer» detras.<br><br>**Y con la correccion entra EL CENTINELA QUE A ESTA GUARDA LE FALTABA.** «Entrar sin capa es lo que hace que le ganen al `@theme`» presupone que el `@theme` emite algo dentro de `@layer theme`, y **Tailwind v4 PODA del `@theme` los tokens que ninguna utilidad usa**: medido con este mismo `compilar()`, con `['bg-fondo']` el bloque `:root, :host` de la capa trae `--color-fondo: #f2f6f9;` y con `[]` **no lo trae**, aunque el bloque siga ahi con sus `--font-*`. O sea que el dia que la lista de clases deje de usar un color —un renombrado, una utilidad que se quita— la prueba de la capa seguiria **verde sin tener enfrente ni una declaracion del `@theme`**: verde por vacia. Dos pruebas nuevas en el mismo archivo: el centinela y la que mide que la poda es real, para que el centinela no sea una precaucion sin medir. La guarda pasa de **4 a 6** pruebas.<br><br>**Rotura del centinela** (PR #30): el centinela compilando con `['p-4']` —una clase que no usa ningun color— en vez de `['bg-fondo']`. **1 de 6 en rojo, y las otras cinco VERDES, incluida la de la capa**, que es exactamente lo que se queria ensenar: `AssertionError: El «@theme» no emitio «--color-fondo», asi que la prueba de la capa no tiene contra quien ganar y pasaria en verde POR VACIA… expected ' --font-sans: -apple-system, BlinkMac…' to contain '--color-fondo: #f2f6f9;'`.<br><br>**Y una medicion que el issue no pedia y hay que decir: esto pone en rojo a `rentas` hasta que arregle UNA linea suya.** `rentas/frontend/verificaciones/tailwind.ts` compila la hoja de la libreria con `base: RAIZ_DE_UI` —la raiz del paquete— en vez del directorio de la hoja, asi que su `loadStylesheet` busca el `@import` relativo donde no esta. Replicado literalmente en un guion aparte, con la libreria de este worktree: `Error: ENOENT: no such file or directory, open '/home/jorge/ws/kamayuk-lib-23/paquetes/ui/temas.css'` en `loadStylesheet`, que tumba los 4 casos de `tailwind-emite-las-clases.test.ts`. Con `base: join(RAIZ_DE_UI, 'estilos')` —lo que hace Vite— el mismo guion compila y emite **16 466** caracteres con las seis paletas dentro, frente a 4 752 hoy. Es un defecto del arnes del consumidor que este cambio destapa: su compilacion resolvia los `@import` de otra forma que el empaquetador, y por eso no medía la hoja que el navegador recibe.<br><br>**VEREDICTO, medido despues (PR #30): `rentas` esta VERDE y el AC4 se cumple.** Primero se confirmo el rojo en la CI de verdad —`linea base (libreria en main): success` · `con esta rama: failure` · `##[error]ESTA RAMA ROMPE A «hneyra/rentas».`, con 4 pruebas suyas en rojo y el mismo `ENOENT … /paquetes/ui/temas.css` en `verificaciones/tailwind.ts:34`—. Y despues **`rentas` lo arreglo en su propio repositorio**: su `main` es `54d8d4e` (PR #132, rama `issue-130`, mezclado a las 13:48:38 UTC), con `base: dirname(hoja)` en `verificaciones/tailwind.ts:51` —el arreglo que esta fila pedia—, un docblock que cita `#125` y **una guarda propia para que no vuelva**, `tailwind-resuelve-los-import.test.ts`. Verificado por la revision con el clon al lado y la libreria por `link:`, corriendo la suite del consumidor **dos veces sobre el mismo arbol**: con la libreria en `6902e9a` **38 archivos / 567 pruebas, rc=0**, y con la rama **38 archivos / 567 pruebas, rc=0**. (Son 567 y no las 524 de la primera medicion porque la suite de `rentas` crecio por su cuenta entre las dos: no se comparan entre si — lo que se compara es base contra rama sobre el mismo arbol.) **El rojo no era un coste del cambio: era un defecto latente que el cambio destapo**, y que nadie podia ver porque `estilos.css` no habia tenido nunca un `@import` relativo.<br><br>**Y un tercer hallazgo de la misma correccion, que va a su propio issue (#33).** El comentario del `:root` de `estilos.css` decia «el oscuro lo declara `temas.css`» y es **falso**: medido, en `temas.css` la cadena `color-scheme` sale **4** veces —el javadoc y los tres `@media (prefers-color-scheme: dark)`— y `grep -cE '^\s*color-scheme\s*:'` da **0**. Ni una es la propiedad. Ahora que las paletas SI llegan al navegador, eso se ve: con el equipo en oscuro la pantalla se oscurece y los controles nativos y la barra de desplazamiento se quedan claros. No se arregla aqui —la propiedad tendria que emitirla `temas/generar.ts` en los tres bloques oscuros, y `temas.css` es archivo generado con su guarda de regeneracion—, asi que queda el comentario diciendo la medicion y apuntando a #33 |
| **La entrada `@kamayuk/ui/fuentes` prometia un archivo que no estaba, y SALE (#24).** `paquetes/ui/package.json` declaraba `"./fuentes": "./fuentes/fuentes.css"` desde el esqueleto del paquete —#2, `60014cc`, 2026-09-12— y `paquetes/ui/fuentes/` **nunca existio**: medido hoy, `ls paquetes/ui/fuentes/` da `No such file or directory` (rc=2) y `grep -rn "ui/fuentes" --exclude-dir=node_modules .` da **cero** coincidencias (rc=1) — nadie la importa, ni en este arbol ni en su prosa. Quien escribiera `@import '@kamayuk/ui/fuentes'` no recibia una fuente sin cara: recibia un fallo de resolucion, y en SU build, que es donde este producto ya midio que salen los rojos mas caros de leer (#4). **De las dos salidas del AC1 se toma la segunda, la entrada sale**, y los tres motivos estan medidos: **(a)** el unico consumidor de `consumidores.json` es `rentas`, que por el artboard V8 fija `--fuente: Arial, Helvetica, sans-serif` y tiene una guarda propia que le prohibe las webfonts —medicion de #24, **no remedida aqui**: `rentas` no esta clonado en este entorno ni es alcanzable desde esta sesion—, asi que nadie pide la entrada; **(b)** los dos `woff2` de Source Sans 3 viven en los clones de `normativa` y `catastro`, que **este entorno no tiene** —`/home/user/` solo trae `kamayuk-lib`, y `find -name '*.woff2'` sobre el arbol da **cero**— y que el issue prohibe tocar desde aqui: bajar unos binarios de otra parte seria **suponer** que son los mismos bytes que los 88 840 medidos, y este repositorio mide; **(c)** **#28 AC-3 dice que quien decide si `@kamayuk/ui/fuentes` hace falta es el SEGUNDO consumidor**, y hoy no hay segundo consumidor — la misma regla con la que #11 dejo fuera `Chart` y `DataTable`. La promesa en prosa sale con la entrada: el docblock de `paquetes/ui/index.ts` decia que las caras «entraran como punto de entrada aparte» y ahora dice lo que hay — ni un `woff2` y ninguna entrada, y de quien es la decision. **Y entra la guarda que faltaba** (AC2), `lo-que-exports-promete-existe.test.ts`: recorre `paquetes/`, lee los seis manifiestos TAL CUAL —no una lista escrita a mano, asi que una entrada nueva la mira sola desde el minuto uno— y comprueba que las **20 promesas** de hoy (**8** subrutas de `exports` mas **12** `main`/`types`, que son la misma promesa sin subruta: por ahi entra el import del nombre a secas y por ahi resuelve `tsc` los tipos) apuntan a un archivo que existe **y que es un archivo y no un directorio**. Y comprueba **la FORMA**: entiende un mapa de subruta a ruta relativa y solo ese, de modo que un condicional anidado, un array de alternativas, el atajo de condiciones (`{"import": "./x.ts"}` —que es un mapa de cadena a cadena y **no** es un mapa de subrutas—), un `exports` que no sea objeto o un destino sin `./` salen **en rojo diciendolo**: un `exports` que la guarda no entiende y pasa en verde es este mismo defecto con un disfraz mejor. **+8 pruebas**: sola sobre `6902e9a` el arbol daba **29 archivos / 357** (eran 28 y 349) y, **mezclado #23 por medio**, da **30 archivos / 361** — `@kamayuk/verificaciones` pasa de **60 a 68** | **Cuatro roturas, cada una aplicada SOLA y restaurada**, con el arbol de vuelta a las tres lineas de esta rama despues de cada una: **(R1)** apartado el destino de una entrada de verdad, `mv paquetes/ui/estilos/estilos.css estilos.css.apartado`; **(R2)** al reves —anadir una entrada que apunta a lo que no hay—, vuelta a escribir `"./fuentes": "./fuentes/fuentes.css"` con el directorio sin crear, que es exactamente el estado de `main`; **(R3)** el lector dejando de anotar la forma que no sabe leer (`if (typeof destino !== 'string') { continue; }`, sin el `anotar`), que es la guarda pasando en silencio; **(R4)** el `exports` entero borrado de `paquetes/ui/package.json`, que es como una guarda se queda sin sujeto sin que nadie la borre | **4 de 4 en rojo, 1 de 8 cada vez, rc=1.** (R1) `AssertionError: Hay entradas de «exports» que prometen un archivo que NO esta. Quien las escriba en un consumidor no recibe una hoja vacia: recibe un fallo de resolucion, y lo recibe en su build. O entra el archivo, o sale la entrada (#24).` / `Donde:` / `  @kamayuk/ui  exports["./estilos.css"]  ->  paquetes/ui/estilos/estilos.css: expected [ { paquete: '@kamayuk/ui', …(3) } ] to deeply equal []`. (R2) el mismo rojo con `  @kamayuk/ui  exports["./fuentes"]  ->  paquetes/ui/fuentes/fuentes.css` — o sea: **la guarda habria cazado el defecto de este issue el dia que se escribio**. (R3) `AssertionError: «un array de alternativas» no se anoto: paso en silencio: expected 0 to be greater than 0`. (R4) `AssertionError: «ui» no promete ni una subruta por «exports»: expected false to be true // Object.is equality`.<br><br>**El AC3 del issue NO aplica, y se dice en vez de callarlo.** «`normativa` y `catastro` quedan apuntados como los que borran su copia cuando se reconstruyan» esta escrito bajo «si entra», y no entra: los **88 840 B** duplicados byte a byte siguen en sus dos repositorios y quien los borre es **#28**, que es el dueno de esa decision.<br><br>**Cifras**: `yarn verificar` **29 archivos / 357 pruebas** mas **3 / 3** de capa, rc=0 (la partida: 28 / 349 mas 3 / 3), y **30 archivos / 361 pruebas**, rc=0, despues de mezclar #23, que entro en `main` mientras esto se escribia; `yarn registro:autoprueba` con sus **nueve muestras**. **Y la mezcla se comprobo en vez de suponerse**: #23 no anadio ninguna entrada de `exports` —arrastra `temas.css` con un `@import` DENTRO de la hoja—, asi que esta guarda sigue contando las mismas **20 promesas** en los seis paquetes, y una de las ocho que vigila es justamente el `./estilos.css` del que cuelga la guarda de #23. La guarda nueva cuesta **7 ms** —seis `JSON.parse` y veinte `statSync`—, asi que no abre capa y no va a `test:capas`.<br><br>**El consumidor no se pudo medir a mano, y lo midio la CI — asi que no hay que suponerlo.** `rentas` no esta clonado en este entorno ni es alcanzable desde esta sesion, de modo que la medida de #10 —su suite dos veces sobre el mismo arbol— la hizo el trabajo de consumidores del PR: `linea base (libreria en main): success` / `con esta rama: success` / `«hneyra/rentas» sigue en verde con esta rama`, con **33 archivos / 524 pruebas, 0 fallos** en la pasada de esta rama —y entre ellas `enlace-con-kamayuk-lib` y `la-resolucion-dice-el-clon-hermano`, que son las suyas que miran justo como resuelve la libreria enlazada—. Lo que ya se sabia aqui es que el cambio **no le quita nada**: la entrada retirada no la importa nadie, y el barrido del arbol es la prueba de ello |
| **La CI mide la rama del consumidor que el PR nombra (#26).** El trabajo `consumidores` clonaba la rama por omision del consumidor, y con eso un cambio de aqui que exija un ajuste alla **no podia salir verde nunca**. Ahora un PR lo dice con una linea de su cuerpo —`consumidor: duenno/sistema@rama`— y la CI mide contra ella. **La logica NO esta en el `yaml`**: el parser y la resolucion viven en `paquetes/verificaciones/rama-del-consumidor.mjs` con sus muestras, porque un `run:` de quince lineas no se puede probar y asi es como se llego a este hueco. **El mecanismo se eligio midiendo los tres**, no suponiendo: (a) **un archivo** —un campo `rama` en `consumidores.json`— sobrevive a la mezcla, medido en un clon de banco (`git merge --no-ff` y `main` sigue diciendo `rama: 'la-rama-que-lo-arregla'`), y como esa rama del consumidor no existe (`git ls-remote --exit-code --heads .../rentas.git la-rama-que-lo-arregla` da **RC=2**) con el AC3 dejaria **rojo todo PR siguiente**: es la exencion muerta de #20 otra vez; (b) **una etiqueta** no dice rama ni consumidor salvo codificandolos en su nombre, y hay que crearla —`consumidor:rentas@la-rama-que-lo-arregla` da **404** en este repositorio—, ademas de que `paquetes.yml` escucha `pull_request` **sin `types:`**, asi que etiquetar despues de abrir no vuelve a correr nada; (c) **una linea del cuerpo** tiene precedente medido aqui —`registro.yml` le pasa `github.event.pull_request.body` a la guarda del registro por `KAMAYUK_CUERPO_DEL_PR` desde #4—, no pide permisos nuevos, y **desaparece al mezclar** porque el cuerpo del PR no es parte del arbol. Gana (c). **Y el veredicto cambia de significado cuando hay rama nombrada**: la linea base puede estar legitimamente roja —esa rama trae el ajuste que espera este cambio—, asi que el criterio de #10 «las dos rojas, no es de aqui» dejaria pasar en VERDE lo que nadie comprobo; con rama nombrada **la rama nombrada es el contrato** y su rojo bloquea. Los tres desenlaces de `git ls-remote` se midieron y se dicen distintos: **RC=0** existe, **RC=2** no existe, **RC=128** no se pudo preguntar (`fatal: could not read Username for 'https://github.com': terminal prompts disabled`). **30 archivos, 371 pruebas** (eran 29 y 353; `verificaciones` pasa de 60 a 78), mas **3 / 3** de capa y **5 casos** del ensayo | **Nueve roturas, cada una aplicada SOLA y restaurada por copia** (`diff` identico al de partida despues de cada una): **(R1)** la comprobacion de existencia deja de romper el trabajo; **(R2)** el consumidor no declarado se ignora; **(R3)** la rama se normaliza a minusculas; **(R4)** la mencion mal formada se ignora; **(R5)** lo que hay dentro de un bloque de codigo cuenta como mencion; **(R6)** el `checkout` del consumidor pierde el `ref:`; **(R7)** el veredicto vuelve al de #10, con un solo camino; **(R8)** el ensayo sale de la CI; **(R9)** el workflow deja de pasar `--comprobar`. Y aparte, **la demostracion del AC3 contra el consumidor de verdad**: `hneyra/rentas` con una rama que no existe por un lado y con `main` por el otro | **9 de 9 en rojo, pero R9 solo DESPUES de corregir la guarda — y ese es el hallazgo.** (R9) **salio VERDE con la rotura puesta**: quitado `--comprobar` de la orden, `toContain('--comprobar')` seguia pasando porque **el comentario de al lado nombra la opcion**. Es la misma forma de defecto que `infrastructure`#114 midio en la guarda del registro —una cabecera que cita el issue no es una fila— aplicada a un `yaml`: un comentario que nombra una opcion no la ejecuta. Corregido exigiendo la ORDEN ENTERA sobre el workflow **sin sus lineas de comentario**, R9 da `el resolvedor no comprueba la existencia de la rama: expected … to contain 'rama-del-consumidor.mjs --consumidor "$QUIEN" --comprobar'`.<br><br>Las otras ocho: **(R1)** `MAL: «la rama «ni-esta-rama-existe-ni-se-crea-jamas-26» NO existe: rojo, y no cae a la rama por omision» esperaba rojo y salio lo contrario` con `rama=ni-esta-rama-existe-ni-se-crea-jamas-26` · `nombrada=si`, y `FALLO: 2 de 5 casos del ensayo`. **(R2)** `AssertionError: expected [] to deeply equal [ 'consumidor-desconocido' ]` —**1 de 18**— y en el ensayo el falso verde entero: `rama=` · `nombrada=no` · `Este PR no nombra ninguna rama de «hneyra/kamayuk-lib»: se mide contra su rama por omision`. **(R3)** `expected 'arreglo-de-la-paleta' to be 'Arreglo-De-La-Paleta'`. **(R4)** `expected [] to deeply equal [ 'mencion-mal-formada' ]`. **(R5)** `expected true to be false` en «dentro de un bloque de codigo NO cuenta» —**2 de 18**— y, lo que de verdad la justifica, el guion contra **un cuerpo de PR que solo ENSENA la sintaxis** dentro de una valla: `[la-rama-no-existe] «hneyra/rentas» no tiene ninguna rama «la-rama-que-lo-arregla»`, RC=1. O sea que sin esa regla **este mismo PR** exigiria una rama que nadie ha creado. **(R6)** `el checkout del consumidor no usa la rama resuelta: expected … to match /ref:\s*\$\{\{\s*steps\.\w+\.outputs\.rama\s*\}\}/`. **(R7)** `el veredicto no distingue si hay rama nombrada: expected … to contain 'RAMA_DEL_CONSUMIDOR'`. **(R8)** `el ensayo de la resolucion no corre en CI: expected … to contain 'node paquetes/verificaciones/ensayar-la-rama-del-consumidor.mjs'`.<br><br>**El AC3, demostrado contra el consumidor de verdad y no contra un doble:** `FALLO: no se puede decidir contra que rama de «hneyra/rentas» medir este PR` · `[la-rama-no-existe] «hneyra/rentas» no tiene ninguna rama «ni-esta-rama-existe-ni-se-crea-jamas-26»` · `Y ESTE TRABAJO NO VA A MEDIR LA RAMA POR OMISION POR SU CUENTA`, **RC=1**; y con `hneyra/rentas@main`, `«hneyra/rentas» tiene la rama «main» (f9703af): se mide contra ella` · `rama=main` · `nombrada=si`, **RC=0**.<br><br>**Y el AC2 medido en la CI de verdad, no en banco** (corrida 33 de este PR, con `hneyra/rentas`): el paso de resolucion dijo `AVISO: la linea 12 no cuenta como mencion porque esta dentro de un bloque de codigo: «consumidor: hneyra/rentas@la-rama-que-lo-arregla»` —el ejemplo del propio cuerpo del PR, descartado y anunciado— y `Este PR no nombra ninguna rama de «hneyra/rentas»: se mide contra su rama por omision`; `actions/checkout` con el `ref` vacio hizo `Determining the default branch` -> `Default branch 'main'`, que es exactamente lo que hacia antes de #26; las dos pasadas de `rentas` dieron **34 archivos / 528 pruebas** cada una (libreria en `2ad2eb0` y libreria en esta rama) y el veredicto salio `el consumidor se midio en: su rama por omision` · `linea base (libreria en main): success` · `con esta rama: success` · `«hneyra/rentas» sigue en verde con esta rama`. El ensayo, corrido en CI **con** token, **5 / 5**: el caso del repositorio al que no se puede preguntar sale igual con token («Repository not found») y sin el («could not read Username»), porque los dos son RC=128.<br><br>**LO QUE NO SE DEMOSTRO, Y NO SE DA POR CUMPLIDO.** La mitad del AC4 que pide «se nombra una rama que ARREGLA el consumidor y el trabajo pasa de rojo a verde con el mismo cambio de la libreria» **no se midio**: en este entorno no hay clon de `rentas` y el repositorio del consumidor no esta en el alcance de la sesion, asi que no se pudo crear alli una rama con un ajuste ni correr el trabajo `consumidores` entero —dos clones, un `yarn install` y dos pasadas de la suite ajena—. Lo que si esta medido es todo lo que decide contra que se mide y como se rompe: la resolucion con sus muestras, la existencia de la rama preguntada a `hneyra/rentas` de verdad, y el veredicto por lectura del `yaml`. **Queda para el primer PR que de verdad necesite un ajuste del consumidor**: nombrar su rama, comprobar que la linea base sale roja con el aviso de que ahi eso se ESPERA, que «Con esta rama» sale verde, y anotar esa corrida. **Cifras**: `yarn verificar` **30 archivos / 371 pruebas** mas **3 / 3** de capa, rc=0; `yarn registro:autoprueba` **9 / 9**; `yarn consumidor:ensayo` **5 / 5**. La rama se rebaso sobre el `main` que trajo #23 antes de entregar, asi que la linea de partida es la de ese `main` —**29 archivos / 353 pruebas**, medido excluyendo el archivo nuevo de este cambio— y no la de 349 con la que se empezo.<br><br>**Y recontado DESPUES de mezclar `main`**, que para entonces ya traia tambien #24 y la correccion de #23: `yarn verificar` **31 archivos / 381 pruebas**, rc=0, mas **3 / 3** de capa, y `@kamayuk/verificaciones` **86** —medido, no sumado—. La mezcla dio conflicto de acumulacion en esta tabla y en la fila de `verificaciones` de `CLAUDE.md`, donde #24 y #26 escriben a la vez: se quedan LAS DOS ediciones, no una |
| **El oscuro se lo dice al navegador: `color-scheme: dark` (#33).** Las seis paletas llegaban desde #23 y `rentas` ya monta el `ProveedorDeTema` (`frontend/src/aplicacion.tsx:281`, su #111 cerrado), asi que el defecto **se veia en pantalla**: con el equipo en oscuro la paleta se oscurecia entera y los controles nativos, la barra de desplazamiento y el fondo previo del lienzo se quedaban **blancos**. El unico `color-scheme` del documento era el `light` de `estilos.css`, y llegaba siempre. **No es un color mal derivado sino una propiedad que no se emitia**, y por eso el arreglo no esta en `estilos.css`: sale de `temas/generar.ts`, y `estilos/temas.css` es archivo generado con su guarda de regeneracion. Ahora `generar.ts` la emite en los **seis** sitios —los tres bloques oscuros por sus dos caminos, `prefers-color-scheme` y `[data-modo='oscuro']`— y `temas.css` se regenero con `KAMAYUK_REGENERAR=1`. **Ni un color se movio, y esta medido y no supuesto**: las **342** declaraciones `--color-*` del archivo (38 × 9 reglas) salen **identicas** por `diff` antes y despues, y el `git diff` de `temas.css` son **12 lineas, todas anadidas y ninguna quitada** —6 de la propiedad y 6 del javadoc—. El `light` de `estilos.css` **se queda**: es el valor por omision, el que el artboard declara, y aquello contra lo que los seis bloques oscuros tienen que ganar; su comentario, que desde el issue decia la medicion del defecto, **vuelve a decir lo que hace el codigo**. La guarda nueva vive en el archivo que #23 dejo, `temas/las-seis-llegan-al-css.test.ts`, y **mide el CSS EMITIDO con el `compilar()` que ya estaba** — ni el generador ni `temas.css`: el generador puede tener razon y el archivo estar sin regenerar, y el archivo estar bien y el `@import` roto; los dos casos estan medidos al lado. **Y no se puede poner verde contando la cadena**, que es la trampa que este issue tenia delante: en el CSS emitido `color-scheme` sale **diez** veces —las 6 declaraciones, la `light` del `:root` y **3** del preludio de `@media (prefers-color-scheme: dark)`, que no declara nada—, asi que se busca **la propiedad dentro de la regla que corresponde** (`bloqueDe` mas un `esquemaDeColorDe` anclado a principio de bloque o a `;`, que dentro del preludio de un `@media` no existen). Se miden los **nueve** bloques —los 6 oscuros con `dark`, los 3 claros sin ninguno— y el recuento `[6, 3]` es su centinela: unos selectores que dejaran de encontrarse no pueden dejar la prueba pasando sobre lo poco que quede. **Las dos guardas de #23 siguen intactas**: las paletas siguen entrando fuera de toda capa y el centinela del `@theme` sigue exigiendo `--color-fondo`. **31 archivos / 383 pruebas** (eran 31 y 381; `las-seis-llegan-al-css.test.ts` pasa de 6 a 8), rc=0, mas **3 / 3** de capa | **Cuatro roturas, cada una aplicada SOLA y restaurada por copia** (`git diff` identico al de partida despues de cada una). **(R1)** el generador deja de emitir la propiedad —las seis— y se regenera: 0 sitios. **(R2)** deja de emitirla en **UNA SOLA** de las seis (`institucional/oscuro` bajo `prefers-color-scheme`) y se regenera: 5 de 6 puestas. **(R3)** el generador se deja BIEN y `temas.css` se vuelve al de `origin/main` — el archivo generado sin regenerar. **(R4)** la guarda se cambia por la debil, la que cuenta apariciones de la cadena, con el `temas.css` de `main` puesto | **R1, R2 y R3 en rojo; R4 en VERDE, y ese es el hallazgo.** **(R1)** `AssertionError: El oscuro no se lo dice al navegador:` y las seis nombradas una a una: `` institucional/oscuro: «@media (prefers-color-scheme: dark) { :root:not([data-modo='claro'])» no declara `color-scheme`, y tiene que declarar `dark` ``; `` institucional/oscuro: «:root[data-modo='oscuro']» … ``; `` alto-contraste/oscuro: «@media (prefers-color-scheme: dark) { [data-tema='alto-contraste']:not([data-modo='claro'])» … ``; `` alto-contraste/oscuro: «[data-tema='alto-contraste'][data-modo='oscuro']» … ``; `` sepia/oscuro: «@media (prefers-color-scheme: dark) { [data-tema='sepia']:not([data-modo='claro'])» … ``; `` sepia/oscuro: «[data-tema='sepia'][data-modo='oscuro']» … ``, y cierra `expected [ …(6) ] to deeply equal []`. **1 fallo de 8** en el archivo. **(R2)** con **cinco de las seis puestas** la guarda sigue mordiendo y dice EXACTAMENTE cual falta: `El oscuro no se lo dice al navegador:` · `` institucional/oscuro: «@media (prefers-color-scheme: dark) { :root:not([data-modo='claro'])» no declara `color-scheme`, y tiene que declarar `dark` `` · `expected [ Array(1) ] to deeply equal []`. Es lo que separa esta guarda de una que cuente: **seis menos uno no es cero**. **(R3)** **DOS rojos a la vez, y con el generador perfecto**: el de regeneracion, `` AssertionError: El archivo de temas dejo de salir de las reglas. Si el cambio es deliberado, cambia la REGLA en `derivar.ts` y vuelve a generarlo: … expected '/* ==================================…' to be '/* ==================================…' // Object.is equality ``; y el del CSS emitido otra vez con sus seis, `expected [ …(6) ] to deeply equal []`. **2 archivos fallados de 3, 2 pruebas de 32.** Es la mitad que el issue pedia demostrar: **mirar el archivo fuente no basta**, ni el generado ni el generador. **(R4)** la guarda debil —`expect(plano).toContain('color-scheme')` mas `apariciones >= 4`— corrida contra el `temas.css` de `main`, con **0** declaraciones en el archivo, da **`Test Files 1 passed (1)` · `Tests 8 passed (8)`** e imprime por el camino `` R4 apariciones de la cadena `color-scheme`: 4 ``. **Cuatro apariciones y cero declaraciones: exactamente el `grep -c` que da 4 con el que el issue midio el defecto.** Una guarda escrita asi habria acompanado a `main` en verde todo este tiempo — que es por que `esquemaDeColorDe` va anclado y por que se mira dentro del bloque. **Cifras**: `yarn verificar` **31 archivos / 383 pruebas**, rc=0, mas **3 / 3** de capa; `yarn registro:autoprueba` **9 / 9**. Partida en `74b3c74`: **31 / 381** y 3 / 3. **LO QUE NO SE MIDIO**: que los controles nativos se pinten oscuros **en un navegador de verdad**. `jsdom` no dibuja un `select` ni una barra de desplazamiento, asi que esto se sostiene hasta el CSS emitido —donde termina lo que la libreria controla— y de ahi en adelante es el comportamiento estandar del agente de usuario ante `color-scheme`. Se ve, en pantalla y de una pasada, en `rentas` |
| **Las tres parejas que el armazon pone juntas en todas las pantallas, y la regla de que ningun token quede sin medir (#38).** Las 22 parejas se escribieron POR TOKEN —«¿sobre que papel suele ir este?»— y lo que se escapa de esa pregunta es lo que solo existe COMPUESTO: un estado (`hover`), una variante de fondo (`--azul-suave`) o un token sin pareja declarada (`--foco`). Entran **(a)** `--tinta-3` sobre `--azul-suave` —la nota del modulo ACTIVO, a 10.5 px, en las 40 pantallas—, **(b)** `--foco` sobre `--superficie` y `--fondo`, y **(c)** la barra CON HOVER, medida sobre el color RESULTANTE y no sobre el de reposo: `Pareja.detras` acepta ahora una **pila de capas** y `color.ts` gana `componer()`/`apilar()`, que mezclan como el navegador (`source-over` en sRGB con gamma). El hover **baja** el contraste, que es por lo que medir el token de reposo contestaba mejor de lo que era. Y con ellas la regla: **todo token esta en una pareja o esta en `SIN_PAREJA` con su motivo**; callarse no es una tercera opcion, y se vigila en las dos direcciones (una exencion de medida que ya no haga falta tambien sale roja). Quedan seis exentos, cada uno con su porque —`--esqueleto` y `--esqueleto-brillo` no pueden ser pareja: darian 1.09:1 y 1.05:1 en `alto-contraste`, que no admite excepciones—. Los arreglos van **en la regla**: `tinta` de `sepia/claro` de `[0.24, 0.52]` a `[0.24, 0.49]` (4.27 -> **4.84**, y el origen da 4.90) y `sobre-barra` de `institucional/oscuro` de `[0.82, 0.97]` a `[0.84, 0.97]` (4.40 -> **4.72**). **34 parejas, 39 excepciones con su cifra** | **Puesta la lista y ANTES de tocar nada**, el rojo salio con las cifras del issue, medidas en el navegador por quien lo escribio: `4.21` la nota en `sepia/claro`, `1.13` el halo en `institucional/claro`, `4.08`/`4.40` la entidad con hover. Luego, **dos roturas dirigidas**: (A) quitar la pareja `--tinta-3`/`--azul-suave`; (B) quitar las dos de `--foco` | (A) `AssertionError: Estos tokens tienen papel declarado y CERO parejas, asi que nadie mide si se leen:` **`--azul-suave`**. (B) la misma nombrando **`--foco`**, y ademas la direccion contraria: `«institucional/claro» declara exenciones que ya no hacen falta: --foco sobre --superficie, --foco sobre --fondo`. El rojo del arranque, literal: `--tinta-3 sobre --azul-suave: 4.21:1, y texto pide 4.5:1 — la nota del modulo ACTIVO en el carril` |
| **El contorno de foco del artboard se reimplanta, y el halo deja de ser el unico indicador (#37).** V8 declara `:focus-visible { outline: 2px solid var(--azul); outline-offset: 1px }` y el boton se habia quedado solo con el anillo blando: **1.13:1** contra el lienzo en el tema y el modo por omision, cuando WCAG 1.4.11 pide 3:1 — cuatro de las seis no llegaban. El indicador vive ahora en **un solo sitio** (`shadcn/foco.ts`) y lo toman los cinco que no son campo (boton, las tres salidas del dialogo, plegable, calendario) mas el arbol de modulos; el campo NO, porque el ya reimplanto la otra regla del artboard —su borde pasa a `--azul`—, y de el se saca el halo a la constante para que no haya dos copias. **La barra lleva OTRO token**, y no es una preferencia: `--azul` sobre `--azul-oscuro` da **1.52 / 6.20 / 1.12 / 10.06 / 1.20 / 4.86**, o sea invisible en los tres modos claros; alli el contorno es `--sobre-barra` (7.03-10.52 con hover, que es el caso malo). **Y `outline-none` sale de todo lo que dibuja contorno**: en Tailwind v4 escribe `--tw-outline-style: none` y `outline-2` emite `outline-style: var(--tw-outline-style)` — los dos juntos dejan una clase perfectamente escrita **que no pinta nada y no deja rastro en el `className`**. Por eso la guarda **compila** la hoja y mira el CSS emitido, y por eso la constante lleva ademas `outline-solid`, que emite `outline-style: solid` directamente y gana por especificidad `(0,2,0)` a `(0,1,0)`: si alguien lo reescribe manana, el contorno sigue | **Dos roturas.** (C) bajar el indicador por debajo de 3:1 en **una sola** combinacion, moviendo la regla `accion` de `sepia/oscuro` de `[0.76, 0.62]` a `[0.46, 0.38]`. (D) que un componente se reescriba el anillo a mano: devolver a `boton.tsx` su `focus-visible:ring-[3px] focus-visible:ring-foco` literal | (C) `AssertionError: El contorno de foco «--azul» (#004672) sobre «--superficie» (#231e19) en «sepia/oscuro» da 1.67:1, y WCAG 1.4.11 pide 3:1` — **una combinacion, nombrada**. (D) `AssertionError: Estos archivos escriben el anillo de foco a mano: /ui/shadcn/boton.tsx`. Sin esa segunda, la constante puede estar perfecta y un componente seguir en 1.13:1, que es como estaban los seis |
| **El sepia deja quieta la semantica, y una guarda nueva mide la distancia ENTRE los cuatro fondos (#36).** `insignia-fondo` y `insignia-tinta` rotaban a `tono: 20` y `tono: 10` en las dos combinaciones del sepia, y los cuatro semanticos salian **el mismo rosa**: `ok` `#fae1e0` y `mal` `#fae0df`, a distancia **0.0011**. Es el hallazgo de #8 —«el sepia entibia el papel y la tinta; la semantica se queda quieta»— que se aplico a `--azul` y no a los cuatro. Quitadas las dos rotaciones, `sepia/claro` vuelve a `ok #d9eee1` verde, `atencion #e9ddc0` ambar, `mal #fae1dc` rojo, `info #d3e4ee` azul. **La guarda de contraste no podia cazarlo** porque mide texto contra su fondo y ahi los cuatro cumplian: un color puede pasar contraste y no significar nada. La nueva mide `distanciaCromatica()` —el plano a/b de OKLab, **sin la L**—, y sin la L a proposito: incluyendola, `ok` y `atencion` del sepia roto salian a 0.0325, mas lejos que `ok` y `mal` en `alto-contraste`, que es correcto, y no habria cazado nada. **El umbral es 0.012 y sale de medir lo que YA es correcto**: el minimo entre los cuatro temas buenos es **0.0140** (`ok` vs `info` en `alto-contraste/oscuro`, el tema que apaga el croma a proposito), asi que se pone justo por debajo — mas alto pondria rojo un tema correcto y mas bajo dejaria de morder. Despues: **0.0282 / 0.0218 / 0.0147 / 0.0140 / 0.0312 / 0.0286**, y lo que cazaba estaba dos ordenes de magnitud abajo | **Dos roturas.** (E) devolver `tono: 20` a `insignia-fondo` de `sepia/claro`. (F) el centinela: dejar el umbral en `0`, que es como una guarda se queda sin sujeto sin ponerse roja | (E) `AssertionError: En «sepia/claro» hay fondos semanticos que son el mismo color: --ok-fondo (#fae1e0) y --mal-fondo (#fae0df) estan a 0.0011, y hacen falta 0.012` — mas otras tres parejas. (F) `AssertionError: expected 0 to be greater than 0.005`. `institucional` y `alto-contraste` siguen verdes en las dos, que era la condicion |
| **Los velos de la barra se declaran POR COMBINACION, y el avatar deja de ser ilegible bajo la pila de dos (#41).** El disco lleva `--barra-realce` DENTRO del boton de sesion, que al pasar el raton se tiñe de `--barra-hover`: dos velos blancos apilados sobre la barra, cuyos alfas no se suman sino que se componen —34.4 % en claro, **43.8 %** en oscuro—. `--sobre-barra` sobre esa pila daba **4.50 / 3.65 / 5.81 / 4.64 / 4.36 / 3.63**, o sea **cinco de las seis por debajo de su minimo** —`alto-contraste` pide 7:1— y la sexta pasando **por el redondeo**: 4.4973 escrito 4.50. Declarados por modo no cabia ninguna, porque lo que limita el blanco no es el modo sino **cuanto admite la barra de ESA combinacion**: presupuesto medido **0.343 / 0.371 / 0.290 / 0.324 / 0.332 / 0.373**. `TRANSLUCIDOS` se parte en `VELOS_DE_LA_BARRA` —seis filas de tres alfas— y `VELOS_QUE_APAGAN` —los dos velos de dialogo, que no se apilan bajo ningun texto y siguen por modo—, y **el origen deja de pasar de largo para los velos**: `derivar('institucional/claro')` ya no devuelve `new Map(base)`, copia los opacos y aplica los velos declarados. **El reposo se protegio primero**: `--barra-control` y `--barra-realce` se quedan donde el artboard los puso siempre que la pila quepa sin tocarlos, asi que en `institucional/claro` y `sepia/claro` la barra EN REPOSO es byte a byte la del artboard y solo baja el hover (0.18 -> 0.17 y 0.16). Donde no cabia, el que cede es el realce, y el hover conserva un suelo sobre el control para seguir siendo un estado; `alto-contraste/oscuro` es la unica donde tambien baja el control (0.14 -> 0.11), porque su barra es casi negra y el 7:1 no deja sitio. Despues: **4.64 / 4.57 / 7.11 / 7.09 / 4.60 / 4.58**, y el avatar EN REPOSO —pareja nueva tambien— **6.94 / 7.73 / 11.34 / 12.19 / 6.91 / 8.00**, que antes era 6.94 / 6.51 / 9.86 / 8.96 / 6.91 / 6.70: ninguna baja. `--barra-realce` sale de `SIN_PAREJA`. Lo que SI cede es el separador vertical, que es decorativo y esta declarado como tal: 1.81 / 2.31 / 1.84 / 2.22 / 1.84 / 2.25 -> **1.81 / 1.94 / 1.60 / 1.63 / 1.84 / 1.89** | **Tres roturas.** (A) **el orden**: declarar las dos parejas del avatar y NO tocar `derivar.ts`. (B) tocar a mano el `temas.css` generado, devolviendo `0.24` a un bloque. (C) —dentro de #39— ver que `institucional/claro` NO salia roja con 4.4973, que es la rotura que la guarda no puede cazar y por la que la cifra se escribe exacta en el codigo | (A) **cinco rojos, uno por combinacion, con las cifras del issue**: `--sobre-barra sobre --barra-realce sobre --barra-hover sobre --azul-oscuro: 3.65:1, y texto pide 4.5:1 — las iniciales del avatar, CON HOVER` en `institucional/oscuro`, y 5.81 / 4.64 contra 7:1 en las dos de `alto-contraste`, 4.36 y 3.63 contra 4.5 en las dos de `sepia`. (B) `El archivo de temas dejo de salir de las reglas… Tocar un valor a mano deja una paleta que nadie puede volver a producir`. (C) `institucional/claro` en VERDE con 4.50: `ratio()` redondea a dos decimales y 4.4973 pasa el `>= 4.5`. Queda arreglado por el camino —ahora da 4.64— pero **la guarda sigue sin poder cazarlo**, y por eso el presupuesto exacto esta escrito en `derivar.ts` |
| **Los dias del mes vecino del calendario se leen, y siguen atenuados (#39).** `shadcn/calendario.tsx:50` pintaba `classNames.outside` con `--tinta-4`, el token que la hoja de esta libreria declara en mayusculas NO-COLOR-DE-TEXTO. Son numeros que **se leen y se pulsan**, sin `aria-hidden` y sin poder llevarlo —los otros tres usos del token son un chevron, una flecha y el separador de una miga, y los tres si lo llevan—. Medido sobre `--superficie`, que es donde el `Emergente` lo dibuja: **2.59 / 3.56 / 7.45 / 5.34 / 2.42 / 3.83**, o sea que **solo llegaba en `alto-contraste/claro`**; sobre `--fondo`, 2.39 / 3.87 / 7.19 / 5.54 / 2.36 / 4.09. Pasa a `--tinta-3`: **5.51 / 6.45 / 17.33 / 13.55 / 5.70 / 6.11** sobre superficie y 5.07 / 7.03 / 16.71 / 14.06 / 5.54 / 6.53 sobre el lienzo, por encima del minimo de su tema en las seis. **Y sigue atenuado**, que era la otra mitad: contra el `--tinta-2` que un dia del mes HEREDA de la raiz del calendario, el de fuera contrasta menos en las seis (5.51 contra 9.17 en el tema por omision) y las dos tintas estan separadas **0.1188 / 0.1140 / 0.0735 / 0.0538 / 0.1110 / 0.1012** en luminosidad de OKLab. Guarda nueva `shadcn/calendario.test.tsx`, que **renderiza** —un `grep` da por bueno mover la clase a otra llave del mapa— y **sube por los ancestros**, porque el color de un dia del mes no esta en su celda sino heredado. `distanciaDeLuminosidad()` entra en `color.ts` como hermana de `distanciaCromatica()`: una insignia se distingue por el tono y dos tintas del mismo gris, por la luz. El umbral **0.05 sale de medir lo correcto**, igual que el 0.012 de #36: justo por debajo del 0.0538 de `alto-contraste/oscuro`, que comprime la rampa de tinta a proposito | **Tres roturas.** (D) devolver `text-tinta-4`. (E) pintar `outside` con `text-tinta-2`, o sea igual que un dia del mes: es «arreglar» la legibilidad borrando la atenuacion, y la mitad (1) lo habria dado por bueno. (F) el centinela: apagar `showOutsideDays` | (D) **cinco rojos**, `El dia de fuera del mes se pinta con «--tinta-4» (#93a3af) y sobre «--superficie» (#ffffff) en «institucional/claro» da 2.59:1, cuando WCAG 1.4.3 pide 4.5:1`, y `alto-contraste/claro` en verde con 7.45 —que es la cifra que dice que el defecto no era uniforme—. (E) `el dia de fuera y el dia del mes se pintan con el MISMO token: entonces no hay nada que distinga un mes de su vecino`. (F) **la primera version de la guarda paso en VERDE**, y ese es el hallazgo: con `showOutsideDays` apagado `react-day-picker` **sigue emitiendo las mismas celdas `outside`**, con `rdp-hidden` y vacias, asi que mirar la clase medía el color de casillas que nadie ve. Corregida para exigir que la celda tenga un numero dentro, sale `ningun dia PINTADO de la rejilla es de fuera del mes… expected null not to be null`. Y (G), subir `SEPARACION_MINIMA` a 0.06: `En «alto-contraste/oscuro» las dos tintas estan a 0.0538 de luminosidad y hacen falta 0.06` |
| **`@kamayuk/api` baja documentos: `descargar()` devuelve el `Blob` y `entregarAlNavegador()` lo entrega (#43).** `Cliente` tenía un solo método, `solicitar<T>()`, que termina en `respuesta.json()`: un PDF no cabía, y un `<a href>` a la misma ruta sale sin `Authorization` y el navegador se baja el 401 con nombre de PDF. La referencia es la interfaz anterior de `catastro` (`22e6d2e`, `frontend/src/api/cliente.ts:516-564`), que ya bajaba la ficha en PDF y lo hacía **todo en una función**: pedir, comprobar, crear el enlace, pulsar y revocar. Aquí se parte en dos. `Cliente.descargar(ruta, { nombre?, senal? })` comparte con `solicitar` el prefijo, la cabecera del token —extraída a `autorizacion()`, una sola para las dos— y `ErrorDeLaApi`; devuelve `{ nombre, tipoDeMedio, contenido: Blob }` y **no toca el DOM**, y eso no es una intención: `descargar.test.ts` corre en `@vitest-environment node`, donde `document` no existe. Un 200 con `Content-Type` que contiene `json` lanza **`NoEsUnDocumento`**, subclase de `ErrorDeLaApi` con `estado: 200` y `tipoDeMedio` —y no un `codigo` inventado, porque `codigo` es la extensión del contrato que escribe el backend—, así que la pantalla atrapa una sola clase y `peldanoDe()` lo clasifica sin conocerlo. **El nombre, al revés que en la referencia**: allí el argumento mandaba sobre la cabecera (`nombre ?? deLaCabecera(respuesta)`), y aquí manda `Content-Disposition`, como pide el issue — quien genera el archivo sabe si es `.pdf`, `.xls` o `.rtf`, y una pantalla que escribe `resumen.pdf` y pide XLS guardaría una hoja con extensión de PDF. Sin cabecera, el argumento; sin los dos, el último tramo de la ruta, en vez de una palabra escrita en la librería. Se lee también `filename*=UTF-8''…` (RFC 6266 §4.3), porque la expresión de la referencia, `filename="?([^";]+)"?`, casa con esa forma y devuelve `*=UTF-8''…` como nombre. `entregarAlNavegador(documento)` vive en `entregar.ts`, es lo único del paquete que toca el DOM y revoca en un `finally`. **Medido en Chromium 151 con Playwright** sobre el archivo real transpilado: 5 MiB bajados byte a byte iguales, `download.failure()` nulo, la URL ya revocada al volver del clic y ningún enlace en la página — y el contrafactual sin `appendChild` baja **lo mismo**, así que colgar el enlace no hace falta en Chromium; se deja por Firefox, que aquí **no se ha medido** (no hay Firefox en la máquina), y el docblock lo dice. **31 pruebas nuevas** (27 de `descargar`, 4 de `entregar`); el paquete pasa de 21 a **52**, y el total de `yarn test`, a **457**. **`yarn verificar` en verde con Node 22.23.2**; con el Node 24.19 de la máquina `paquetes/shell/armazon.test.tsx` da **9 rojos también en `origin/main` sin este cambio** (`RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal`, entre el `AbortSignal` de jsdom y el `Request` de undici que usa `react-router`) — medido con `git stash -u`, y es ajeno a #43: la CI corre en 22 | **Tres roturas.** (A) **la del issue**: quitar de `descargar()` las tres líneas de la guarda `if (/json/i.test(tipoDeMedio)) throw new NoEsUnDocumento(…)`. (B) devolver el orden de la referencia al nombre: `opciones.nombre ?? nombrePropuesto(…) ?? ultimoTramo(ruta)`. (C) que el cliente toque el DOM: `document.createElement('a').remove()` antes del `return` de `descargar()` | (A) **3 failed \| 24 passed (27)**, uno por tipo de medio: `× LECCION 2: un 200 con JSON no es un documento, y LANZA en vez de entregarse > un 200 con «application/json» lanza NoEsUnDocumento, que es un ErrorDeLaApi` → `AssertionError: expected { nombre: 'resumen-42.pdf', …(2) } to be an instance of NoEsUnDocumento`, y lo mismo con `application/json;charset=UTF-8` y `application/problem+json`. El rojo dice exactamente el defecto: **la promesa resolvió un documento llamado `resumen-42.pdf` con JSON dentro**. (B) **7 failed \| 20 passed (27)**: `con cabecera, manda la cabecera AUNQUE la pantalla haya dado otro` → `expected 'resumen.pdf' to be 'resumen-42.xls'`, y las seis formas de la cabecera → `expected 'no-este.pdf' to be …`. (C) **17 failed \| 10 passed (27)** con `ReferenceError: document is not defined` — los diez verdes son justo los que lanzan antes de llegar al `return` (los errores y el 200 con JSON), que es la prueba de que el entorno `node` mide lo que dice |
| **`@kamayuk/sesion` alcanza a la copia de `rentas`: la sonda del emisor y la consola de la cuenta (#42).** El paquete salió de `rentas/frontend/src/api/identidad.ts` en `efada07` —medido: quitados comentarios y formato, `main` es ese archivo parametrizado, con `entrar(): Promise<void>` en los dos— y `rentas` le añadió en su copia `rentas#112` (`7939902`) y `rentas#115` (`da53d28`). Suben las dos: `Identidad.entrar()` devuelve `Promise<FallaDeLaPuerta \| null>` y pregunta a `${realm}/.well-known/openid-configuration` **antes** de tocar `sessionStorage` (una ida que no ocurrió no gasta del tope), y `urlDeLaCuenta(pagina)` / `abrirLaCuenta(pagina)` con `PaginaDeLaCuenta` exportado, derivadas del `realm`. **La diferencia se midió entera**, comparando el código sin comentarios de `rentas@ac379ac` contra `main`: catorce divergencias, de las que suben cinco (sonda, ida no contada, motivo del navegador con 8 s, credenciales, cuenta) y se quedan en `rentas` las del parámetro (cuándo se lee la configuración, `redirect_uri`, destino, claves, tope, estado de módulo), más `arranque.ts` y los dos `e2e`; `escalera.ts` es idéntica salvo el `import`. La tabla entera está en el PR. **La sonda sube con una línea que la de `rentas` no tiene, `credentials: 'omit'`, y por medida**: el emisor del clúster comparte origen con las interfaces (`https://<dominio>/keycloak/realms/kamayuk` frente a `/rentas/`; el registro de `infrastructure` anota `issuer: https://vmd205066.contaboserver.net/keycloak/realms/kamayuk` en el mismo nodo que sirve `/rentas/`), y en Chromium 151 contra un servidor del mismo origen que pone una cookie, la sonda de `rentas` tal cual llegó con `Cookie: KEYCLOAK_SESSION=la-sesion-del-emisor` y la misma con `omit`, sin `Cookie`; ninguna con `Authorization`. **Y un hallazgo en una guarda al demostrar la rotura que pide el issue**: `sinComentarios` sustituía cada comentario de bloque por un espacio y se llevaba sus saltos de línea, así que el rojo de `sin-suponer-un-sistema` nombraba una línea que no era — `identidad.ts:129`, mitad del docblock de `FallaDeLaPuerta`, para un literal escrito en la **297**. Se vacía el bloque conservando los saltos, con su prueba. **Pruebas**: `identidad.test.ts` de 21 a **33** (las 21 siguen; 5 portadas de #112, 5 de #115 y 2 nuevas: sin credenciales, y el emisor de su instancia), `@kamayuk/sesion` de 33 a **45**, `@kamayuk/verificaciones` de 86 a **87** | **Seis roturas, cada una aplicada SOLA y restaurada por copia**, `cmp` idéntico después de cada una: **(R1)** la sonda devuelve `null` aunque el `fetch` falle (la pide el issue); **(R2)** `const colado = '/rentas/api/v1';` en `identidad.ts` (la pide el issue); **(R3)** la sonda de `rentas` tal cual, sin `credentials: 'omit'`; **(R4)** `urlDeLaCuenta` con `http://localhost:8181/realms/kamayuk` escrito a mano; **(R5)** la sonda movida detrás de las escrituras del rebote en `sessionStorage`; **(R6)** `sinComentarios` sustituyendo otra vez el bloque por un espacio | **6 de 6 en rojo, rc=1.** (R1) **4 de 33**: `AssertionError: navego hacia un emisor que no contesta: expected "spy" to not be called at all, but actually been called 1 times` en «con el emisor caido NO navega, y devuelve quien no contesto y en que URL», más la espera agotada, «una ida que no ocurrio NO gasta una del tope» (`expected false to be true`) y «y pregunta al emisor DE ESTA instancia». (R2) `no se supone: prefijo-de-un-sistema` — `AssertionError: ADR-0030 §2 pone el sistema delante de la ruta para que LA RUTA DIGA QUIEN RESPONDE…` / `Donde aparece:` / `paquetes/sesion/identidad.ts:297  const colado = '/rentas/api/v1';: expected [ { …(3) } ] to deeply equal []`, **1 de 14** — y antes de corregir la guarda, el mismo rojo decía `identidad.ts:129`. (R3) **1 de 33**: `expected undefined to be 'omit'` en «y NO manda credenciales: ni cookies del emisor ni el token, aunque lo haya». (R4) **1 de 33**: `expected 'http://localhost:8181/realms/kamayuk/…' to be 'https://identidad.catacaos.gob.pe/rea…'` en «y SIGUEN al realm de la configuracion» — y «las dos URL cuelgan del REALM configurado» **pasó en verde con el literal**, que es por lo que existen las dos. (R5) **1 de 33**: `expected false to be true` en «una ida que no ocurrio NO gasta una del tope». (R6) **1 de 14**: `expected 3 to be 6` en «el rojo nombra la linea DEL ARCHIVO: un docblock de varias lineas no la corre (#42)».<br><br>**Cifras**, con Node **22.23.2** —el de la CI—: `yarn lint`, `yarn typecheck` y `yarn test` en verde, **33 archivos / 439 pruebas** sobre `226b742` (ese `main` da **33 / 426**), y **35 / 470** después de rebasar sobre `c61068f`, que trajo #43 mientras esto se escribía —la mezcla dio conflicto de acumulación en esta tabla y en las filas de `api` y `sesion` de `CLAUDE.md` y `README.md`: se quedan LAS DOS ediciones—. **Con Node 24, el del puesto, `main` ya da 9 rojos en `armazon.test.tsx`** (`TypeError: RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal.` desde `react-router`), así que se midió con el 22 descargado aparte. **`yarn test:capas` salió rojo en este puesto por tiempo, no por lo que mide**: con carga media **15–19 sobre 6 núcleos** —otras sesiones corriendo Gradle y vitest— las tres capas agotaron sus 5 s (`Test timed out in 5000ms`, 2 de 3, luego 3 de 3, y 3 de 3 otra vez tras rebasar), y con `--testTimeout=120000` pasan **3 de 3** tardando 11–13 s cada una. Ninguna importa nada de `sesion` ni de `verificaciones`: sus `import` son `@testing-library/react`, `vitest` y `../index.ts` de `ui`. El verde de `test:capas` es el de la CI, y lo dio: en la corrida `34787725459` de este PR, `yarn verificar` **35 / 470** más **3 / 3** de capa. **El consumidor, medido a mano**: `rentas@ac379ac` clonado junto a esta rama —su `link:` resolviendo a este árbol—, `yarn verificar` pasó `lint`, `typecheck` e `i18n` y dio **42 archivos / 612 pruebas, 611 en verde**: la que faltaba fue «y el arbol ofrece los diez modulos, con sus rotulos» por `Test timed out in 5000ms` (8 085 ms, carga 20), y ese archivo sola da **47 / 47**, rc=0. No importa nada de la identidad: `rentas` no importa `@kamayuk/sesion` en su código de aplicación, sólo `peldanoDe` desde `verificaciones/resuelve-el-clon-hermano.test.ts`, y `peldanoDe` no cambia. **Y la CI lo midió sin carga**: el job `consumidores` dio **42 / 612** con la librería en `c61068f` y **42 / 612** con esta rama, `linea base (libreria en main): success` · `con esta rama: success` · `«hneyra/rentas» sigue en verde con esta rama.` |
| **`@kamayuk/verificaciones/prohibiciones` publica su FORMA, y la guarda de `exports` mira también el tipo (#46).** La entrada existía y apuntaba a un archivo que está —`./prohibiciones.mjs`— y aun así llegaba rota: dentro de esta librería el `.mjs` se tipa solo (ruta relativa más `allowJs`), pero desde un consumidor llega por `node_modules/`, donde **TypeScript no aplica `allowJs`**. Entra `paquetes/verificaciones/prohibiciones.d.mts`: la interfaz `Prohibicion` y los cinco exports (`PROHIBICIONES`, `REGLAS_EXIGIDAS`, `CLIENTE_DE_API`, `PUERTA_DE_IDENTIDAD`, `DONDE_SE_LLAMA_A_FETCH`). **La forma, nunca la lista** — una lista declarada aquí sería el fork de `rentas#137` otra vez, con tipos. **AC-2, medido contra el consumidor real y no contra un ejemplo**: un frontend de mentira en `/tmp/claude-1001/consumidor-46/` con `eslint.prohibiciones.mjs`, `verificaciones/remedio.mjs` y `verificaciones/las-prohibiciones-son-las-de-la-libreria.test.ts` copiados **verbatim** de `rentas@bb13f73`, el `tsconfig.base.json` de `rentas` tal cual (`preserveSymlinks`, `moduleResolution: bundler`, `allowJs`+`checkJs`, `strict`) y `node_modules/@kamayuk/verificaciones` → este worktree. **Tres estados**: (A) con el `.d.ts` de `rentas` y sin `.d.mts` —`rentas` hoy— **RC=0**; (B) sin ninguno de los dos —borrar el `.d.ts` hoy— **RC=2, 16 errores**; (C) sin el `.d.ts` y con el `.d.mts` **RC=0**. O sea que `frontend/verificaciones/tipos/kamayuk-verificaciones.d.ts` **se borra**, y su borrado es el PR de `rentas`; **lo que falta por comprobar es la suite de `rentas`, que desde aquí no se puede correr**. **AC-3**: `lo-que-exports-promete-existe.test.ts` pasa de 8 a **16 pruebas** con la segunda promesa —un módulo se publica tipado—, y la regla de «promete tipos» **no es una lista de excepciones sino la extensión del destino**: `.ts/.tsx/.mts/.cts/.json` traen su tipo, `.js/.mjs/.cjs` necesitan la declaración que TypeScript busca al lado (`.d.ts/.d.mts/.d.cts`, emparejada — un `x.d.ts` junto a un `.mjs` no lo mira nadie), `.css` y compañía no son módulos y **no se les pide nada**, y una extensión sin clasificar **sale roja diciéndolo**. Exigirle tipos a una hoja `.css` habría desactivado la guarda sola. **AC-4, el barrido**: 20 promesas en los seis paquetes —`main`, `types` y `exports` de cada uno—; 18 `trae-su-tipo` (los seis `index.ts`), 1 `no-es-un-modulo` (`@kamayuk/ui` → `./estilos/estilos.css`), 1 `necesita-declaracion` (`./prohibiciones.mjs`, **la única sin tipos y la de este issue**), 0 sin clasificar. `yarn verificar` en verde: **465 pruebas en 35 archivos** más las 3 de `test:capas` | **Tres roturas.** (A) **la del issue**: `mv paquetes/verificaciones/prohibiciones.d.mts /tmp` y correr la guarda. (B) **la del exceso**, que es el modo de fallo propio de esta regla: añadir `'.css': '.d.ts'` a `DECLARACION_DE`, o sea pedirle tipos a una hoja de estilos. (C) **la de la deriva**: renombrar `clave` → `identificador` en el `.d.mts` y correr `yarn typecheck` de esta librería — es la comprobación de que la declaración no puede separarse del archivo que describe, porque con un `.d.mts` al lado TypeScript la prefiere también para el `import './prohibiciones.mjs'` relativo de `reglas-de-eslint.test.ts` | (A) **1 failed \| 15 passed (16)** en «cada entrada que promete un MODULO lo publica tipado», y el rojo **nombra el archivo que falta**: `@kamayuk/verificaciones exports["./prohibiciones"] -> paquetes/verificaciones/prohibiciones.mjs / falta paquetes/verificaciones/prohibiciones.d.mts`. (B) **4 failed \| 12 passed (16)**: el barrido señala `@kamayuk/ui exports["./estilos.css"] … falta paquetes/ui/estilos/estilos.d.ts` —una librería correcta puesta en rojo— y con él caen las tres muestras que existen para eso: «a una hoja `.css` no se le piden tipos», «la declaración que se pide es la que TypeScript busca» (`expected './estilos/estilos.d.ts' to be null`) y la de la clasificación. (C) **RC=2 con cinco `TS2339`** en `paquetes/verificaciones/reglas-de-eslint.test.ts` (101, 139, 154, 185, 193): `Property 'clave' does not exist on type '{ identificador: string; regla: string; selector: string; message: string; salvo?: readonly string[] \| undefined; }'` — el rojo sale **aquí**, en `yarn typecheck` del dueño, y no en el build de otro repositorio. Y el contrafactual del AC-2, estado (B), es el rojo del issue reproducido: **2 `TS7016`** (`eslint.prohibiciones.mjs:71:25` y `las-prohibiciones-son-las-de-la-libreria.test.ts:12:8`, «Could not find a declaration file for module '@kamayuk/verificaciones/prohibiciones'»), **13 `TS7006`** y **1 `TS2345`** |
| **El contraste se compara SIN redondear, y el redondeo se queda donde sirve: el mensaje (#48).** `color.ts` publicaba `ratio()` **ya redondeado a dos decimales** y las cuatro guardas de color comparaban ESE número contra su umbral. Eso no deja el umbral en los 4.5:1 que pide WCAG 1.4.3: lo deja en **4.495**, y movido hacia el lado malo — una pareja a **4.4973:1** se escribe `4.50` y satisface el `>=`. No es hipotético: salió midiendo #41, donde el avatar sobre la barra con hover en `institucional/claro` daba exactamente 4.4973 y la guarda lo daba por bueno; el issue contó «falla en cuatro de las seis» cuando eran **cinco**. La misma forma estaba en las otras dos medidas, que redondeaban a cuatro decimales antes de comparar contra `SEPARACION_MINIMA` (#39) y contra `DISTANCIA_SEMANTICA_MINIMA` (#36). Ahora **quien decide es `contraste()`, `distanciaDeLuminosidad()` y `distanciaCromatica()`, las tres crudas**, y quien escribe es `ratio()` / `aLaVista(v, n)`. **Y el arreglo es de TIPO, no de comentario**: `ratio()` devuelve **texto** —`'4.50'`—, así que volver a comparar la cifra del mensaje contra un umbral **no compila**. Se re-corrieron las cuatro guardas en las seis combinaciones y **no deja de pasar ni una pareja**: el margen más ajustado de todo el árbol es **0.0687** —`--sobre-barra` sobre la pila del avatar con hover en `institucional/oscuro`, 4.568724:1 contra 4.5—, catorce veces más ancho que la franja que el redondeo escondía; le siguen 0.0814 (`--borde-hover` sobre `--superficie` en `institucional/claro`, 3.081396 contra 3) y 0.0831 (sepia/oscuro, 4.583050). En distancia semántica el más ajustado es **0.002009** (`--ok-fondo` vs `--info-fondo` en `alto-contraste/oscuro`, 0.014009 contra 0.012) y en luminosidad **0.0038** (`alto-contraste/oscuro`, 0.05379872 contra 0.05): los dos por encima de la mitad de una unidad del cuarto decimal, así que el redondeo tampoco los estaba salvando. **`institucional` y `alto-contraste` en reposo no se tocan**: no cambia ni un color, `temas.css` no se regenera y el diff no entra en `derivar.ts`. Entra además `ratioQueNoLlega(a, b, exigido)`: un rojo de esta franja decía «4.50:1, y texto pide 4.5:1», que se lee como una contradicción y manda a buscar el defecto a la guarda; con él dice **4.4972:1**. Dos decimales siguen siendo lo normal — se afloja sólo donde dejaría de explicar el rojo. **`yarn verificar` en verde: 461 pruebas en 35 archivos** (457 antes; las 4 nuevas son la guarda permanente de #48) más las 3 de `test:capas` | **Tres roturas.** (A) **la del issue**: la pareja construida a propósito, `#017dbd` sobre `#ffffff` = **4.497210:1**, metida en la guarda de temas de verdad en lugar de `--tinta-2 sobre --superficie`, y corrida contra el `color.ts` de `main` y contra el nuevo. (B) devolver la línea de decisión a `const medido = ratio(delante, detras)`. (C) devolver `ratio()` a número redondeado | (A) **Con el código de `main`, las cuatro combinaciones cuyo mínimo es 4.5 pasan EN VERDE** —`institucional/claro`, `institucional/oscuro`, `sepia/claro` y `sepia/oscuro`— y sólo las dos de `alto-contraste` salen rojas, y no por la franja sino porque su mínimo es 7: `--tinta-2 sobre --superficie: 4.5:1, y texto pide 7:1`. Con el código nuevo salen **las seis**: `AssertionError: «institucional/claro» tiene parejas que no se leen:   --tinta-2 sobre --superficie: 4.4972:1, y texto pide 4.5:1 — texto secundario de una tarjeta`. Ese verde de cuatro es el hallazgo entero del issue, y es el mismo que #41 anotó y no pudo cazar. (B) no es un rojo de prueba sino de **compilación**, que es lo que se buscaba: `paquetes/ui/temas/temas.test.ts(150,11): error TS2365: Operator '>=' cannot be applied to types 'string' and 'number'`. (C) **6 failed | 11 passed (17)** en `color.test.ts`, y el que nombra la mitad estructural es `EL CENTINELA: lo redondeado es TEXTO… → expected 'number' to be 'string'`, con `y escrita SI dice 4.50 → expected 4.5 to be '4.50'` al lado |
| **El interprete de pantallas sube a `@kamayuk/ui` con el segundo consumidor (#27)** (`paquetes/ui/interprete/`: `Pantalla.tsx`, `BloqueDeLaPantalla.tsx`, `CampoDelBloque.tsx`, `TablaDelBloque.tsx`, `datos.ts` tal cual y la mitad de `tipos.ts` que el interprete lee, renombrada `DefinicionDePantalla/Bloque/Campo/Tabla` y `ColumnaDeTabla` porque `Campo` y `Tabla` ya son piezas de este paquete; `TEXTOS_DEL_INTERPRETE` en `ui/textos.tsx`; se exporta desde `ui/index.ts` **sin subruta nueva en `exports` y sin `peerDependency` nueva**. Lo que en `rentas` salia de `i18next` y de `tono.ts` entra por `props` —`traducir`, `textos` y `tonoDeLaInsignia`, esta ULTIMA obligatoria: un verde por omision pintaria «Vencida» como bien—. El arbol, las definiciones y el vocabulario de tonos se quedan en cada sistema. Pruebas: `interprete/Pantalla.test.tsx` (17, con datos NEUTROS —un puesto, un nicho—), la pieza en `el-texto-propio-es-dato` con su AC4 por omision, seis barreras de tipo, y los centinelas de `el-texto-visible-es-dato` y `sin-suponer-un-sistema` que exigen que el barrido baje a `ui/interprete`) | **Cinco roturas, cada una SOLA y restaurada por copia comparada con `cmp`**: **M1** el titulo del bloque sin `traducir`; **M2** el marcador de fecha escrito dentro **como expresion** (`: 'dd/mm/aaaa'}`), corrida contra la prueba montada Y contra el barrido estatico; **M3** `import { Tarjeta… } from '@kamayuk/ui'` dentro de `interprete/`; **M4** `const DE_OTRO = /contribuyente/;` en `TablaDelBloque.tsx`; **M5** `opciones?` opcional en `CampoDeLista`. **Y una que no hubo que provocar**: la primera corrida de `yarn verificar` | **M1**: 1 roja de 10, ««Pantalla» saca todas sus palabras del saco» con `«bloque» (por texto, en h2)` y `«bloque sin filas»`. **M2**: la montada **roja** —`«dd/mm/aaaa» (por texto, en button)`— y el barrido estatico **9/9 en VERDE**: un literal escrito como expresion no es `JsxText`, y eso ya lo decia `el-texto-visible-es-dato`; hacen falta las dos guardas. **M3**: 1 roja, `paquetes/ui/interprete/BloqueDeLaPantalla.tsx:1 import … from '@kamayuk/ui'`. **M4**: 1 roja, «no se supone: vocabulario-tributario» en `TablaDelBloque.tsx:31`. **M5**: `tsc` rojo — `TS2578: Unused '@ts-expect-error' directive` en la barrera, y `TS18048 'opciones' is possibly 'undefined'` dos veces en la pieza. **La que salio sola**: `shadcn/foco.test.ts` roja, «Estos archivos escriben el anillo de foco a mano: /ui/interprete/CampoDelBloque.tsx» — el disparador de fecha traia de `rentas` `focus-visible:ring-[3px] focus-visible:ring-foco` escrito a mano, anterior a #37; ahora toma `CONTROL`, como los otros tres controles. Con todo restaurado, **`yarn verificar` con Node 22 (el de la CI, en `node:22`)**: 34 archivos / **446 pruebas** y las 3 de capas, verde — y tras rebasar sobre #43, **36 archivos / 477 pruebas**, y sobre #42 **36 / 490**, y las 3 de capas, verde. **Con el Node 24 de la maquina salen 9 rojas en `shell/armazon.test.tsx`** —`RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal`, de `react-router` sobre el `undici` de Node 24 y jsdom— que **no son de este cambio**: el mismo archivo con Node 22 da 30/30. Y **`rentas` `main` contra esta rama**, con Node 22: `yarn verificar` 42 archivos / **612 pruebas**, verde — el cambio es aditivo, `rentas` todavia usa su copia |
| **Una cuarta identidad, `clasico`, con su PROPIA paleta de origen, y `ciudadano` entra en `consumidores.json` (#56).** Hasta #56 el generador suponía un origen único: `derivar(base, clave)` recibía «la base» y el origen era la comparación `clave === 'institucional/claro'`. Ahora hay **dos orígenes, cada uno leído de su CSS**: el `@theme` de `estilos/estilos.css` (del que salen `institucional`, `alto-contraste` y `sepia`, sin cambios) y **`estilos/clasico.css`**, un archivo que **no se importa sino que se lee** —importado, su bloque saldría dos veces al navegador y ganaría la copia que fuera después—. `ORIGEN_DE` en `temas/derivar.ts` da **un** origen por identidad, `leerLosOrigenes()` en `temas/base.ts` los lee todos, y `derivar(origenes, clave)` elige: pasarle a `clasico` la base de `institucional` deja de poder escribirse. `clasico/claro` es el origen **tal cual** para los 38 `--color-*` —incluidos los cinco translúcidos, con la marca `DEL_ORIGEN` en `VELOS_DE_LA_BARRA`, que no es una ausencia: una combinación que olvide sus velos sigue reventando— y `clasico/oscuro` sale con reglas propias (hoy los mismos números que `institucional/oscuro`, **copiados y no compartidos**, medidos sobre las rampas de este origen). **La fuente es de la identidad**: `clasico.css` declara `--font-sans: Arial, Helvetica, sans-serif` y `generar()` la emite sólo en `[data-tema='clasico']`, que casa también en oscuro; el `@theme` no declara ninguna y los otros bloques no emiten nada. **Los valores que no venían del artboard, y por qué**: esqueleto `#eeeeee`/`#f9fbfd` (como el del `@theme`: `--linea-2` y `--sup`), velos que apagan `rgba(10, 76, 134, 0.4)` / `rgba(51, 51, 51, 0.38)` (como los del `@theme`: su `--azul-oscuro` y su `--tinta`), y **los tres velos de la barra NO son los del artboard**: con sus 10 %/22 %/18 % las iniciales del avatar con hover daban **3.55:1** y lo secundario (`#CFE3F4`) sobre el hover **4.22:1**; medido sobre `#0a4c86` la barra admite 25.9 % de blanco total bajo `--sobre-barra` y 15.3 % bajo `--sobre-barra-2`, y quedan en **6 %/12 %/15 %** (hover en el techo, reposo a 0.09 del hover como pide #41, pila 25.2 %) → avatar con hover **4.62**, secundario sobre hover **4.54**. `#777` y `#888` del artboard no entran como tinta (4.48 y 3.54 sobre blanco). **Contraste**: `MINIMOS.clasico` es AA; las parejas de texto pasan en las dos —la más justa, `--mal-tinta` sobre `--mal-fondo` en claro, **4.5313**—, y los filos del artboard (`#DDD`, `#EEE`, `#CCC`, `#999`) y el halo `#1BA0D7` entran en `EXCEPCIONES` **con su cifra**, como los de V8. **`--mal-borde` NO es el `#EBCCD1` de la tabla del issue: lo corrigió la revisión a `#a94442`**, porque en la librería ese token es el borde del campo con `aria-invalid` (`shadcn/control.ts`) y el fondo de la insignia de avisos de `BarraGlobal`, el artboard dibuja el campo con error en `#A94442` (`IN_MAL`), y `#EBCCD1` es sólo el filo de la alerta, que `Alerta` ya saca de `border-mal-borde/40`. Con `#EBCCD1` daba 1.49:1 y necesitaba excepción; con `#a94442` da **5.85:1** en claro y la excepción sale. En oscuro da **1.30:1**, la misma cifra que `institucional/oscuro` y por lo mismo (la regla de `filo` lleva el filo más oscuro del origen al extremo oscuro de su tramo), y como mueve el mínimo de la rampa de filos, los demás filos de `clasico/oscuro` se re-midieron: 1.94 / 2.04 / 1.82 / 2.02 / 1.82 / 1.56 / 1.73, halo 1.61 / 1.78. Distancia semántica mínima 0.0228/0.0187; separación de tintas del calendario 0.0607/0.0833. **Byte a byte**: los nueve bloques de las otras tres identidades en `temas.css` son los de `main` —`cmp` sobre los 12 949 bytes, y una guarda con la huella SHA-256 de cada bloque—, y en `estilos.css` sólo cambian dos comentarios que citaban el archivo renombrado `las-seis-llegan-al-css` → `las-paletas-llegan-al-css`. **La fuente se mide con la cascada**, no leyendo `temas.css`: `jsdom` no entiende `@layer` ni resuelve `var()` (medido: `font-family` sale `""` para cualquier tema), así que `temas/la-fuente-es-de-la-identidad.test.ts` compila la hoja publicada, ordena capas/especificidad/orden/`@media`, casa selectores con `Element.matches` y resuelve `var()` hasta `<html>`, con cinco centinelas contra CSS escrito a mano. Medido además en Chrome for Testing 153 sin cabeza sobre la misma hoja: `Arial, Helvetica, sans-serif` con `clasico` (y con `data-modo="oscuro"`, y en un `<p>` que lo hereda); con `institucional` y `sepia`, la pila de Tailwind que empieza por `-apple-system`. **Consumidores, medidos en local con Node 22**: `ciudadano` (`main`, `2102a47`) **verde, 96 pruebas**; `rentas` (`main`, `1fd7fd9`) **verde con la librería en `main` (616) y ROJO con esta rama**: `src/preferencias/MandoDeTema.tsx(55,7): error TS2741: Property 'clasico' is missing in type '{ institucional: string; 'alto-contraste': string; sepia: string; }' but required in type 'Readonly<Record<Identidad, string>>'` (y el mismo en `MandoDeTema.test.tsx(116,7)`) — es el rojo que ese mando diseñó para el día que entrara una cuarta identidad, y lo arregla un rótulo en `rentas`, fuera de este PR. **`yarn verificar` en verde con Node 22: 537 pruebas en 37 archivos** (502 en 36 en `main` tras #50: temas +13, `ProveedorDeTema` +3, foco +4, calendario +4, fuente +11) más las 3 de capa. Con Node 24 `paquetes/shell/armazon.test.tsx` da 9 rojos **también en `main`**: es el entorno, no este cambio | **Trece roturas, cada una sola y restaurada con `git checkout`.** (1) `--color-tinta-3: #999999` en `clasico.css`. (2) `#e8f1f9` → `#e8f1fa` a mano en el bloque `clasico/claro` de `temas.css`. (3) `--velo` oscuro de `0.62` a `0.6` en `VELOS_QUE_APAGAN` —una regla compartida— **y regenerado**. (4) `clasico` en medio de `COMBINACIONES`, regenerado. (5) `generar()` sin los bloques oscuros de `clasico`, regenerado; (5b) sin su `color-scheme`. (6) `clasico` fuera de `IDENTIDADES`. (7) `--font-sans` fuera de `clasico.css`, regenerado. (8) `--font-sans: Arial…` dentro del `@theme`, regenerado. (9) `'clasico/claro'` con velos propios en vez de `DEL_ORIGEN`. (10) `derivar()` tomando siempre `origenes.institucional`. (11) `--color-acento` borrado de `clasico.css`. (12) La cascada de la guarda de la fuente sin capas. (13) La corrección de la revisión: `--mal-borde` a `#a94442` en `clasico.css` **sin** tocar `EXCEPCIONES`, primero sin regenerar y luego regenerado | (1) `«clasico/claro» tiene parejas que no se leen: --tinta-3 sobre --superficie: 2.85:1, y texto pide 4.5:1 — texto terciario y placeholder` (y sobre `--sup` 2.75, sobre `--azul-suave` 2.49), más `El dia de fuera del mes se pinta con «--tinta-3» (#999999) y sobre «--superficie» (#ffffff) en «clasico/claro» da 2.85:1` y la de regeneración; **`clasico/oscuro` sigue verde**, porque la rampa lleva la tinta a su tramo sea cual sea el origen. (2) `El archivo de temas dejo de salir de las reglas`. (3) **La guarda de regeneración VERDE** y sólo la nueva roja: `Estos bloques de estilos/temas.css ya no son los de main antes de #56:` con los seis bloques oscuros de las tres identidades — es el caso para el que existe. (4) `EL CENTINELA: el archivo se trocea en los nueve de antes mas los tres de clasico` con los tres de `clasico` tras `institucional/oscuro`. (5) `clasico/oscuro: el CSS emitido no trae «@media (prefers-color-scheme: dark) { [data-tema='clasico']:not([data-modo='claro'])»` y `el oscuro del sistema no llega para las cuatro identidades: expected 3 to be 4`; (5b) `«[data-tema='clasico'][data-modo='oscuro']» no declara color-scheme, y tiene que declarar dark`. (6) `expected 'institucional' to be 'clasico'` en «recordada, se RESPETA» y `expected [ 'institucional', …(2) ] to include 'clasico'`; «un servicio la trae por omision» sigue verde, que es correcto. (7) `expected '-apple-system, BlinkMacSystemFont, "S…' to be 'Arial, Helvetica, sans-serif'` en claro y en los dos caminos del oscuro. (8) `la fuente de fabrica ya empieza por Arial: «Arial, Helvetica, sans-serif»` y la huella de `institucional/claro`, `alto-contraste/claro` y `sepia/claro`; **las tres «NO cambia» siguen verdes** porque comparan con la de fábrica, que también se movió — quien lo caza es el centinela, y por eso está. (9) `clasico/claro es estilos/clasico.css TAL CUAL` con `rgba(255, 255, 255, 0.15)` → `0.14`. (10) `La exencion de «--borde-campo sobre --superficie» en «clasico/claro» decia 1.61:1 y ahora mide 1.59:1`, `expected '#69a5d6' not to be '#69a5d6'` en «se deriva de SU origen» y la de «tal cual». (11) `el origen de «clasico» no declara estos colores del @theme: expected [ '--acento' ] to deeply equal []` y `«clasico/claro» no declara estos tokens`. (12) `expected 'dentro' to be 'fuera'` y `expected 'theme' to be 'base'` en los centinelas; **la prueba sobre la hoja real siguió verde**, porque ahí `[data-tema='clasico']` gana también por orden — sin centinela, esa guarda habría pasado con la cascada rota. (13) Sin regenerar: `El archivo de temas dejo de salir de las reglas`. Regenerado: `«clasico/claro» declara exenciones que ya no hacen falta: --mal-borde sobre --superficie` y `La exencion de «--linea sobre --superficie» en «clasico/oscuro» decia 1.88:1 y ahora mide 1.94:1` — la exención muerta y la cifra que se movió, las dos direcciones |
| **El intérprete de `@kamayuk/ui` gana el punto de extensión y los estados de una lectura: la primera parte de lo que `catastro` dibuja (#44).** `bloques` admite, además del bloque de #27, tres piezas —`{ tipo: 'aviso' }`, `{ tipo: 'pie', lee, escribe?, falta? }` y `{ tipo: 'delConsumidor', clave }`, que se busca en `<Pantalla piezas>` y recibe los mismos `datos`, `traducir` y `textos`— y **tres modificadores comunes** a todas salvo el pie: `lectura: { clave, espera? }` (`en-espera`, `pidiendo`, `fallo` con el peldaño **ya resuelto** y `reintentar` solo si puede cambiar algo, `con-datos`, en `datos.lecturas`), `cuando` (`{dato, vale}` / `{dato, hay}` sobre `datos.nombrados`) y `fallosDe`. Los textos pueden llevar un dato (`Texto` = cadena, `{plantilla}`, `{desde}`, `{segun, casos, otro?}`: `traducir` pasa por la frase y **nunca por el dato**) y el bloque gana `pie`. Los ocho huecos salen de combinar eso: `no-puede-con-motivo` **no tiene pieza**, es un aviso con `texto: {desde}` y `cuando: {hay}`. `PeldanoDeUnFallo` es un subconjunto **estructural** del `Peldano` de `@kamayuk/sesion` —`@kamayuk/ui` no lo importa— y una barrera de tipo exige que el de hoy quepa tal cual. **Dos decisiones de forma, medidas contra `rentas@6d1d3a3` y no supuestas**: (A) `DefinicionDePantalla` es **genérica con el valor por omisión de hoy**, porque ensancharla a `PiezaDeLaPantalla` da **RC=2 con 13 errores** en `rentas` (`src/catalogo.ts(69,12): error TS2339: Property 'campos' does not exist on type 'PiezaDeLaPantalla'`, y en `catalogo-de-claves.ts`, `la-siembra-abre-los-destinos`, `los-cuarenta-destinos-se-recorren` y `pantallas-del-artboard`); (B) `TextosDelInterprete` **no gana claves** —van en `TEXTOS_DE_LAS_PIEZAS`, dentro del mismo `ui/textos.tsx`— porque una cuarta da **RC=2** en `rentas`: `src/i18n/textosDelMarco.ts(133,12): error TS1360 … does not satisfy the expected type 'Record<keyof TextosDelInterprete, string>'` y `(150,11): error TS2741: Property 'pidiendo' is missing`. Y `ausencia.explicacion: ''` deja de dibujar la caja de arriba (`rentas/frontend/src` no la pasa vacía nunca). `sin-suponer-un-sistema` gana `vocabulario-catastral` (`/predio\|ficha\|catastral/i`, sin límites para que `predioId` case; 0 aciertos sobre la producción de hoy) con su línea en la muestra. Un ejemplo por hueco en `interprete/muestras.ts`, que `piezas.test.tsx` monta uno a uno con un centinela de las ocho claves. Pruebas: **502 → 552** (+46 de `piezas.test.tsx`, +1 pieza montada en `el-texto-propio-es-dato`, +3 de `sin-suponer-un-sistema`: la suposición nueva sobre producción y sobre la muestra, y el centinela de los archivos de #44), más cinco barreras y una de compatibilidad en `barreras-de-tipos.tsx` | **Siete roturas, cada una SOLA, con su copia de respaldo y restaurada comparando con `cmp`**: **R1** la clave sin registrar dibuja `null` en vez del aviso; **R2** el fallo pinta el título y el detalle del saco en vez de los del peldaño recibido; **R2b** se quita la línea de la incidencia; **R3** `const predioId = lee[0];` dentro de `PieDeOperaciones.tsx`; **R4** `{ desde }` pasa por `traducir`; **R5** `DefinicionDelPie` gana `lectura?`; **R6** `Reintentar` escrito dentro de `EstadoDeLaLectura.tsx`; **R7** se borra la línea nueva de la muestra de `supone-un-sistema.ts` | **R1**: 3 rojas de 46, `la clave «olvidada» no dejo NINGUN aviso: se dibujo un hueco en blanco: expected null not to be null`, `sin piezas, TODAS las claves avisan … expected to have a length of 2 but got +0` y la de `toString`. **R2**: 2 rojas, `fallo: dibuja EL PELDANO RECIBIDO` y `el fallo de la vecina va ENCIMA`, las dos con `Unable to find an element with the text: No tiene el permiso que esta parte necesita`. **R2b**: 1 roja, `Unable to find an element with the text: Incidencia INC-7F3A`. **R3**: `no se supone: vocabulario-catastral` en rojo con `paquetes/ui/interprete/PieDeOperaciones.tsx:37  const predioId = lee[0];`. **R4**: 3 rojas, `expected '«42»' to be '42'`, `expected '«nada»' to be 'nada'` y la del motivo del aviso. **R5**: `yarn typecheck` RC=2, `barreras-de-tipos.tsx(127,3): error TS2578: Unused '@ts-expect-error' directive.` **R6**: las DOS guardas del texto, cada una por su lado: `paquetes/ui/interprete/EstadoDeLaLectura.tsx:91  «Reintentar»  (por texto JSX)` y, montada, `«Pantalla con las piezas de #44» … «Reintentar» (por texto, en button[boton])`. **R7**: `y sobre la muestra, «vocabulario-catastral» encuentra lo que tiene que encontrar → expected 0 to be greater than 0`. Restauradas las siete: `yarn verificar` RC=0, **552 + 3 de capa** (587 tras rebasar sobre #56, que trae 35); `rentas` contra esta rama, `yarn verificar` RC=0 con **615 pruebas en 44 archivos** en `6d1d3a3` y otra vez en `7702788` (tras rentas#165), y `yarn e2e` **69 pasadas** |
| **`catastro` entra en `consumidores.json`: la CI de la libreria mide al segundo consumidor (#45).** Una linea de datos —`{"repositorio": "hneyra/catastro", "directorio": "catastro", "ruta": "frontend", "orden": "yarn verificar"}`—, sin tocar el trabajo `consumidores` ni `los-consumidores-se-miran`. El `README.md` gana «Quien la consume, y a quien mide la CI», con el recuento de `link:../../kamayuk-lib/paquetes/*` sobre el `main` de cada sistema el 2026-09-14: seis en `rentas`, `catastro` y `caja`, cero en `normativa`. **AC 1, medida otra vez**: `catastro@a5843b5` (catastro#136) en un directorio hermano del worktree, Node 22.23.2, `yarn install --frozen-lockfile && yarn verificar` → RC=0, **16 archivos, 155 pruebas**. **AC 4**: `gh api repos/hneyra/kamayuk-lib/actions/secrets` → `total_count: 0`, asi que `GH_CLONE_KEY` **no existe aqui** y `secrets.GH_CLONE_KEY \|\| github.token` cae en `github.token`; y basta porque `hneyra/catastro` es **PUBLIC** —`git ls-remote` sin credenciales da RC=0—. En la CI lo confirman el `ls-remote --comprobar` y el `actions/checkout` de la fila `hneyra/catastro`, los dos en verde | **(M1)** `"directorio": "catastro-web"` en la entrada nueva, restaurado por copia. **(M2) Lo que no hubo que provocar**: `catastro` clonado con `git clone --depth 1` —el checkout del trabajo `consumidores`— al lado de esta rama, y `yarn verificar` | **M1**: «× el `directorio` es el que el `link:` del consumidor espera» · `AssertionError: El directorio no cuadra con el repositorio: hneyra/catastro se clona en «catastro-web»: expected [ Array(1) ] to deeply equal []` · `Tests 1 failed \| 5 passed (6)`; restaurado, `6 passed`. **Y lo que la guarda protege es una convencion, no una resolucion**: con la carpeta renombrada a `catastro-web`, `catastro-web/frontend/../../kamayuk-lib` sigue cayendo en la libreria y la suite de `catastro` sale **identica** (las mismas 2 rojas de M2, 153 verdes). El `mv` del trabajo usa ese mismo campo, asi que el clon no «cae en otro sitio» como dice el comentario de la guarda; no se toca, porque cambiar la guarda no entra en #45.<br><br>**M2 — el trabajo sale VERDE sin medir nada, y la CI del PR lo confirma.** En local, RC=1 con `verificaciones/el-inventario-es-la-v6.test.ts` rojo (2 de 155): `Este clon no tiene el commit 22e6d2e de la V6, y el inventario sale de ahi. Pasa en un checkout superficial`. La CI de `catastro` lo resuelve con un paso propio (`frontend.yml`, «La V6 congelada, para el inventario», catastro#116) que el trabajo de aqui no tiene. En el PR #68, `hneyra/catastro sigue en verde con esta rama` sale **pass en 49 s** con `linea base (libreria en main): failure` · `con esta rama: failure` · `##[warning]«hneyra/catastro» ya estaba rojo con la libreria en «main». Esta rama NO es la causa`, y las dos pasadas con `Tests 2 failed \| 153 passed (155)`. **O sea que el AC 3 NO se cumple**: con la base roja, una rotura de verdad sobre `catastro` saldria como aviso y no bloquearia. El remedio es de una de dos: `fetch-depth: 0` (o el `git fetch` de ese commit) en el checkout del consumidor —cambiar el trabajo, que #45 excluye— o que `catastro` no dependa de su historial en `yarn verificar`. En el mismo PR, `hneyra/rentas sigue en verde con esta rama`: base `success`, rama `success`, **43 archivos, 616 pruebas** las dos veces. `yarn verificar` de la libreria: RC=0, **36 archivos, 502 pruebas** mas las 3 de capa |
| **El intérprete de `@kamayuk/ui` gana los actos con observación, lo impedido con motivo y la navegación entre hojas (#66).** Los cinco huecos de `HUECOS.md` como dato, en archivos propios (`interprete/{tipos-de-los-actos.ts,acciones.ts,interaccion.ts,GrupoDeAcciones.tsx,ActoDeLaPantalla.tsx}`, `ui/shadcn/boton-con-motivo.tsx`, `shell/navegacion.tsx`). **`acto-con-observacion`**: pieza `{ tipo: 'acto', clave, titulo, campos: CampoDelActo[], observacion: { etiqueta, ayuda?, largo: { minimo, maximo } }, advertencia?, impedido?, hecho? }` —la observación **obligatoria en el tipo** y sus límites **sin valor por omisión**—, que solo existe abierta (`<Pantalla actoAbierto>` controlado, o estado local) y la envía `<Pantalla actos>[clave]({ valores, observacion, parametros })`; el manejador puede devolver una promesa (en curso, una sola llamada), y el fallo es el de `datos.lecturas.get(clave)` —rechazo sin fallo = aviso de costura—. **Decisión de forma: la observación es un área de texto en todos los actos** (en la V6 era de una línea en los actos y área en el alta). **`impedido-con-motivo`**: `Impedimento = { si: Condicion, motivo: Texto }` y la pieza exportada `BotonConMotivo` (`aria-disabled` + `aria-describedby` a un texto visible; nunca `disabled`). **`acciones-del-bloque`**: `DefinicionDeBloque.acciones`, unión exclusiva `abre`/`va`/`hace` (con `?: never`: sin ellos `{ abre, va }` compilaba, medido con la barrera). **`confirmacion-de-lo-irreversible`**: `advertencia` abre la `Confirmacion` de Radix. **`navegar-a-otra-hoja`**: `<Pantalla navegacion={useNavegacion()}>`; en `@kamayuk/shell`, `ofrece` mira el índice del catálogo e `ir` devuelve `'abierta' \| 'pregunta' \| 'no-ofrecida'` por el mismo `irA`; sujeto y parámetros los escribe solo `ubicacionDe` (`#/<slug>?sujeto=…`, en la búsqueda porque `#/entradas/` tiene que seguir sin abrir nada, hasta #67). 15 claves nuevas en `TEXTOS_DE_LAS_PIEZAS`, ninguna en `TextosDelInterprete` ni en `TextosDelArmazon`. Pruebas: **587 → 631** (+35 `actos.test.tsx`, +7 `shell/navegacion.test.tsx`, +1 recorrido de una escritura entera en `el-texto-propio-es-dato`, +1 centinela de archivos en `sin-suponer-un-sistema`) y siete barreras de tipo más una de compatibilidad (`useNavegacion()` cabe en `NavegacionDeLaPantalla`). `rentas@7702788` contra esta rama: `yarn verificar` RC=0, **615** pruebas, sin cambios | **Nueve roturas, cada una SOLA, con copia de respaldo y restaurada comparando con `cmp`**: **R1** `motivoDelActo` no mira la observación (`return undefined`) → 4 rojas, entre ellas `no envia SIN observacion…`: `expected '' to be 'La observacion tiene 0 caracteres y n…'` y `sin observacion no llega ni a preguntar`: `expected <div role="alertdialog" …> to be null`; **R2a** `Impedimento.motivo` opcional → `barreras-de-tipos.tsx(184,1): error TS2578: Unused '@ts-expect-error' directive.`; **R2b** `BotonConMotivo` sin `aria-describedby` → 6+ rojas: `el boton impedido NO dice por que: expected '' to be 'Falta elegir un grupo en la lista.'`; **R3** `motivoDeLaAccion` no pregunta `ofrece` → `un destino que el catalogo NO ofrece…`: `expected null to be 'true'`; **R3b** `ir` del armazón sin `if (!indice.has(clave))` → `se pidio ir a una hoja que el catalogo no ofrece, y el marco no lo dijo: expected 'pregunta' to be 'no-ofrecida'`; **R4** el primario envía aunque haya `advertencia` → `lo irreversible se envio sin confirmar: expected "spy" to not be called at all, but actually been called 1 times`; **R5** `const minimo = 5` escrito en la librería → `los limites son DATO…`: `expected { llamadas: +0, …(1) } to deeply equal { llamadas: 1, motivo: undefined }`; **R6** «Cerrar» escrito dentro de `ActoDeLaPantalla.tsx` → `el-texto-visible-es-dato`: `paquetes/ui/interprete/ActoDeLaPantalla.tsx:184  «Cerrar»  (por texto JSX)`; **R7** sin la guarda de la doble pulsación **quitando las dos a la vez** (el `enVuelo` y el motivo «Escribiendo…») → `una doble pulsacion envio dos veces: expected "spy" to be called 1 times, but got 2 times`. **Medido y dicho**: quitando solo `enVuelo` sale VERDE —`fireEvent` vacía el estado entre dos clics y el motivo «en curso» ya corta la segunda—, así que la referencia es la segunda barrera y la prueba no la separa |
| **El intérprete de `@kamayuk/ui` expresa los campos y las tablas que `catastro` dibuja en dos hojas o más (#65).** Diez huecos de `HUECOS.md`, como dato y sin tocar `PiezaDeLaPantalla.tsx`: campos y tablas se quedan dentro del bloque y heredan `cuando`, `lectura` y `fallosDe` de #44. **Filas por nombre**: una tabla con `clave` toma `datos.tablas.get(clave) = { filas: [{ celdas, datos?, clave?, realzada? }], conteo? }`, y las reglas de la fila leen `fila.datos` con las mismas `resolverTexto` y `seCumple`; `datos.filas` por índice sigue igual para `rentas`. `tabla.vacio?: Texto` (con `[]` se dice la frase fuera de la `<table>` y **sin conteo**; sin frase, un aviso del saco `data-tabla-sin-motivo`), `CampoDeEntrada.marcador`, `ReglaDeLaInsignia` = `{ segun?, casos, otro }` \| `{ tonoDesde, siNoTrae }` en `ColumnaDeTabla.insignia` y en `CampoDeSoloLectura.insignia` (con regla, `tonoDeLaInsignia` no se llama), `OpcionDeLista { valor, rotulo }` (el vacío viaja por Radix codificado y sale `''`), `CampoDeLista.ayuda`, `bloque.tablas` (con `clave` obligatoria), `tabla.detalleDeFila { texto, cuando? }`, `tabla.accionesPorFila { columna, acciones, segun?, sinAcciones, nombreDelGrupo? }` —**las acciones son las de #66** (`AccionDeFila = DefinicionDeAccion & { clave }`: `abre` con `con`, `va` o `hace`, con su `impedida`), dibujadas con el mismo `GrupoDeAcciones` y resueltas contra los datos de la pantalla con los de la fila encima; lo único que la fila añade es cuáles ofrece— y `tabla.cabeceraFija`. `BotonConMotivo` suma su `aria-describedby` al motivo en vez de pisarlo, para que la acción lleve también el detalle de su fila. Tres reglas puras exportadas: `resolverInsignia`, `accionesQueOfrece` (una clave no declarada revienta) y `tablasSinVacio`. Dos palabras al final de `TEXTOS_DE_LAS_PIEZAS` (`tablaSinMotivo`, `accionesDeLaFila`), detrás de las de #66. **Forma medida contra `rentas@7702788`**: `CampoDeLista.opciones` no se ensancha en la definición por omisión, porque `src/i18n/catalogo-de-claves.ts:56` hace `salida.push(...campo.opciones)` sobre `string[]`; `DefinicionDeCampo<O>` y `DefinicionDeBloque<T, O>` son genéricos como `DefinicionDePantalla`. **La cabecera fija, medida en Chromium** (`rentas` servido con Vite contra esta rama, hoja de 420 px, 80 filas): marco de 331 px con `scrollHeight` 3 336, foco por Tab y diez `PageDown` → `scrollTop` 770 con el `th` quieto en `top` 88.25. Pruebas, rebasado sobre #66: **631 → 678** (+46 en `campos-y-tablas.test.tsx`, +1 pantalla montada en `el-texto-propio-es-dato`), ocho barreras de tipo en `barreras-de-campos-y-tablas.tsx`, tres archivos más en el centinela de `sin-suponer-un-sistema`. `rentas` `yarn verificar` RC=0 con 615; `catastro@4d0ca5f` `yarn verificar` RC=0 con 211 | Cada rotura sola, con copia, ejecutada y restaurada comparando con `cmp`: **R1** el aviso de tabla sin motivo apagado (`{false && filas !== undefined && …}`); **R2** el valor de la opción por `traducir` (`value={haciaRadix(traducir(valorDe(o)))}`); **R3** el tono de la columna con regla sacado del texto (`tono={tonoDeLaInsignia(celda)}`); **R4** `accionesQueOfrece({ ...definicion, segun: undefined }, …)` (toda fila ofrece todas); **R4c** los datos de la pantalla encima de los de la fila; **R5** `AccionesDeLaFila` sin `describidoPor={idDelDetalle}`; **R12** `BotonConMotivo` con `[idDelSuyo ?? tambienDescrito]` (el motivo pisa al detalle); **R6** el marco de cabecera fija sin `tabIndex: 0`; **R7** `accionesQueOfrece` se salta la clave no declarada en vez de reventar; **R8** `Sin acciones` escrito en `AccionesDeLaFila.tsx`; **R9** `const fichaDecide = 0;` en `reglas-de-las-tablas.ts`; **R10** `otro?:` opcional en `ReglaDeLaInsignia`; **R11**, en Chromium, la tarjeta sin ceder el alto (`const cedeElAlto = false;` en `BloqueDeLaPantalla.tsx`) | **R1** (3 rojas): `una tabla sin filas y sin motivo se dibujo MUDA: expected null not to be null`. **R2** (3): `el valor «UNO» paso por traducir: cambiar de idioma cambiaria lo que se envia: expected [ Array(8) ] to not include 'UNO'` y `expected [ '«UNO»', '«DOS»' ] to deeply equal [ 'UNO', 'DOS' ]`. **R3** (3): `el tono se DEDUJO del texto de la celda: expected [ Array(6) ] to deeply equal []`. **R4** (3): `expected [ <button …(4)></button>, …(1) ] to have a length of +0 but got 2`. **R4c**: `expected [ [ 'aprobar', …(1) ] ] to deeply equal [ [ 'aprobar', { registro: 'D-1' } ] ]`. **R5**: `Expected element to have accessible description: Anulacion: Duplicado · Anulado por inspector.3 · Anulado el 02/09/2026 Received:` (vacío). **R12**: `Expected … Nadie atiende «ver» en esta pantalla: pulsarlo no haria nada. Anulacion: Duplicado · … Received: Nadie atiende «ver» en esta pantalla: pulsarlo no haria nada.` **R6**: `expected <body><div>…(1)</div></body> to be <div data-slot="tabla-marco" …(4)>`. **R7**: `expected [Function] to throw an error`. **R8**: `paquetes/ui/interprete/AccionesDeLaFila.tsx:60  «Sin acciones»  (por texto JSX)`. **R9**: `no se supone: vocabulario-catastral` con `paquetes/ui/interprete/reglas-de-las-tablas.ts:72  const fichaDecide = 0;`. **R10**: `barreras-de-campos-y-tablas.tsx(60,5): error TS2578: Unused '@ts-expect-error' directive.` RC=2. **R11**: marco de **3 336 px** (sin límite), `scrollTop` **0** tras diez `PageDown` y el `th` fuera de la vista (`top` 1.25 con la página desplazada) |
| **`@kamayuk/api` sube archivos: `multipart/form-data` con avance, cancelación y los rechazos distinguidos.** Hasta hoy **ninguna interfaz del producto podía subir un archivo**, y no por descuido: el endpoint es `multipart`, `solicitar()` hace `JSON.stringify` de todo cuerpo y fija `Content-Type: application/json`, y un `fetch` suelto está prohibido en todo `src/` —la lista de excepciones de `catastro` está **vacía a propósito** (`catastro`#110)—. Medido el 2026-09-14 sobre los tres frontends que existen: **cero** `FormData`, **cero** `XMLHttpRequest`, **cero** `<input type="file">`; nadie lo había resuelto, así que no había forma que copiar. **La operación**: `Cliente.subir<T>(ruta, { archivo, nombre?, campo?, campos?, metodo?, senal?, alAvanzar?, limiteDeBytes?, admite? })`, con el mismo prefijo, el mismo `autorizacion()` —la misma función, leída en cada llamada— y el mismo `ErrorDeLaApi` que `solicitar` y `descargar`. **El `Content-Type` NO se fija a mano**: con un `FormData` lo pone el navegador con su `boundary`, y escribirlo lo manda sin separador. **Transporte: `XMLHttpRequest`, y por una sola razón** — el avance; `fetch` no lo da sin `duplex: 'half'` (Chromium, HTTP/2) y aun así mediría lo escrito y no lo recibido. Queda encerrado en `subir.ts`, vigilado por `el-xhr-vive-en-un-solo-sitio.test.ts`; **no** se añadió la décima prohibición de ESLint, y está medido por qué: cada consumidor exige la muestra de cada clave **en su árbol** y un `salvo` sin situar **lanza al cargar su config**, o sea cinco repositorios a la vez. Cancela con `senal` y rechaza con `senal.reason`, como `fetch`. Distingue los tres desenlaces: `ArchivoRechazado` con `motivo` `'demasiado-grande'`/`'tipo-no-admitido'` —local con `estado: 0` **sin mandar un byte**, o del 413/415— y el 422 de la casa como `ErrorDeLaApi` («no válido», con la regla dentro). El rechazo local no es un lujo: medido sobre `catastro`, **no hay manejador de `MaxUploadSizeExceededException`**, así que pasarse del límite (el de Spring por omisión: **1 MB/10 MB**, porque no se declara `spring.servlet.multipart`) contesta **500 `ERROR_INTERNO`**. `admite` usa la gramática del `accept` de HTML —extensión, comodín y tipo— porque el tipo de medio de un `.xlsx` llega vacío según el equipo. Las tres clases de error se mudan a `errores.ts` para romper un ciclo. **101 pruebas en `paquetes/api`** (eran 52) | **Siete roturas, una por afirmación.** (1) `setRequestHeader('Content-Type', 'multipart/form-data')` en `subir.ts`, y su variante en minúsculas. (2) El token leído **al crear el cliente** en vez de en cada subida. (3) Quitado el `removeEventListener('abort')` del `loadend`. (4) `POR_EL_ARCHIVO` vaciado, o sea 413 y 415 sin traducir. (5) Un `new XMLHttpRequest()` suelto en `paquetes/api/entregar.ts`. (6) **El ciclo, de verdad**: una clase puesta en `cliente.ts` y derivada en `subir.ts`. (7) La comprobación de `limiteDeBytes` anulada | (1) `AssertionError: alguien fijo el Content-Type a mano: la peticion sale sin «boundary»: expected 'multipart/form-data' to be undefined`, y la de la lista entera, `expected [ 'Accept', 'Authorization', …(1) ] to deeply equal [ 'Accept', 'Authorization' ]` con `+ "Content-Type"`. **La minúscula da el mismo rojo**: la prueba busca la cabecera sin mirar mayúsculas. (2) Tres rojos, entre ellos `el token se lee en CADA subida… expected undefined to be 'Bearer el-de-antes'`. (3) `cancelar despues de terminar no hace nada… expected true to be false`. (4) `expected ErrorDeLaApi: Lo que dijo el backend { …(6) } to match object { name: 'ArchivoRechazado', …(5) }`, los dos casos. (5) `Un XMLHttpRequest fuera de subir.ts…` con `+ [ "paquetes/api/entregar.ts:48" ]`. (6) **El rojo real no era el que yo había escrito en el docblock**: no `ReferenceError: Cannot access … before initialization`, sino `TypeError: Class extends value undefined is not a constructor or null ❯ paquetes/api/subir.ts:357:31 ❯ paquetes/api/cliente.ts:38:1` — se corrigió el comentario contra lo medido. (7) `pesa de mas: se rechaza AQUI, sin mandar un solo byte` en rojo. **Y la línea base**: `yarn test` deja 16 rojos en `shell`, `ui/temas` y `las-capas-corren` que **son de antes** — se midieron en un `git worktree` sobre `fc034a6`: los mismos 16, 65 pasan, 81 en total. **Consumidores** (AC-8): `catastro/frontend` con el enlace vivo da `tsc --noEmit` **RC=0**, `eslint .` **RC=0** y **287 de 288** pruebas, con el único rojo en `el-motor-dice-lo-mismo` por un `spawnSync yarn ENOENT` del entorno; `rentas/frontend` **no importa `@kamayuk/api` en `src/`** —solo `ui`, `shell` y `sesion`, medido— y lo único que le llega es por `@kamayuk/sesion`, que compila aquí |
| **El estado de una hoja vive en la ruta: maestro-detalle, pestañas, acceso por hoja y hojas a sangre (#67).** `@kamayuk/shell`: `#/<slug>/<sujeto>?<parametro>=<valor>` (`ruta.ts`: se parte por la PRIMERA barra, el sujeto se codifica entero y se descodifica sin reventar —medido en `react-router@7.18.3`: `pathname` llega sin descodificar—, parámetros en orden alfabético) y **sólo lo que `Destino.enLaRuta` declara**; lo demás se ignora con aviso (`alIgnorarDeLaRuta`, por omisión `console.warn`) y la hoja se abre igual. `#/<slug>` no cambia y `#/<slug>/` sigue «no ofrecido» (#20). `useHoja()` gana `ruta`, `moverLaRuta` y `marco`; `Destino` gana `acceso`/`tambien` (+`accesosDe`) y `aSangre`; `Armazon` gana `marco` y `enLaBarra`; la pantalla lleva `key` por destino (#61 H10). **Rebasado sobre #66**: `ubicacionDe` deja la forma provisional `?sujeto=` y escribe la de #67, y el árbol, la paleta, `ir` y `moverLaRuta` pasan por ella; una prueba lee con `leerLaRuta` lo que `ubicacionDe` escribe. `@kamayuk/ui`: piezas `maestroDetalle` (listbox, ↑/↓/Inicio/Fin mueven el foco e Intro/Espacio eligen, sin que la elección siga al foco) y `pestanas` (tablist itinerante, ←/→/Inicio/Fin activan), que escriben la ruta y la restituyen por `<Pantalla hoja={useHoja()}>` —`HojaDelMarco` es un subconjunto estructural de `HojaAbierta`, con barrera de tipo—; `nombrados` gana `ruta.*` y `marco.*` (`parametro-del-marco`); `datos.listas`; las piezas anidadas se numeran en **anchura** (`indicesDeLasPiezas`) para que las de primer nivel conserven su índice; `actoEnLaRuta(hoja)` pone el acto de #66 en `?acto=`. Ningún texto nuevo en `TextosDelArmazon` ni en `TextosDelInterprete` (rompen `rentas` al compilar): lo que dicen las piezas nuevas es de la definición. Guarda nueva `el-marco-no-decide-permisos`, centinela de #67 en `sin-suponer-un-sistema` y las piezas de #67 montadas en `el-texto-propio-es-dato`. Pruebas: **587 → 650** antes de rebasar; **695** sobre `main` con #66 | **Once roturas, cada una SOLA, con copia de respaldo y restaurada comparando con `cmp`**: **R1** la pestaña no llama a `moverLaRuta`; **R2** las pestañas ignoran la ruta y guardan la abierta en su estado; **R3** la forma de la URL cambia a `#/d/<slug>` (medido contra `rentas`); **R4** `<Fragment>` sin `key` por destino; **R5** `rutaDeLaHoja` deja pasar lo no declarado; **R6** `import type { Peldano } from '../sesion/escalera.ts'` en `catalogo.ts`; **R7** `aSangre = false`; **R8** `ubicacionDe` vuelve a la forma provisional de #66 (`?sujeto=`); **R9** mover el foco del maestro elige; **R10** `actoEnLaRuta` no escribe la ruta; **R11** (rentas) el guardia de su intérprete sobre la pieza nueva | **R1**: 5 rojas, `cambiar de pestana no escribio la ruta: expected '#/registros/A%2F42' to be '#/registros/A%2F42?ver=historial'` y `recargar no restituyo la pestana: expected 'false' to be 'true'`. **R2**: 3 rojas, `recargar no restituyo la pestana: expected 'false' to be 'true'` y `Unable to find an element with the text: 06/09/2026` (el bloque de la pestaña de la ruta). **R3**: en la librería 46 rojas de 107 en `paquetes/shell`; en `rentas` `yarn verificar` RC=1 con **46 rojas de 615** (`los-cuarenta-destinos-se-recorren`: `Unable to find an accessible element with the role "heading" and name "Panel"`) y `yarn e2e -g ini-panel` RC=1: `Error: «ini-panel» no abrio por su hash` — así saldría en el job `consumidores`. **R4**: `lo tecleado en el panel aparecio en Turnos: la pantalla no lleva \`key\` por destino: expected 'escrito en el panel' to be ''`. **R5**: 7 rojas, `expected '#/registros/41?colado=x&ver=historial' to be '#/registros/41?ver=historial'`. **R6**: `paquetes/shell/catalogo.ts:2  importa @kamayuk/sesion  «import type { Peldano } from '../sesion/escalera.ts';»`. **R7**: `a sangre…: expected false to be true`. **R8**: 10 rojas, `expected '/detalle?sujeto=A%2F1+b&estado=BAJA' to be '/detalle/A%2F1%20b?estado=BAJA'`, `expected { slug: 'detalle', sujeto: null, …(1) } to deeply equal …` (lo escrito no es lo leído) y `elegir en el maestro no escribio la ruta: expected '#/registros?sujeto=A%2F42' to be '#/registros/A%2F42'`. **R9**: `expected [ { sujeto: '41' } ] to deeply equal []`. **R10**: `abrir el acto no escribio la ruta: expected [] to deeply equal [ Array(1) ]`. **R11** no fue una rotura provocada sino un rojo de verdad al medir `rentas`: `el-interprete-no-nombra-un-sistema` salió rojo con `PestanasDeLaPantalla.tsx: nombra la clave de hoja «panel»` —el literal `'panel'` de los `id` de WAI-ARIA casa con una hoja de `rentas`—; renombrado a `'cuerpo'`/`'rotulo'`, verde. Restauradas todas: `yarn verificar` 695 pruebas en verde (las tres de capa, con la máquina a carga 13, pasaron por tiempo solo aparte); `rentas@origin/main` contra esta rama rebasada: `yarn verificar` RC=0 con **615 pruebas en 44 archivos** y `yarn e2e` RC=0 con **69 pasadas**. **Y al mezclar `main` —que entretanto se llevó #65 (los campos y las tablas) y #74 (`subir`)— los conflictos fueron cinco, y uno solo era de verdad**: `HISTORY.md` e `index.ts` eran dos añadidos al final del mismo sitio, y `datos.ts` y `el-texto-propio-es-dato.test.tsx`, dos campos y dos muestras que no se tocan. El de fondo es `Pantalla.tsx`: #67 convirtió el `map` de los bloques en el `dibujar` recursivo y #65 le había añadido al bloque dos entradas —`datosDeLasTablas` e `interaccion`—, que en el lado de #67 **no existían**. Entran en `dibujar`, así que las tablas con nombre y las acciones de fila **también valen dentro de una pestaña o de un detalle**. No es una suposición: `tsc` ya obliga a `interaccion`, que es obligatoria, y quitando `datosDeLasTablas={datos.tablas}` de la línea mezclada `campos-y-tablas.test.tsx` da **16 rojas de 46** —`Unable to find an accessible element with the role "cell" and name "C-01"`, `Unable to find an element with the text: «Vigente»`, `…role "button" and name "Aprobar"`—, y restaurada vuelve a **46 / 46**. Mezclado: `yarn verificar` RC=0 con **791 pruebas en 47 archivos** (695 + las 96 que traía `main`) más **3 / 3** de capa. **Y `CLAUDE.md` deja de contar el historial**: su tabla «Qué hay hoy» era una crónica encadenada —«Capa 1 hecha (#6)… Y desde #19… #23… #33… #44… #56… #66…»—, que es la forma de ESTE archivo, donde cada uno de esos issues ya tiene su fila: estaba dicho dos veces, y `CLAUDE.md` se carga entero en cada sesión. Ahora cada fila dice **qué hay y qué no se toca** y manda aquí para el porqué; las cifras se **midieron** paquete a paquete (67/3, 101/5, 45/2, 364/20, 113/9, 101/8, que suman las 791) en vez de heredarse, el intérprete gana fila propia y las 17 referencias a issues que la tabla citaba **tienen todas su fila aquí**. De 23 815 a 19 728 bytes. **Y la fila del intérprete dice también los campos y las tablas de #65**, que `main` trajo sin documentar: medido en el árbol y no copiado del PR que lo trajo —los **siete** tipos de campo (`tipoDe` revienta ante un octavo, y el `switch` de `CampoDelBloque` tiene esas siete letras), la tabla con `clave` que toma sus filas de `datos.tablas` por nombre, los **tres** estados de una tabla (`TablaDelBloque.tsx:90`), el `role="group"` con nombre de las acciones de fila, y **tres** reglas puras contadas con `grep -c '^export function' reglas-de-las-tablas.ts`—. Con ellas se escribe un límite que no estaba en ningún sitio: **`cabeceraFija` sólo se mira en el primer nivel**, así que una cabecera fija dentro de una pestaña o de un detalle no hace que la hoja ceda el alto. **Y el consumidor se midió a mano, porque el job no puede**: los tres `consumidores` caen en el `actions/checkout` con `Not Found - …get-a-repository` —`github.token` no alcanza a otro repositorio privado, y pasa igual en `main`, cuyo último verde es `fc034a6`—, así que se clonó `rentas@7702788` hermanado con este árbol y se corrió su `yarn verificar` **dos veces con el mismo árbol del consumidor**, que es lo que el job hace: con la librería en `origin/main`, **614 de 615**; con esta rama, **614 de 615**. La misma única roja en las dos, `el-puerto-del-arnes-sale-del-arbol.test.ts`, y es **del entorno**: levanta un servidor en `[::1]` y este contenedor no tiene IPv6 (`Error: listen EAFNOSUPPORT … ::1`, confirmado aparte con un `socket(AF_INET6)` que da `errno 97`); el archivo no importa **nada** de `@kamayuk` (`grep -c '@kamayuk'` → 0). Línea base y rama, idénticas: esta rama no rompe a `rentas` |
| **`caja` entra en `consumidores.json`: la CI de la libreria empieza a medir la ventanilla (#75).** Una linea de datos —`{"repositorio": "hneyra/caja", "directorio": "caja", "ruta": "frontend", "orden": "yarn verificar"}`—, sin tocar el trabajo `consumidores` ni `los-consumidores-se-miran`, que ya lee la lista entera: la guarda del `directorio` es generica y reconoce la entrada nueva sin cambio. `caja/frontend` se rehizo sobre estas librerias en `caja`#83 y `caja`#87, y su `frontend/package.json` en `main` (`82435fe`) trae **seis** `link:../../kamayuk-lib/paquetes/*`, medido el 2026-09-16 con `gh api`. **El censo del `README.md` estaba atrasado dos veces**: decia `caja` «todavia no» —ahora «si, desde #75»— y **no nombraba a `ciudadano`**, que entro en la lista con #56; gana su fila con el recuento del mismo dia (**tres** `link:`). El `_comentario` del JSON nombra ya a los tres que entraron despues de `rentas`. **El clon, esta vez, necesita la llave**: `hneyra/caja` es **PRIVATE** y `gh api repos/hneyra/kamayuk-lib/actions/secrets` lista `GH_CLONE_KEY`, asi que `secrets.GH_CLONE_KEY \|\| github.token` ya no cae en `github.token` como en #45 | **(M1)** `"directorio": "caja-web"` en la entrada nueva —el nombre que `caja` le da a su interfaz, y por eso el error plausible—, con `npx vitest run paquetes/verificaciones/los-consumidores-se-miran.test.ts` en `node:22` por Docker; restaurado por copia y comparado con `cmp` (identico) | **M1**: «× la CI mira a sus consumidores > el `directorio` es el que el `link:` del consumidor espera» · `AssertionError: El directorio no cuadra con el repositorio: hneyra/caja se clona en «caja-web»: expected [ Array(1) ] to deeply equal []` · `Tests 1 failed \| 5 passed (6)`; restaurado, `los-consumidores-se-miran` y `la-rama-del-consumidor` dan `Test Files 2 passed (2)` · `Tests 24 passed (24)`. **AC 3, en la CI del PR #76** (corrida `35030608880`, commit `52f6b53`): «hneyra/caja sigue en verde con esta rama» **pass en 2 min 37 s**, con `caja@82435fe` en su rama por omision, `linea base (libreria en main): success` · `con esta rama: success`, y las dos pasadas con `Test Files 35 passed (35)` · `Tests 356 passed (356)`. El `actions/checkout` del consumidor privado cierra con la llave. En la misma corrida, base y rama en `success` para los otros tres: `rentas` **615**, `ciudadano` **427** y `catastro` **310** pruebas; y «Lint, tipos y pruebas» y `Registro` en verde |
| **El comentario del `directorio` decia un sintoma que no ocurre, y ahora dice el medido (#77).** Visto al implementar #75 y ya anotado en la fila de #45. El comentario de «el `directorio` es el que el `link:` del consumidor espera» decia que un campo torcido deja el clon «en otro sitio» y que «el enlace resuelve al clon equivocado». **Ni una cosa ni la otra**: el `mv consumidor '${{ matrix.directorio }}'` del workflow usa ESE mismo campo —`paquetes.yml`, paso «Ponerlo donde el enlace lo busca»—, asi que el clon cae exactamente donde el campo diga, y `cache-dependency-path` y los tres `working-directory` de despues lo siguen con `format('{0}/{1}', matrix.directorio, matrix.ruta)`. Lo que el `link:` mira es la **profundidad**, no el nombre: los seis de `caja/frontend/package.json` son `link:../../kamayuk-lib/paquetes/*`, que desde `<directorio>/frontend` suben dos niveles hasta la raiz del espacio de trabajo, que es donde `actions/checkout` deja la libreria con `path: kamayuk-lib`. El comentario dice ahora eso, dice el rojo que de verdad sale y **declara que la comprobacion es un proxy**: exigir el ultimo trozo del repositorio garantiza un solo tramo y descarta el nombre `kamayuk-lib` —las dos formas de tumbar la resolucion— pero es mas estricta que ella. Sin cambio de codigo ejecutable: solo el comentario | **(M0) Medido en un banco con la disposicion del job**, `node:22` por Docker con yarn 1 —el de los cuatro consumidores, que no declaran `packageManager`—: `kamayuk-lib/paquetes/formato` mas tres consumidores con el mismo `link:../../kamayuk-lib/paquetes/formato`, en `caja/frontend`, `caja-web/frontend` y `a/b/frontend`. **(M1)** `"directorio": "caja-web"` en la entrada de `hneyra/caja` de `consumidores.json`, con `npx vitest run paquetes/verificaciones/los-consumidores-se-miran.test.ts`; restaurado por copia y comparado con `cmp` (identico) | **M0**: `caja/frontend` y `caja-web/frontend` dan los dos `yarn install` RC=0 y `require('@kamayuk/formato')` → `la libreria`: **el nombre torcido a la misma profundidad resuelve IGUAL**, como ya midio #45 con `catastro-web`. `a/b/frontend` —dos tramos— tambien da `yarn install` **RC=0** y deja el symlink colgando (`node_modules/@kamayuk/formato -> ../../../../kamayuk-lib/paquetes/formato`), y el rojo no llega hasta el import: `Error: Cannot find module '@kamayuk/formato'` · `code: 'MODULE_NOT_FOUND'`. **M1**: «× la CI mira a sus consumidores > el `directorio` es el que el `link:` del consumidor espera» · `AssertionError: El directorio no cuadra con el repositorio: hneyra/caja se clona en «caja-web»: expected [ Array(1) ] to deeply equal []` · `Tests 1 failed \| 5 passed (6)`; restaurado, `Test Files 1 passed (1)` · `Tests 6 passed (6)`, y `npx eslint` del archivo sin una sola linea. **Lo que se ve y no se toca**: la guarda comprueba **menos** de lo que su nombre promete —el nombre, no la resolucion—, y ensancharla es otro issue |
| **La pila de capas vive en un solo sitio, y una guarda la vigila (#81).** `paquetes/ui/shadcn/capas.ts` declara las nueve capas del producto como cadenas literales —`z-[1]` la cabecera fija, `z-[79]` la barra global, 84/85 la paleta, 85/86 el cajón, 88/89 la confirmación y **`z-[90]` toda superficie flotante**— y los ocho archivos que las usaban dejan de escribir el número. **Literales y no números porque Tailwind v4 descubre las clases leyendo el TEXTO**: un `` `z-[${CAPA}]` `` compuesto en ejecución no lo ve el escáner y la regla no se emite, que es peor que el número equivocado. La regla que faltaba, escrita: **una superficie flotante tiene que estar por encima de cualquier superficie desde la que se pueda abrir**; el reparo obvio —que una confirmación deba tapar un desplegable abierto— no puede ocurrir, porque `Confirmacion` es modal de Radix y al abrirse atrapa el foco, lo que cierra la capa. `las-capas-se-apilan.test.ts` comprueba las dos mitades: que **ninguna clase de capa viva fuera de `capas.ts`** y que toda flotante esté por encima de toda modal. Viene de `pcf`, que lo parcheó en su propia hoja de estilos **dos veces** —T2 puso 87, T5 tuvo que subirlo a 90— y las dos veces lo encontró un navegador; ese parche **desaparece** con este cambio. La entrada de `pcf` en `consumidores.json` va **aparte** (#82) y **no con esto**: ese `job` clona la rama por omisión del consumidor y hoy `hneyra/pcf` sólo tiene el commit inicial, así que saldría rojo por no encontrar nada que verificar. Primero se empuja `pcf`. **4 pruebas nuevas, más su muestra** | **Tres roturas, dos en la guarda y una de punta a punta.** (1) Devolver `z-50` al desplegable, o sea la primera mitad. (2) Bajar `CAPA_FLOTANTE` a `z-[87]`, o sea la segunda. (3) **La de verdad**: con `CAPA_FLOTANTE` en 87, reconstruir el bundle de `pcf` y correr su Playwright. Y además la muestra `capa-flotante-por-debajo-del-velo.ts`, que viola las dos mitades a propósito y está declarada en `otras-muestras.ts` — sin declararla, `reglas-de-eslint` sale roja diciendo que sobra una muestra sin dueño, que es como se comprobó que ese registro también muerde | (1) `AssertionError: la pila de capas vive en 'paquetes/ui/shadcn/capas.ts' y en ningun otro sitio: expected [ Array(1) ] to strictly equal [] + "paquetes/ui/shadcn/desplegable.tsx: z-50"`. (2) `expected 'CAPA_FLOTANTE (87) no esta por encima de CAPA_VELO_DE_CONFIRMACION (88)' to be undefined`. (3) **El rojo original, reproducido desde la librería**: `<div data-slot="velo-de-confirmacion" class="fixed inset-0 z-[88] bg-velo"> intercepts pointer events` — el mismo mensaje, palabra por palabra, que `pcf` vio en T5. Restaurado el 90 y reconstruido, pasa en 1,2 s. **En esta máquina la línea base ya traía 65 rojos** (Node 26 contra `engines: >=22`: `localStorage` sin definir y el `AbortSignal` de undici), idénticos antes y después; lo verde de este cambio se midió como delta: lint, tipos y las 3 de capa limpias, 65 → 65 rojos y 726 → 730 en verde |
| **`@kamayuk/api` deja leer la respuesta tal cual y mandar la clave de idempotencia (#57).** Dos cosas que la V6 de `normativa` hacía con su cliente propio y que no cabían por aquí. **`solicitarRespuesta(ruta, opciones?)`** devuelve `{ estado, cabeceras, texto }` con el cuerpo **sin `JSON.parse`, sin `JSON.stringify` y sin `trim`**: un recurso firmado se verifica sobre los bytes que llegaron, y reserializar el objeto da otro texto —y por tanto otra huella— que *casi siempre* coincide, la peor de las propiedades. **No es una segunda petición: es la misma.** `solicitar` y `solicitarRespuesta` salen las dos de `pedir()`, así que el prefijo, el token leído en cada llamada, el `Accept`, el `Content-Type`, la señal y el `ErrorDeLaApi` por la misma `problemaDe` **son el mismo código** y no una promesa del docblock. Y **no mira el `Content-Type`**: un 200 con JSON es su caso normal, lo contrario que `descargar()`, y una prueba pone las dos llamadas una al lado de la otra. Las `cabeceras` son `Omit<Headers, 'append' \| 'delete' \| 'set'>`: se leen, no se reescriben. **`claveDeIdempotencia?`** entra en `OpcionesDeSolicitud` —así vale para las dos—, sale como `Idempotency-Key` y **en blanco lanza antes de llamar a `fetch`**, porque el backend trata una clave en blanco como si no hubiera clave (`EmitirCertificado.java:151` de `rentas`) y el reintento duplicaría la escritura sin avisar. **Ninguna cabecera libre**: `OpcionesDeSolicitud` no gana `cabeceras` ni `headers` —por ahí se sobrescribiría el `Authorization` o viajaría el inquilino **sin que ninguna prohibición de ESLint lo viera**, porque el identificador prohibido no aparece— y una prueba espía el cable comprobando la **lista entera** de cabeceras, no que contenga las suyas. **Lo que NO entra, y está dicho**: comprobar la huella (`crypto.subtle`, el `ETag` fuerte, el `W/`) es de `normativa` #67, y generar la clave, de `normativa` #68. **17 pruebas nuevas**: `paquetes/api` pasa de 101 a **118** y el total de 791 a **808** | **Criterio 2 (los bytes exactos)** — `texto: JSON.stringify(JSON.parse(await respuesta.text()))` en `solicitarRespuesta`: `Tests 2 failed \| 36 passed (38)` · «el texto es identico al que se sirvio…» · `AssertionError: expected '{"a":1,"b":"ó Resolución"}' to be '{"a": 1.0, "b": "ó Resolución"}' // Object.is equality`, y de propina «un cuerpo vacio es cadena vacia» · `promise rejected "SyntaxError: Unexpected end of JSON input" instead of resolving`. **Criterio 3 (la cabecera)** — quitado `...idempotencia(opciones.claveDeIdempotencia)` de las cabeceras de `pedir()`: `Tests 6 failed \| 32 passed (38)` · «con clave, «solicitar» manda Idempotency-Key tal cual» · `AssertionError: expected undefined to be '0f8c2a11-…'`, las tres de la clave en blanco · `promise resolved "{ ok: true }" instead of rejecting` —o sea que **la escritura habría salido**— y la del conjunto exacto · `expected [ 'Accept', 'Authorization', …(1) ] to deeply equal [ 'Accept', 'Authorization', …(2) ]`. **Criterio 4 (sin cabeceras libres)** — añadido `readonly cabeceras?: Record<string, string>` a `OpcionesDeSolicitud`: `yarn typecheck` RC=2 · `barreras-de-tipos.tsx(213,5): error TS2578: Unused '@ts-expect-error' directive.` y `(217,5)` igual. **Y la barrera de las cabeceras de solo lectura** — `CabecerasDeLaRespuesta = Headers` a secas: RC=2 · `barreras-de-tipos.tsx(228,3)` y `(230,3)`, los dos TS2578 | `yarn lint` limpio, `yarn typecheck` RC=0, `paquetes/api` **118 pruebas en 5 archivos** y `paquetes/verificaciones` **101**, las dos en verde. **Lo que se vio y no se toca**: en este equipo (Node 24) fallan **21 pruebas de `paquetes/shell`** —`armazon`, `navegacion` y `estado-en-la-ruta`— con `TypeError: RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal` desde `react-router`; **medido sobre el árbol sin tocar (`git stash`), fallan las mismas 21**, así que es del entorno y no del cambio: la CI corre Node 22 y `main` está verde |
| **Los nombres de importe pasan a ser del PRODUCTO, y `cifra-tributaria-literal` entra como OPCIONAL (#58).** `CAMPOS_DE_DINERO` deja de ser la lista de `rentas` y gana `uit\|alicuota\|arancel\|valorUnitario\|valorArancelario\|valorReferencial\|valorNumerico\|valorM2\|deduccion\|depreciacion\|reajuste\|baseImponible`; la lista de `reduce` de `aritmetica-con-importes` gana `parametros`. **Es una UNIÓN y no un parámetro por sistema**, por lo mismo que cerró `rentas`#137: dos listas en verde midiendo cosas distintas. La décima de la V6 de `normativa` entra **fuera de `PROHIBICIONES`**, en `PROHIBICIONES_OPCIONALES` + `REGLAS_OPCIONALES` (segunda lista escrita a mano, con sus dos direcciones comprobadas), con su muestra `muestras/cifra-tributaria-literal.ts` de **cinco formas** y un segundo linter que la enciende. `prohibiciones.d.mts` declara los dos exports nuevos (#46). **Los cuatro campos decimales del snapshot de `normativa`, decididos**: `valorNumerico` y `valorM2` **entran**; `porcentaje` y `valor` **no**, y a los dos los cubre la guarda de formas de la API —los dos llegan como `"texto"` en `docs/50-api/formas-de-la-api.json`—, no ESLint. **101 → 144 pruebas** en `paquetes/verificaciones`; 791 → **834** en total. | **Seis roturas, cada una aplicada sola y restaurada con `cmp`.** (R1) quitar `arancel` de la unión; (R2) dejar `LITERAL_DE_CIFRA` en sólo `Literal[value=type(number)]`; (R3) quitar `parametros` de la lista de `reduce`; (R4) meter `porcentaje` en la unión; (R5) meter `tramos` en la lista de `reduce`; (R6) meter `valor` en la unión. **Y la medición que decide el diseño entero, hecha contra el consumidor de verdad**: meter `cifra-tributaria-literal` dentro de `PROHIBICIONES` (10 claves, 9 reglas exigidas) y correr `yarn verificar` de `rentas/frontend` con el `link:` apuntando a esta rama. | **(R1) 1 en rojo**, y nombra el nombre: «`arancel` salio de la union de CAMPOS_DE_DINERO: es un campo de dinero sin vigilar» · **(R2) 3 en rojo**, y las tres nombran la forma en CADENA: «La forma «CADENA en una constante» de la muestra dejo de estar senalada», «propiedad de una clase» y «con la opcional encendida, roja: expected '' to match /Ninguna cifra tributaria literal/» — la muestra entera seguía en verde con `toContain`, y es justo por eso que las cinco formas se juzgan una a una · **(R3) 1 en rojo**: «un `reduce` sobre los parametros del conjunto sale rojo: expected '' to match /Aritmetica con un importe/» · **(R4) 1 en rojo**: «`porcentaje` a secas entro en CAMPOS_DE_DINERO y pone rojo a `catastro` por un indice de columna» (`catastro:src/pantallas/piezas/avance-por-fila.tsx:37`) · **(R5) 1 en rojo**: «`tramos` entro en la lista de `reduce` y pone rojo a `catastro` por sumar longitudes» (`codigo-por-tramos.tsx:87`, `suma + t.digitos`) · **(R6) 1 en rojo**: «`valor` a secas … pone rojo a `catastro` por el numero de una columna» (`columnas-de-la-respuesta.tsx:42` y `opciones-de-una-lectura.tsx:33`). <br><br>**Y la medición de la opcional: `rentas` cae con 2 rojos, sin tocar una línea suya**, exactamente los dos previstos y por dos motivos distintos: «La prohibicion «cifra-tributaria-literal» no tiene muestra que la viole» —su prueba exige la muestra **en SU árbol**, y tiene nueve— y «las reglas no senalan codigo correcto > el codigo que las respeta pasa limpio», que es la línea `export const alicuotaPredial = '0.006';`. **La misma línea es correcta en `rentas` y roja en `normativa`**, y eso no es un desacuerdo: es la diferencia entre consumir una cifra y publicarla. <br><br>**Los consumidores, medidos a mano con el `link:` apuntando a esta rama y contra la línea base**: `rentas` 44 archivos / **615 pruebas**, idéntico con la rama y con `main`; `catastro` 45 archivos / **310 pruebas**, idéntico también. (Las dos salidas terminan en el mismo `Unhandled Rejection` de `react-router` bajo Node 24 **en las dos pasadas**; la CI corre Node 22.) |
| **`pcf` entra en `consumidores.json`: la CI de la libreria empieza a medir el Catastro Fiscal del MEF (#82).** Una linea de datos —`{"repositorio": "hneyra/pcf", "directorio": "pcf", "ruta": "frontend", "orden": "yarn verificar"}`— sin tocar el trabajo `consumidores` ni `los-consumidores-se-miran`, que ya leen la lista entera. `pcf` es el port del sistema MEF Catastro Fiscal (Django 3.2 + Angular 12) a Spring Boot 4.1 y React 19, y su `frontend/package.json` en `main` trae **cuatro** `link:../../kamayuk-lib/paquetes/*` —`ui`, `shell`, `api` y `formato`—, medido el 2026-09-16 con `gh api` sobre el remoto. **No enlaza `sesion`** y no es un olvido: su login es el JWT HS256 propio del sistema que porta, con captcha, no PKCE contra Keycloak. Su `yarn verificar` encadena lint, tipos y **1 169 pruebas**; su arnes de navegador —84 specs de Playwright— queda fuera, igual que en los otros cuatro. **El clon necesita la llave**: `hneyra/pcf` es **PRIVATE** (`gh api repos/hneyra/pcf --jq .visibility`), asi que `secrets.GH_CLONE_KEY \|\| github.token` no puede caer en `github.token`. El censo del `README.md` gana su fila y el recuento del mismo dia. **Entro DESPUES de #81 y no con el**, a proposito: ver la ultima columna | **(M1)** `"directorio": "pcf-frontend"` en la entrada nueva —el error plausible, porque lo que se clona es su `frontend/`—, con `npx vitest run paquetes/verificaciones/los-consumidores-se-miran.test.ts`; restaurado por copia y comparado con `cmp`. **(M2) Y el ensayo que de verdad importaba, porque esta linea hace correr una suite ajena**: se monto **la misma disposicion que el trabajo `consumidores` arma** —clon `--depth=1` de `hneyra/pcf` junto a una copia de `kamayuk-lib`, como `<x>/pcf/frontend` al lado de `<x>/kamayuk-lib`— y se corrio `yarn install && yarn verificar` desde cero | **(M1)** `AssertionError: El directorio no cuadra con el repositorio: hneyra/pcf se clona en «pcf-frontend»: expected [ Array(1) ] to deeply equal []`. **(M2)** `Test Files 42 passed (42) · Tests 1169 passed (1169)`, en **20,2 s** desde un clon limpio: el `link:` resuelve y la suite del consumidor pasa, asi que esta linea no entra roja. **Y por eso #82 no viajo con #81**: cuando la pila de capas se preparo, `hneyra/pcf` solo tenia su commit inicial —el trabajo estaba en local, 28 commits por delante— y este `job` clona la rama por omision del consumidor. Anadir la linea entonces habria dado rojo **por no encontrar `frontend/`**, que es un rojo que no dice nada de la libreria. Se separo, se empujo `pcf`, y entonces entro |
| **Las tablas del intérprete aprenden lo que `normativa` dibuja, y la paginación y el orden SUBEN de `catastro` (#61, primera parte).** Seis huecos de `normativa/frontend/diseno/HUECOS.md` que ni #44 ni su reparto (#65, #66, #67) cubren: **H01** `paginacion-y-orden-en-el-servidor` —`tabla.paginacion` y `tabla.orden`, con la página, el tamaño, el campo y el sentido **en la ruta** (`hoja`) y, sin marco, en el estado de la tabla; los dos mandos son `BotonConMotivo` (#66) y el orden va por **lista blanca**, con `aria-sort` en la columna cuyo `campo` es el que se ordena—; **H21** `tablas-grandes` —`paginacion.en: 'cliente'`, que corta en memoria—; **H22a** `celda-nula-con-palabra-y-nota` —`CeldaDeLaTabla = string \| { texto: string \| null, nota? }` y `tabla.sinDato`—; **H23** `cabecera-con-campo-y-dominio` —`columna.campo`/`columna.dominio` y, con N5, `campo.campo` en la etiqueta—; **H26** `vacio-con-su-salida` —`tabla.vacioConSalida: { titulo, texto?, acciones }`, con las acciones de #66, **campo aparte y no una union con `vacio`**—; y **H34** `filas-de-contenido-que-viajan` —`tabla.filasDeContenido`, las filas que SON el texto de la pantalla—. Tres reglas puras nuevas en `reglas-de-las-tablas.ts` (`paginaDeLaTabla`, `textoDeLaCelda` y `notaDeLaCelda`) y `cambiosEn` en `hoja.ts`, para que cambiar de orden y volver a la primera página sea **un solo** movimiento de la ruta. Catorce palabras nuevas en `TextosDeLasPiezas` —el saco aditivo, no `TextosDelInterprete`—. **La subida es ADITIVA a propósito**: las claves de las muestras son los nombres de los huecos de `normativa` (`paginacion-y-orden-en-el-servidor`, `tablas-grandes`) y **no** `paginacion` ni `orden`, que son las de las piezas locales de `catastro` (catastro#137); su guarda `las-piezas-locales-son-las-de-huecos` lee las claves de todo `MUESTRAS…` de `interprete/` y habría salido roja con las suyas. Su adopción es catastro#144, y **aquí no se tocó `catastro`**. **892 pruebas en 48 archivos** (+41) | **Seis roturas, y la sexta la encontro la CI.** (1) `const dibujadas = todas;` en `TablaDelBloque.tsx` —pintar las 54 129 filas en vez de la página—. (2) `{leido ?? ''}` en lugar de `<SinDato>` para una celda `null`. (3) Los mandos de página con `<Boton disabled>` en vez de `BotonConMotivo`. (4) `{textos.paginaAnterior}` sustituido por la palabra `Anterior` escrita dentro de la pieza. (5) `const alicuota = tamanos.length;` en `MandosDeLaTabla.tsx`. **(6) No fue una rotura buscada sino el primer intento**: `vacio?: T \| VacioDeLaTabla<T>`, o sea ensanchar un campo que ya existia en vez de anadir uno nuevo | **(1)** no es una aserción sino el proceso: `FATAL ERROR: Ineffective mark-compacts near heap limit Allocation failed - JavaScript heap out of memory` tras `120 949 ms` y 2 GB, y detrás `Error: Channel closed` del trabajador de Vitest — **montar todas las filas no es «más lento», es un `yarn verificar` que no termina**. **(2)** dos rojos: `expected  to have a length of 1 but got +0` (no hay ningún `[data-celda-sin-dato]`) y `expected undefined to be 'Ninguna operacion publica este dato.'`. **(3)** `expected null to be 'true'` sobre `aria-disabled`, y `el mando impedido salio del orden del tabulador: expected <body>…</body> to be <button data-slot="boton" …>` — o sea que con `disabled` el foco **no llega al botón**. **(4)** las **dos** guardas del texto a la vez: `el-texto-visible-es-dato` con `paquetes/ui/interprete/MandosDeLaTabla.tsx:150  «Anterior»  (por texto JSX)` y `el-texto-propio-es-dato` con `«Pantalla con las tablas de #61» deja llegar texto que NO pasó por el saco: «Anterior» (por texto, en button[boton-con-motivo])`. **(5)** `sin-suponer-un-sistema` > `vocabulario-tributario`: `paquetes/ui/interprete/MandosDeLaTabla.tsx:128  const alicuota = tamanos.length;`. Y una medición de propina: `const alicuotaDelPredio = …` **sólo** disparó `vocabulario-catastral`, porque el patrón tributario lleva `alicuotas?\b` y detrás de `alicuota` venía una letra — el límite de palabra no existe dentro de camelCase, que es la lección de #2 vista desde el otro lado. <br><br>**(6) LO QUE LA CI ENCONTRO Y EL ARBOL LOCAL NO PODIA**: con la union, `caja` —que no esta clonado en este espacio de trabajo— deja de compilar: `src/i18n/catalogo-de-claves.ts(64,50): error TS2345: Argument of type 'string | VacioDeLaTabla<string>' is not assignable to parameter of type 'string'`, y el veredicto del job: `ESTA RAMA ROMPE A «hneyra/caja». Con la libreria en «main» su suite sale en VERDE; con esta rama, en ROJO.` `caja` recorre sus definiciones metiendo `tabla.vacio` en una lista de claves de traduccion, y sobre una union esa lectura no vale. **Es exactamente el defecto que #44 dejo escrito para `DefinicionDePantalla` y `TextosDelInterprete`, en un campo donde nadie lo habia previsto**: lo nuevo tiene que ser ADITIVO, porque estas definiciones las RECORREN cuatro sistemas. Arreglado con `vacioConSalida` como campo propio —gana a `vacio` si estan los dos, con su prueba— y **sin tocar `caja`**. Los otros tres consumidores (`rentas`, `ciudadano`, `catastro`) salieron verdes en esa misma pasada: la union solo rompia a uno, que es justo por lo que el job mide a los cuatro. <br><br>**Vocabulario propio, a cero (AC-10):** el barrido de `conjunto\|sellad\|ambito\|snapshot\|normativa` sobre el código de producción de `paquetes/ui` y `paquetes/shell` **sin comentarios** da **1 hallazgo, y es previo a este cambio**: `ui/interprete/muestras.ts:146`, una frase de una muestra de #44. En lo añadido aquí, cero. <br><br>**Los consumidores, medidos a mano con el `link:` apuntando a esta rama y contra la línea base**: `catastro` **45 archivos / 310 pruebas**, idéntico con la rama y con `main` —`las-piezas-locales-son-las-de-huecos` incluida—, y vuelto a medir tras el arreglo de (6). (Las dos pasadas terminan en el mismo `Unhandled Rejection` de `react-router` bajo Node 24, **en las dos**; la CI corre Node 22, y por lo mismo los 3 archivos de `paquetes/shell` salen rojos en local antes y después de este cambio, con los mismos 21 casos.) |
| **`normativa` entra en `consumidores.json`: la CI de la libreria mide al quinto consumidor, y la lista queda completa (#60).** Una linea de datos —`{"repositorio": "hneyra/normativa", "directorio": "normativa", "ruta": "frontend", "orden": "yarn verificar"}`— sin tocar el trabajo `consumidores` ni `los-consumidores-se-miran`, que ya leen la lista entera. **La precondicion se midio antes de escribirla**: `normativa@6cea153` (`main`, PR `normativa`#78, que cierra `normativa`#55) trae **cinco** `link:../../kamayuk-lib/paquetes/*` en `dependencies` —`api`, `formato`, `sesion`, `shell` y `ui`—. **No enlaza `verificaciones`**, y no es un olvido: sus diez prohibiciones de ESLint son suyas, y la decima —`cifra-tributaria-literal`— aqui es OPCIONAL desde #58 porque `normativa` es justo el sistema que PUBLICA las cifras. **Su suite no lee nada fuera de su propio clon**: `grep` sobre `frontend/src` y `frontend/verificaciones` no deja ni un `../../../` que salga del repositorio —las tres que el issue midio leyendo `../../../backend` y `../../../docs` sobre la V6 (`c01fe9a`) desaparecieron con la reconstruccion, y de todas formas apuntaban dentro del repositorio que el trabajo clona entero— y el unico hermano que nombra es `../../kamayuk-lib` en dos `@source` de `estilos.css`, que es el que el trabajo clona. **No necesita `git fetch` como `catastro`**: ninguno de sus **20** archivos de prueba invoca `git` por subproceso —`grep -n 'execFileSync\|execSync\|spawnSync'` sobre `frontend/verificaciones` no devuelve nada—, asi que el clon sin historia le basta. **El clon NO necesita la llave**: `hneyra/normativa` es **PUBLIC** (`gh api repos/hneyra/normativa --jq .visibility`), asi que `secrets.GH_CLONE_KEY \|\| github.token` cae de pie por los dos lados. El `README.md` gana su fila, el recuento del mismo dia y **deja de decir «Vacío»** para `@kamayuk/ui` y `@kamayuk/shell` y «13 pruebas» para `@kamayuk/verificaciones`, que era el desfase que el AC-5 nombra | **(M1)** `"directorio": "normativa-web"` en la entrada nueva —el error plausible, porque asi se llama su frontend, `@kamayuk/normativa-web`—, con `yarn test paquetes/verificaciones/los-consumidores-se-miran.test.ts`; revertido con el `sed` inverso y comprobado con `git diff`. **(M2) Y el ensayo que de verdad importa, porque esta linea hace correr una suite ajena**: se monto **la misma disposicion que el trabajo `consumidores` arma** —`<x>/normativa/frontend` al lado de `<x>/kamayuk-lib`— y se corrio `yarn install --frozen-lockfile && yarn verificar` desde cero, **con las dos versiones de Node**: la del corredor (22) y la de este portatil (24) | **(M1)** `AssertionError: El directorio no cuadra con el repositorio: hneyra/normativa se clona en «normativa-web»: expected [ Array(1) ] to deeply equal []`, 1 de 6 en rojo; revertido, **6 de 6**. **(M2)** Con **Node 22.14.0** —el `.nvmrc` de `normativa/frontend` y el `node-version: "22"` del trabajo— `Test Files 20 passed (20) · Tests 183 passed (183)`, RC=0 en 38 s: el `link:` resuelve y la suite del consumidor pasa, asi que esta linea no entra roja. **Con Node 24.19.0 sale ROJA, y hay que decirlo para que nadie lo persiga**: 183 pruebas pasan igual pero el proceso muere con RC=1 por **15 rechazos sin capturar**, `TypeError: RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal` desde `undici` dentro de `react-router`, y **le pasa igual a esta libreria** (`3 failed \| 46 passed`, 1 658 errores, misma traza en `paquetes/shell/navegacion.test.tsx`). Es de la maquina, no del consumidor ni de esta rama: el corredor instala 22. **Y la libreria con la linea puesta**, en Node 22: `Test Files 49 passed (49) · Tests 896 passed (896)` mas `3 passed (3)` de capa, RC=0 |
| **La guarda del `directorio` deja de ser un proxy y comprueba lo que decide la resolucion (#79).** Visto al cerrar #77, que ya habia declarado el hueco sin tocarlo. `los-consumidores-se-miran.test.ts` comprobaba **solo el nombre** —que `directorio` fuera el ultimo trozo del repositorio—, que protege de rebote: es mas estricto que la resolucion y **ciego a la mitad del camino**, porque `ruta` no la miraba nadie. Ahora son **tres renglones**, cada uno diciendo lo que es. **(1) La profundidad**, que DECIDE: `<directorio>/<ruta>` tiene que bajar exactamente `NIVELES_QUE_SUBE_EL_LINK = 2` —el `../../` que los cuatro consumidores llevan escrito—, y un tramo vacio, `.` o `..` no cuenta como nivel, porque haria que contarlos mienta. **(2) El choque con `kamayuk-lib`**, que tambien DECIDE — y **no por donde parecia**: medido, un consumidor que llegara a estar en `kamayuk-lib/frontend` resolveria su `link:` (yarn lo acorta a `../../../paquetes/formato` y `require` devuelve la libreria); lo que se rompe es el `mv`. **(3) El nombre**, que se conserva **declarado como CONVENCION**, con su motivo escrito: con ella la disposicion del corredor es la misma que la del disco de quien desarrolla, asi que un rojo de CI se reproduce en local con las mismas rutas; hoy no cuesta nada —las cuatro entradas ya la cumplen— y el dia que un consumidor necesite otro nombre, lo que se borra es **ese** renglon con su muestra, no los dos de arriba. Las tres reglas son funciones puras sobre la lista, de modo que las **tres muestras** les pasan disposiciones que este disco no tiene. El rojo de las tres **nombra `consumidores.json`**, que es donde se arregla. El `_comentario` del JSON y el `README.md` dicen lo mismo. Pruebas del archivo: **6 a 11**; `paquetes/verificaciones`, **101 a 106** | **(M0) La mitad que #79 pedia medir antes de escribirla**, en un banco con la disposicion del job (`node:22` por Docker, yarn 1 —el de los cuatro consumidores, que no declaran `packageManager`—): `kamayuk-lib/paquetes/formato` y tres consumidores con el mismo `link:../../kamayuk-lib/paquetes/formato` en `caja/frontend`, `a/b/frontend` y `caja-ruta-honda/apps/web`. **(M0b)** el choque, con el `mv` de verdad del workflow (`mv consumidor 'kamayuk-lib'` sobre un `kamayuk-lib` ya existente) y con un consumidor puesto a mano en `kamayuk-lib/frontend`; y de paso `mv consumidor consumidor`. **(M1)** `"ruta": "apps/web"`, **(M2)** `"directorio": "a/b"`, **(M3)** `"directorio": "kamayuk-lib"` y **(M4)** `"directorio": "caja-web"` en la entrada de `hneyra/caja` de `consumidores.json`. **(M5)** y **(M6)**: las dos reglas nuevas puestas a `.filter(() => false)` —una guarda que no puede fallar—, para ver si las muestras la cazan con el JSON sano. Todo restaurado por copia y comparado con `cmp` (identico) | **M0**: `caja/frontend` RC=0 y `require('@kamayuk/formato')` da `la libreria`; **`a/b/frontend` y `caja-ruta-honda/apps/web` dan los dos `yarn install` RC=0** y luego `Error: Cannot find module '@kamayuk/formato'` · `code: 'MODULE_NOT_FOUND'` — o sea que **la `ruta` rompe exactamente como el `directorio`**, y los tres dejan el MISMO symlink `../../../../kamayuk-lib/paquetes/formato`, colgando en los dos ultimos. **M0b**: `mv consumidor 'kamayuk-lib'` sale **RC=0** y mete el clon DENTRO (`kamayuk-lib/consumidor`, `kamayuk-lib/paquetes`), de modo que el `working-directory` siguiente da `sh: 1: cd: can't cd to kamayuk-lib/frontend` · RC=2; con el consumidor en `kamayuk-lib/frontend`, `yarn install` RC=0, `formato -> ../../../paquetes/formato` y `require` devuelve `la libreria`; y `mv consumidor consumidor` sale **RC=1** con `mv: cannot move 'consumidor' to a subdirectory of itself` —falla en el acto y en el paso que lo causa, por eso ese choque NO se vigila y esta escrito por que—. **M1**: «× el consumidor cae a la PROFUNDIDAD que su `link:` mira» · `AssertionError: En «consumidores.json» hay consumidores que no caen donde su «link:» los busca: hneyra/caja: «caja/apps/web» baja 3 niveles, y el «link:» sube 2: expected [ Array(1) ] to deeply equal []` · `Tests 1 failed \| 10 passed (11)`. **M2**: el mismo renglon mas el de la convencion, `Tests 2 failed \| 9 passed (11)`. **M3**: «× ningun `directorio` choca con el clon de esta libreria» · `AssertionError: En «consumidores.json» hay un «directorio» que choca con el clon de esta libreria: hneyra/caja pide clonarse en «kamayuk-lib», que ya es el clon de esta libreria: expected [ Array(1) ] to deeply equal []`, mas la convencion, `Tests 2 failed \| 9 passed (11)`. **M4**: **solo** «× LA CONVENCION: el `directorio` se llama como el ultimo trozo del repositorio» · `AssertionError: En «consumidores.json» el «directorio» no se llama como el repositorio (es la CONVENCION, no la resolucion): hneyra/caja se clona en «caja-web»: expected [ Array(1) ] to deeply equal []` · `Tests 1 failed \| 10 passed (11)` — que es la decision explicita del AC2: a la misma profundidad **no pasa**, y sale rojo por la convencion y por nada mas. **M5**: «× LA MUESTRA: un camino que no baja dos niveles sale rojo…» · `AssertionError: «a/b/frontend» paso en verde: expected [] to have a length of 1 but got +0`. **M6**: «× LA MUESTRA: un `directorio` llamado `kamayuk-lib` sale rojo, y solo el» · `AssertionError: «kamayuk-lib» como directorio paso en verde: expected [] to have a length of 1 but got +0` —con el JSON sano las dos veces: sin las muestras, las reglas se habrian quedado verdes para siempre—. Restaurado: `Test Files 1 passed (1)` · `Tests 11 passed (11)`; `paquetes/verificaciones` entero, `Test Files 8 passed (8)` · `Tests 106 passed (106)`; `yarn typecheck` RC=0 y `npx eslint` del archivo sin una linea |
| **El producto sube a Node 24, y el `AbortSignal` de otro realm deja de ser un misterio (#90).** Los **tres** `node-version: "22"` de `.github/workflows/paquetes.yml` —incluido el del trabajo `consumidores`, que es el que mide a los **seis** sistemas— y el de `registro.yml` pasan a `"24"`, y `engines.node` de `>=22` a `>=24`; el comentario del `setup-node` que cita ese `engines` se corrige con ellos, que si no queda mintiendo. **Y lo que #60 anotó como «es de la máquina» era un defecto de verdad, medido en la fuente**: `undici` —el que viene dentro de Node— comprueba la señal con `webidl.is.AbortSignal = webidl.util.MakeTypeAssertion(AbortSignal)`, y `MakeTypeAssertion = (I) => (O) => FunctionPrototypeSymbolHasInstance(I, O)`, o sea el `instanceof` ordinario, que **recorre la cadena de prototipos y no se puede engañar** con un `Symbol.hasInstance` propio. Ese `AbortSignal` se resuelve **al arrancar Node**, antes de que exista jsdom, así que es el nativo; el `AbortController` global bajo Vitest es el de jsdom, de otro realm. Quien construye el `Request` es `createClientSideRequest` de `react-router`, **en cada navegación** del enrutador de datos: con Node 24 la navegación muere ahí dentro y el hash se queda como estaba. Se arregla en `vitest.setup.ts` —un `Request` del arnés que acepta la señal del documento y la deja pasar **tal cual**, porque `react-router` lee `request.signal` para cortar sus cargadores— y **no** cambiando los globales del documento: el `AbortSignal` nativo ya no es alcanzable desde ahí (ninguna API pública lo devuelve, medido con `--expose-internals`), y darle a jsdom otro `AbortController` haría que su propio `addEventListener(…, { signal })` rechazara la señal por el mismo motivo, al revés. En el navegador hay **un** realm y este camino no existe | **Dos roturas.** **(M1)** Quitado el `Request` del arnés y corrida la suite entera con `node:24` (v24.21.0). **(M2)** El `engines`: la misma suite con `node:22` (v22.23.2), sin `--ignore-engines` | **(M1)** `Test Files 3 failed \| 46 passed (49) · Tests 21 failed \| 880 passed (901) · Errors 1658 errors`, todas de `paquetes/shell` y todas con la misma forma —`AssertionError: expected '#/dep-lista' to be '#/detalle/42?estado=BAJA'`— más 1 658 `TypeError: RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal` desde `createClientSideRequest`. Con el arnés puesto, **`Test Files 49 passed (49) · Tests 901 passed (901)`, 0 errores**, y `yarn verificar` entero RC=0 (lint, tipos, las 901 y las `3 passed (3)` de capa; sus 3 `Timeout calling "snapshotSaved"` son de antes y salen iguales en Node 22, que por eso llevan `--dangerouslyIgnoreUnhandledErrors`). **(M2)** `error kamayuk-lib@0.1.0: The engine "node" is incompatible with this module. Expected version ">=24". Got "22.23.2"` — la declaración muerde, no es decorado. **Y lo que el propio trabajo `consumidores` midió con Node 24**, que es para lo que existe: **verdes** `ciudadano` y `pcf`; **rojos los otros cuatro, todos por esta misma traza y ninguno por esta rama** —los dos pasos, línea base y rama, dan `failure`, así que el veredicto avisa y no bloquea—: `rentas` `Test Files 46 passed (46) · Tests 652 passed (652) · Errors 2168`, `catastro` `45 passed (45) · 310 passed (310) · Errors 18`, `caja` `35 passed (35) · 356 passed (356) · Errors 249` y `normativa` `Test Files 2 failed \| 35 passed (37) · Tests 2 failed \| 426 passed (428) · Errors 213`. **Ninguna prueba en rojo en tres de los cuatro, y aun así RC=1**: con `CI=true` Vitest no perdona los rechazos sin atender. `caja` lo arregla en `caja`#93 con este mismo arnés; a los otros tres les toca en su repositorio |
| **Lo que se elige en un campo sale del bloque: vive en la ruta de la hoja (#94).** El intérprete guardaba lo tecleado en el estado de `<Pantalla>` y no lo publicaba por ningún sitio —ni en `nombrados`, ni por una `prop`—: la página y el orden llegaban a la ruta y **un filtro no**, así que la caja «Buscar giro» de `rentas`#172 y los quince mandos de `seg-aud`, `tra-veh` y `tra-pap` no acotaban nada. Un campo —el que se teclea y el que elige de una lista— puede declarar `eleccion: { enLaRuta, cuando? }`. **No se abre un segundo canal hacia `nombrados`, y ese es el hallazgo**: desde #67 la ruta ya está ahí como `ruta.<clave>`, así que escribir en la ruta es a la vez publicarlo para `resolverTexto`, `seCumple` y los `parametros` de una acción `va` — sin exponer un estado que cambia en cada tecla. **Dos momentos, no tres**: `alElegir` —un gesto, un movimiento de ruta: lista y calendario por omisión— y `alSalir` —al salir del campo y con Intro: lo que se teclea, un movimiento por valor terminado—; **el retardo se miró y NO entra**, porque `bod`/`bode`/`bodeg`/`bodega` son cuatro direcciones que nadie pidió, cuatro lecturas y cuatro entradas en el botón de atrás. Cambiarlo **devuelve a la primera página** las tablas de su bloque, en un solo `cambiosEn` (AC-3). Un filtro **no ensucia la hoja** —vive en la barra de direcciones, no es trabajo sin guardar—, y por eso su borrador va en un saco aparte de `tecleado`: en el mismo, `alEnsuciar` no habría vuelto a avisar nunca después de tocar un filtro. **Y una fecha pasa a viajar en ISO y a leerse `dd/mm/aaaa`**: lo que el calendario guardaba era `20/9/2026`, que ningún backend del producto parsea, que hay que escapar en una dirección, que empata `9` con `09` partiendo la caché y que ordenado como texto va después de `10`. Aditivo y medido como tal: **930 pruebas en 50 archivos** (901 antes) más las **3 de capa** de siempre, y los seis consumidores en verde | **Cinco roturas, una cada vez y restauradas con `cmp`.** (1) `cambioAlElegir` sin el bucle que reinicia la paginación de las tablas del bloque. (2) `valoresDe` sin la línea que lee la ruta. (3) `onSelect` del calendario de vuelta a `dia.toLocaleDateString('es-PE')`. (4) El disparador de la fecha enseñando el valor tal cual en vez de `fechaLeida(valor)`. (5) `MomentoDeLaEleccion` ensanchado a `string`. **Y una sexta que salió al revés y cambió el código**: se escribió `eleccion?: never` en `CampoDeSoloLectura` y en `CampoDeCasilla` —por el motivo de los `?: never` de `DefinicionDeAccion`— y al quitarlos el `typecheck` **siguió en rojo**: `DefinicionDeCampo` está discriminada por `tipo`, TypeScript estrecha a una rama antes de mirar las propiedades de más, y las dos barreras salen con TS2353 solas. Las dos declaraciones **se borraron**, y lo que queda escrito es lo medido | (1) `expected { Object (parametros) } to deeply equal { parametros: { pagina: '0', …(2) } }` — faltaban `"pagina": "0"` y `"paginaDeLaOtra": "0"` — y 3 pruebas rojas, entre ellas la del AC-3. (2) **5 rojas**: `Unable to find an accessible element with the role "button" and name "15/03/2026"`, y `expected [] to deeply equal [ { parametros: … } ]` — sin restituir de la ruta, salir del campo tampoco escribe. (3) En la prueba de capa: `- "fecha": "2026-03-20"` / `+ "fecha": "20/3/2026"`. (4) 1 roja, la del AC-2 de la fecha, con el mismo `Unable to find… "15/03/2026"`. (5) `barreras-de-campos-y-tablas.tsx(105,3): error TS2578: Unused '@ts-expect-error' directive.` **Y un hueco que se deja escrito en vez de tapado**: elegir un día DENTRO del calendario —lo único que pasa por `onSelect`— exige abrir una capa de Radix, y bajo jsdom eso no cabe de las tres maneras que se probaron: con `userEvent`, **118 s** y caducada a los 5 s; con `fireEvent` + cuadros inmediatos al final del archivo, pasa sola en 7 s pero **dentro de `yarn test` entero vuelve a dar `Test timed out in 5000ms`**; y en su propio archivo `capa-*` —lo que manda #11— `yarn test:capas` deja de caber y caducan **tres de los cuatro**, los de #11 incluidos. Se queda sin prueba de jsdom **a propósito**: lo que compone el valor (`fechaDeUnDia`) se prueba puro, y el gesto entero lo recorre Playwright en Chromium en el consumidor que lo pide (`caja`#98), donde abrir una capa cuesta milisegundos |
| **Las acciones de la CI suben a la mayor que declara `node24` (#93).** El producto ya subió en #90 —`engines: ">=24"` y los cuatro `node-version: "24"`, que **no se tocan**—, pero eso es el Node que corre nuestro código: el de una acción lo dice el `runs.using` de su `action.yml`, y por eso cada trabajo seguía sacando «Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24». Las **8** referencias del árbol —las únicas, comprobado con un `grep uses:` sobre `.github/` entero, que no tiene acciones locales ni compuestas— pasan de `@v4` a **`@v7`**: `paquetes.yml` 42, 44, 137, 147, 173, 190 y `registro.yml` 40, 44, todas `actions/checkout` y `actions/setup-node`. **La mayor se eligió leyendo el `runs.using`, no el número**, que es lo que AC-1 pide y lo que `caja` midió al descubrir que `actions/upload-artifact@v5` todavía declaraba `node20`: `actions/checkout@v7/action.yml` dice `using: node24` y `actions/setup-node@v7/action.yml` dice `using: 'node24'` (literales, sin comillas el primero y con ellas el segundo). **`v7` es la más alta**: `v8` da 404 en las dos, y ese 404 significa algo —control hecho con `@v99`, que da 404, y con `@v7.0.0`, que da 200—. Los `inputs` que los workflows usan sobreviven al salto, comprobado uno a uno contra los manifiestos de v7 (`repository`, `ref`, `token`, `path`, `fetch-depth`; `node-version`, `cache`, `cache-dependency-path`): lo único retirado de `setup-node` entre v5 y v7 es `always-auth`, que aquí no lo usa nadie. Los dos YAML se parsearon **con un parser de verdad** (PyYAML 6.0.1) antes y después, no a ojo. **Y entra una guarda con sus muestras**, `paquetes/verificaciones/las-acciones-corren-en-node-24.test.ts`, que lee el **directorio** `.github/workflows/` —un workflow nuevo entra solo— y saca sus `uses:`. **No baja el `action.yml` a propósito**: eso exige red, y una guarda que necesita red es una que se salta el día que la red falla; lo que fija es la mayor **cuya medición se hizo**, con el `runs.using` literal anotado al lado en `MAYOR_MINIMA_POR_ACCION`. Su trabajo no es descubrir la mayor nueva —eso lo hace una persona, con el `curl` que el propio rojo escribe— sino que nadie baje de la medida sin rojo. **Tres puertas, no una**: por debajo de la mayor medida, una acción que la tabla no conoce, y una referencia que no es `@vN` —un SHA, una rama, un `docker://`, una acción local—, cuya mayor no se puede comparar con nada; las dos últimas salen **rojas diciéndolo** en vez de pasar en verde por no saber, que es el criterio de `lo-que-exports-promete-existe` (#24). Más dos centinelas, contra el barrido que se queda sin sujeto. **940 pruebas en 51 archivos** (eran 930 en 50), más las 3 de capa | **Dos roturas, las dos sobre el disco de verdad y no sobre una muestra.** **(M1)** `registro.yml:40` devuelto a `actions/checkout@v4`, que es exactamente el defecto que este issue cierra, volviendo. **(M2)** Insertado `- uses: actions/upload-artifact@v5` en `registro.yml` —el caso que `caja` midió: mayor alta y `node20` dentro—, para comprobar que la puerta de «no medido» muerde y que la del número **no** se la come antes. Además, la dirección contraria en las muestras: un `@v7.0.0` no saca ni un renglón, sin lo cual una guarda que devolviera siempre la referencia pasaría M1 | **(M1)** `Tests 10 \| 1 failed`, y sólo el renglón que toca: `AssertionError: Hay acciones por debajo de la mayor medida… registro.yml:40 «actions/checkout@v4» — por debajo de @v7, la mayor medida con «using: node24»: expected [ Array(1) ] to deeply equal []`. Las otras nueve en verde, las dos puertas hermanas incluidas. **(M2)** falla **otra** prueba y no la misma: `AssertionError: Hay acciones de las que no se midió nada… registro.yml:48 «actions/upload-artifact@v5» — no esta en MAYOR_MINIMA_POR_ACCION: mide su «runs.using» y anotalo ahi`, con el `curl` a ejecutar en el propio mensaje; y la de la mayor **pasa en verde**, que es la prueba de que las dos puertas son dos y no una escrita dos veces. Restaurados los dos, `yarn verificar` RC=0 —lint, tipos, `Test Files 51 passed (51) · Tests 940 passed (940)` y las `3 passed (3)` de capa— y `yarn registro:autoprueba` con sus 9 muestras. Línea base medida antes de tocar nada, en el mismo árbol: `50 passed (50) · 930 passed (930)`. **Y el salto destapó un defecto que el propio bump causaba, medido en la primera corrida del PR**: `actions/checkout@v7` ya no deja las credenciales del clon en su `.git/config` como hacía `@v4`, sino en un archivo aparte enganchado con `includeIf.gitdir:<ruta absoluta>/.git` — y **un `includeIf` por ruta absoluta no sobrevive a mover el directorio**. El trabajo `consumidores` clonaba en `consumidor/` y lo movía con `mv consumidor '<directorio>'`, así que tras el `mv` la condición dejaba de casar y el clon se quedaba sin autenticación: el `orden` de `catastro`, que baja un commit antes de verificar, salía `fatal: could not read Username for 'https://github.com': No such device or address` con **RC=128** en sus DOS pasos, donde con `@v4` salía RC=1. **Defecto silencioso de libro**: `catastro` ya estaba rojo por su cuenta, así que el veredicto de #10 seguía diciendo «no es de aquí» y nada se ponía en rojo —pero esta CI había dejado de medir a `catastro`, y el día que `catastro` se arreglara el rojo habría sido nuestro y por un motivo que no se lee—. Los otros cinco consumidores no tocan la red y no se enteraban. Se arregla clonando **directo en `path: ${{ matrix.directorio }}`** y borrando el paso del `mv`: un paso menos y la ruta del `includeIf` ya es la definitiva. La regla de `los-consumidores-se-miran` no cambia —lo que prohíbe no es un `mv` concreto sino que dos clones se pisen—, sólo el comentario que explicaba el mecanismo. **Lo que esta fila NO puede afirmar**: el «después» del recuento se midió sobre la PRIMERA corrida del PR, la que aún llevaba el `mv`; la corrida que valida este arreglo es la siguiente, y la lee la revisión |
| **El arnés del `Request` se publica, y esta librería lo importa desde ahí (#92).** El `Request` de arnés que acepta la señal creada por jsdom —lo que permite correr con Node 24 (#90)— estaba **copiado** en el `vitest.setup.ts` de aquí y en el de `caja`, y hacía falta en tres más: `rentas` (2 168 errores), `catastro` (18) y `normativa` (213, con dos pruebas rojas). Cinco copias de la misma costura es lo que esta librería existe para evitar. Ahora vive en `paquetes/verificaciones/arnes-del-request.ts`, lo publica `exports` como `@kamayuk/verificaciones/arnes-del-request` —una `.ts`, así que trae su tipo y `lo-que-exports-promete-existe` la mira sola desde el minuto uno— y **este repositorio es su primer consumidor**: su `vitest.setup.ts` pasa de 49 líneas de copia a un `import './paquetes/verificaciones/arnes-del-request.ts'`, **por ruta relativa**, que es lo único que resuelve igual aquí y en el consumidor (#4). Se monta al importarlo: la segunda línea que se puede olvidar deja exactamente el defecto de hoy sin que nada lo diga, que es la lección ya pagada de #23. **La explicación viaja con el código**, como pedía el issue: por qué es el `Request` y no los globales del documento —el `AbortSignal` nativo ya no es alcanzable desde ahí, y darle a jsdom otro `AbortController` rompería su `addEventListener(…, { signal })` al revés— está en el docblock del módulo. **La guarda es de dos piezas porque no puede ser de una**: esta librería **no puede leer el árbol de un consumidor que no tiene clonado**, y una prueba que afirmara algo sobre el `vitest.setup.ts` de otro repositorio estaría afirmando lo que no midió. Así que (1) `el-arnes-del-request-se-publica.test.ts` barre **este** árbol y exige que la asignación del `Request` global aparezca en un solo archivo —el publicado— y que `vitest.setup.ts` lo importe, y (2) `el-arnes-del-request-no-se-copia.mjs` se **publica para que el consumidor lo ejecute contra el suyo** (`node node_modules/@kamayuk/verificaciones/el-arnes-del-request-no-se-copia.mjs`; aquí, `yarn arnes:copias`). Las dos usan **el mismo barrido**, importado y no copiado, que sería empezar el defecto de este issue en la guarda que lo cierra; y por lo mismo `sinComentarios` se muda de `texto.ts` a `comentarios.mjs` —lo necesita también el guion, que corre con `node` pelado— y `texto.ts` lo reexporta, así que ninguna de las cinco guardas que lo usan cambia una línea. **Lo que la guarda NO puede forzar, dicho y no escondido**: que el consumidor enchufe ese guion a su `verificar`, y que añada la línea. **948 pruebas en 52 archivos**, y `@kamayuk/verificaciones` **171 en 11** —remedidas sobre el directorio: la cifra que CLAUDE.md y el README traían, 144 en 8, se había quedado vieja por un archivo— | **Tres roturas, más una que se rompió sola.** **(M1)** Quitado el `import` del `vitest.setup.ts` y corrida la suite entera con Node 24 (v24.10.0). **(M2)** Devuelta la copia a mano al `vitest.setup.ts`, con el import puesto. **(M3)** La entrada nueva de `exports` apuntando a `./arnes-del-request.mjs`, que no existe. **(M4, no buscada)** El docblock de `arnes-del-request.test.ts` **citaba** la anotación de entorno de Vitest para decir por qué no se usa | **(M1)** `Test Files 4 failed \| 48 passed (52)` · `Tests 23 failed \| 925 passed (948)` · `Errors 1658 errors`: las 21 de `paquetes/shell` de siempre —`TypeError: RequestInit: Expected signal ("AbortSignal {}") to be an instance of AbortSignal.` desde `createClientSideRequest`— **más las 2 de la guarda nueva**, «`vitest.setup.ts` no enchufa el arnés: sin él, con Node 24 cada navegación del enrutador de datos muere dentro de `createClientSideRequest`: expected false to be true». **(M2)** «El arnés del `Request` está escrito a mano fuera del módulo que lo publica. Se importa: `import '@kamayuk/verificaciones/arnes-del-request';` — Donde aparece: `vitest.setup.ts:51  globalThis.Request = RequestQueAceptaLaSenalDelDocumento;`», **y una sola vez**: la muestra escribe la misma asignación también dentro de un comentario y ésa no cuenta. **(M3)** «Hay entradas de «exports» que prometen un archivo que NO está … `@kamayuk/verificaciones  exports["./arnes-del-request"]  ->  paquetes/verificaciones/arnes-del-request.mjs`», más el rojo de #46 pidiendo su `.d.mts` — la entrada nueva la vigila #24 sin tocarla. **(M4)** Vitest busca esa anotación con una expresión regular **en el primer comentario del archivo**, así que nombrarla la aplica: el archivo corría en entorno `node`, `document` era `undefined`, no había dos realms y las dos pruebas del rojo salían «expected undefined to be an instance of TypeError» — la explicación apagaba lo que explicaba. Arreglado sin citarla, y con un CENTINELA que exige `typeof document === 'object'`. **Y el guion, contra un árbol con la copia**: `RC=1` nombrando `arnes-del-request-copiado.ts:28`; contra un árbol sin nada, `RC=0` diciendo qué línea falta. Todo restaurado: `yarn verificar` RC=0 —lint, tipos, `Test Files 52 passed (52)` · `Tests 948 passed (948)` y las `3 passed (3)` de capa—, y `yarn registro:autoprueba` con sus nueve muestras |
| **`@kamayuk/sesion` dice quién entró: los claims del `id_token` que ya guardaba (#70).** El `id_token` estaba en el cierre desde el primer día y sólo servía para el `id_token_hint` de `salir()`, así que `Identidad` no publicaba ni el token ni sus claims y la barra de `catastro` (catastro#110, AC 10) se quedó con «Cuenta sin identificar». Ahora **`quienEntro(): QuienEntro \| null`** da `nombre` (`name`), `usuario` (`preferred_username`) y `municipalidad` (`municipalidad_id`), leídos en `quien-entro.ts` —una función pura de un texto a tres campos, que **no nombra ningún almacenamiento**— y guardados en el cierre. **Se olvida en `salir()` sin tener que acordarse de él**: `salir()` llama a `fijarToken(null)`, que es el único sitio donde el `id_token` cambia y donde los tres claims se calculan; un segundo sitio que olvidar es un sitio que algún día no se olvida. Y se calculan **una vez**, no en cada llamada, porque esto lo lee la barra en cada dibujado. **Sin validar la firma, y dicho con esas palabras en el docblock**: el backend es quien valida —la API manda el `access_token` y el emisor lo verifica— y esto es para dibujar; validar el JWKS en el navegador sería una vuelta más para adornar una cadena, y no protegería de nada, porque quien pueda escribir en la memoria de la pestaña puede escribir el nombre ya decodificado. **Los tres claims están MEDIDOS en el realm y no supuestos** (`infrastructure:despliegue/identidad/realm-kamayuk.json`, cliente `kamayuk-backoffice`): el mapeador `municipalidad-id` es `oidc-usermodel-attribute-mapper` sobre `municipalidad_id` con **`id.token.claim: "true"`** —viaja en el `id_token`, o sea que hay algo que leer— y con **`jsonType.label: "long"`**, o sea que llega **como número de JSON** y sale en texto; en el ámbito `kamayuk-servicio` el mismo mapeador lo lleva con `id.token.claim: "false"`, y de ahí que los tres campos sean anulables. **El campo NO se llama `municipalidadId`, que es lo que #70 proponía, y está medido por qué**: ese identificador lo prohíbe `municipalidad-en-el-cliente` (`Identifier[name='municipalidadId']`), que no distingue declarar de leer — el rojo sale **dos veces**, y la segunda es **leer el campo**, que es lo que escribiría cada consumidor en su propio árbol. El nombre del issue no pondría rojo a la librería: pondría rojo a los cinco sistemas. **`null` y «tres nulos» son dos cosas**: `null` es «no hubo canje, o no se pudo leer»; el objeto con tres nulos es «hubo canje y el emisor no dijo nada». **El AC-4 es de `catastro`** y aquí no se toca: lo consume su #122. **+29 pruebas**: `paquetes/sesion` de 45 en 2 archivos a **74 en 3**; el total de **940 en 51 a 969 en 52**, más las **3** de capa de siempre — remedido tras rebasar sobre #93, que entró a `main` mientras esto se escribía; contra la base de partida (`f097794`) eran 930 en 50 → 959 en 51 | **Cinco roturas, cada una aplicada sola, restaurada por copia y comparada con `cmp`.** **(R1) que `salir()` deje de olvidar los claims**: `fijarToken` recalculando sólo `if (identidad !== null)`, que es el olvido plausible. **(R2) que el decodificador reviente en vez de dar `null`**: quitado el `try/catch` de `cargaUtilDe`. **(R3) guardar los claims ya decodificados**, que es el atajo que un consumidor pediría para no perder el rótulo al recargar: `localStorage.setItem('kamayuk.barra', JSON.stringify(quienEntroEnMemoria))` — y **(R3b) el mismo en `sessionStorage`**, que es la mitad que ninguna guarda de antes veía. **(R4) leer la carga byte a byte**, como la devuelve `atob`, en vez de con `TextDecoder`. **(R5) admitir la municipalidad sólo como texto**, o sea no creerse el `jsonType.label: "long"` del realm | **(R1)** `Tests 1 failed \| 73 passed (74)` · «salir lo OLVIDA: null otra vez, como el token» · `AssertionError: expected { nombre: 'Rosa Quispe Mamani', …(2) } to be null`. **(R2) el rojo que justifica el issue entero**: `Tests 6 failed \| 68 passed (74)`, y dos de los seis son **el canje muriéndose** — «un id_token ilegible NO rompe el canje (base64url mal formado en la carga)» · `AssertionError: promise rejected "DOMException{ stack: 'InvalidCharact…' }" instead of resolving` y «(la carga no es JSON)» · `promise rejected "SyntaxError: Unexpected token 'o', "no so…" instead of resolving`; los otros cuatro, en la función pura: `InvalidCharacterError: Invalid character`, `InvalidCharacterError: The string to be decoded is not correctly encoded.`, `SyntaxError: Unexpected token 'o', "no soy json" is not valid JSON` y `expected [Function] to not throw an error but 'InvalidCharacterError: Invalid charac…' was thrown`. O sea que sin el `try/catch` un `id_token` ilegible **deja fuera del sistema a quien el emisor acaba de dejar entrar**, y el síntoma aparece en el canje. **(R3)** dos rojos, y la diferencia entre ellos es el hallazgo: el nuevo nombra el dato —`AssertionError: «Rosa Quispe Mamani» aparece en un almacenamiento del navegador: expected [ Array(1) ] to deeply equal []`— y el viejo sólo cazó el recuento, `AssertionError: expected 1 to be +0 // Object.is equality`. **(R3b) y ahí se ve que la prueba nueva hacía falta**: movido a `sessionStorage`, la prueba vieja —«NADA de lo que se canjeo acaba en localStorage ni en sessionStorage»— pasa **en verde**, y sólo el barrido nuevo sale rojo con la misma línea de arriba. **En las dos, `npx eslint paquetes/sesion/identidad.ts` da `RC=0`**: la prohibición `token-en-almacenamiento` mira el NOMBRE de la clave, y `'kamayuk.barra'` no lleva ninguna de sus palabras — la prohibición no puede ver esto, y por eso el AC-3 pide barrer los **valores**. **(R4)** `expected 'JosÃ© Ã\u0091Ã¡Ã±ez GutiÃ©rrez' to be 'José Ñáñez Gutiérrez'` — la versión fácil pinta así **casi todos** los nombres de estas interfaces. **(R5)** dos rojos, uno en cada archivo: `expected { nombre: 'Rosa Quispe Mamani', …(2) } to deeply equal { … }` con `"municipalidad": null` recibido donde se esperaba `"150101"` — la medición del `jsonType` del realm es lo que hace que ese campo diga algo. **Y la línea base restaurada**: `yarn verificar` RC=0 con `Test Files 52 passed (52) · Tests 969 passed (969)` más `Tests 3 passed (3)` de capa, y `yarn registro:autoprueba` con sus 9 muestras |
| **Las tres costuras de `delConsumidor` que encontró `rentas` al estrenarla (#102).** **(1) `esBloque` sale por el índice**: vivía en `paquetes/ui/interprete/componer.ts` y `paquetes/ui/index.ts` exportaba de ese mismo archivo `piezasSinRegistrar`, `resolverTexto` y `seCumple`, pero no a él, así que la guarda de `rentas` lo copió —una segunda definición de «qué es un bloque» que no se entera el día que ésta cambie—. Borrar la copia es de `rentas` y aquí no se toca. **(2) `piezasSinRegistrar` acepta el registro que la librería publica**: con `Readonly<Record<string, ComponentType<never>>>` un `PiezasDelConsumidor` **declarado con su tipo** no compilaba —el miembro `ComponentClass` de `ComponentType` lleva `defaultProps?: Partial<P>`, y con `P = never` sólo admite `undefined`—, y da igual lo que el registro lleve dentro: basta el tipo declarado. **El rojo se reprodujo ANTES de tocar la firma**, escribiendo la guarda como la escribe el consumidor. La firma pasa a **`Readonly<Record<string, unknown>> \| undefined`**: el cuerpo sólo pregunta `Object.hasOwn`, y lo que se rompió fue pedirle al VALOR algo que nadie leía. `PiezasDelConsumidor \| undefined` también lo arreglaba, y **se midió contra seis registros** con `tsc` (un archivo temporal, borrado): los dos tipos aceptan el publicado, un `Record` con las claves en una unión, uno de `FunctionComponent` y un literal `as const` —los cuatro que acepta `<Pantalla piezas>`—, los dos rechazan uno declarado como `interface` —igual que `<Pantalla>`— y **sólo difieren en uno que `<Pantalla>` tampoco monta** (un componente que exige una prop que el intérprete no da). Se eligió `unknown` para que un cambio futuro en el tipo de una pieza no pueda volver a romper la guarda de nadie, y porque `PiezaDeLaPantalla.tsx`, donde vive `PiezasDelConsumidor`, ya importa `componer.ts`: pedirlo cerraba un ciclo de tipos. El motivo, en el docblock. **(3) Aplanar es el contrato**: `DatoConNombre` sigue siendo `string \| boolean \| null` y una serie entra como `recaudacion.<i>.<campo>`, documentado en `DatoConNombre` y en `DatosDeLaPantalla.nombrados` **sin API nueva**, con los tres motivos —un canal diseñado con un solo uso ataría la API a ese uso; si hay canal entra con el gráfico cuando suba a bloque del intérprete (#25, que espera al segundo consumidor que pida una serie), para que la API salga de dos usos; y `recharts` cuesta +324,74 kB de JS (+98,12 kB gzip)—. **La barrera es un archivo nuevo, `paquetes/ui/interprete/costuras-del-consumidor.test.tsx`**, que importa **por el índice** —importado de `./componer.ts` pasaría en verde con las dos costuras— y llama a la guarda con un `PiezasDelConsumidor` de verdad —una función, un `memo` y una clase con `defaultProps`— **sin `as`**: es prueba de vitest y barrera de `tsc` a la vez. **989 pruebas en 55 archivos** (eran 987 en 54), `paquetes/ui` **436 en 23** (eran 434 en 22), más las 3 de capa — remedidas con `yarn test`, `yarn test paquetes/ui` y `yarn test:capas` | **La reproducción y dos roturas, cada una aplicada sola sobre el árbol ya confirmado (`adadc20`) y restaurada con `git checkout --`, comprobado con `git diff --exit-code` (RC=0).** **(R0) La reproducción**: la prueba nueva añadida sobre `main` (`88e89f6`) sin tocar nada más —con la firma de antes y sin exportar `esBloque`— y `yarn typecheck`. **(Ra) Quitado `esBloque` del `export` de `paquetes/ui/index.ts`** y corridos `yarn test` y `yarn typecheck`. **(Rb) Devuelta la firma a `Readonly<Record<string, ComponentType<never>>> \| undefined`** (con su `import type { ComponentType }`) y corrido `yarn verificar`. **Lo que (Rb) enseña**: la barrera de la firma es **sólo del compilador** —bajo (Rb) las dos pruebas de vitest pasan en verde, `Tests 2 passed (2)`, porque en ejecución el tipo no existe—, así que muerde porque `yarn verificar` corre `tsc` antes que las pruebas | **(R0)** `yarn typecheck` RC=2 con dos errores: `costuras-del-consumidor.test.tsx(7,10): error TS2305: Module '"../index.ts"' has no exported member 'esBloque'.` y `costuras-del-consumidor.test.tsx(63,43): error TS2345: Argument of type 'Readonly<Record<string, ComponentType<PropsDeUnaPiezaDelConsumidor>>>' is not assignable to parameter of type 'Readonly<Record<string, ComponentType<never>>>'. · 'string' index signatures are incompatible. · Type 'ComponentType<PropsDeUnaPiezaDelConsumidor>' is not assignable to type 'ComponentType<never>'. · Type 'ComponentClass<PropsDeUnaPiezaDelConsumidor, any>' is not assignable to type 'ComponentType<never>'. · Type 'ComponentClass<PropsDeUnaPiezaDelConsumidor, any>' is not assignable to type 'ComponentClass<never, any>'. · Types of property 'defaultProps' are incompatible. · Type 'Partial<PropsDeUnaPiezaDelConsumidor> \| undefined' is not assignable to type 'undefined'. · Type 'Partial<PropsDeUnaPiezaDelConsumidor>' is not assignable to type 'undefined'.` — la cadena termina en `defaultProps`, que es lo que el issue decía. **(Ra)** `yarn test`: `Test Files 1 failed \| 54 passed (55)` · `Tests 1 failed \| 988 passed (989)` · «`esBloque` sale por el indice: el consumidor no tiene que copiarlo» · `TypeError: (0 , esBloque) is not a function`; y `yarn typecheck` RC=2 con el TS2305 de (R0), el mismo literal. **(Rb)** `yarn verificar` RC=2 en el paso de tipos, con el TS2345 de (R0) **idéntico línea a línea**, y `error Command failed with exit code 2.` **Y la línea base restaurada**: `yarn verificar` RC=0 con `Test Files 55 passed (55) · Tests 989 passed (989)` más `Tests 3 passed (3)` de capa, y `yarn registro:autoprueba` con sus 9 muestras |
| **La escalera sirve a una interfaz que ESCRIBE, y sus palabras se pueden traducir (#52).** `peldanoDe()` pasa de siete peldanos a **nueve**: el **409** deja de caer en la ultima rama —«averia · Reintente… avise a soporte», el remedio contrario: ninguno de los 409 de este producto se arregla reintentando— y el 422 **`ORDEN_NO_ADMITIDO`** deja de compartir titulo y remedio con `VALIDACION`, porque no lo provoca quien escribe sino la pantalla. `Peldano` gana **`reintentable`** —dos preguntas y no una: un 409 no es una averia Y ADEMAS no cambia por insistir— y **`incidencia`** como campo. `ErrorDeLaApi` conserva **las cinco extensiones del contrato** y no dos: `detalles`, `incidencia` y `parametroQueFalta` (`{ejercicio, llave?}`, tal cual y sin interpretar) llegaban por el cable y se tiraban en el constructor. Las treinta palabras salen a `paquetes/sesion/textos.ts` y entran por el **segundo argumento opcional** de `peldanoDe`, `Partial` fundido encima como `<Armazon textos>`; las dos frases que suponian `rentas` —«no hay padron que ensenar», «revise con que cuenta esta entrando»— se van, y el docblock deja de dar el 404 como «la cuenta no es usuario» (medido: en `identidad` esa cuenta recibe **403 `SIN_PRIVILEGIO`**, `GuardiaDeAcceso.java:127-136`). `el-texto-visible-es-dato` gana una **cuarta forma** —la frase: dos rachas de letras separadas por un espacio— sobre `sesion`, con `identidad.ts` como unica excepcion declarada y comprobada entera (`kamayuk-lib`#42 la reescribe). De paso, `consumidores.json` decia que `normativa` «NO enlaza `@kamayuk/verificaciones`» y lo enlaza desde `normativa`#62 (`673df0d`): corregido. **955 pruebas en 49 archivos, 0 fallos** (antes 901; `escalera.test.ts` 12 → **55**, `cliente.test.ts` 38 → **44**, `el-texto-visible-es-dato` 9 → **14**, `sin-suponer-un-sistema` 20 → 20) | **Siete roturas, las siete del issue.** (1) Quitada la rama del 409. (2) La rama del orden deja de leer el `codigo` y mira solo `estado === 422`. (3) El constructor de `ErrorDeLaApi` pone `this.incidencia = null` en vez de copiarla. (4) `titulo: 'Hay que volver a identificarse'` escrito otra vez dentro de `escalera.ts`. (5) La rama del 409 ignora el saco recibido y lee `TEXTOS_DE_LA_ESCALERA` directamente. (6) El segundo argumento de `peldanoDe` se hace obligatorio. (7) `'/rentas/api/v1'` escrito dentro del saco | **(1)** 10 rojas en `escalera.test.ts`: `expected 'averia' to be 'conflicto'`, `«409» se ensena como una averia: expected true to be false` y `expected [ [ '409', '500' ], …(1) ] to deeply equal [ [ '422 sin codigo', …(1) ] ]` — o sea que el 409 y el 500 pasan a verse igual, que es el defecto entero. **(2)** 5 rojas: `expected 'no-valido' to be 'orden-no-admitido'`, `expected 'no-valido' not to be 'no-valido'` y `expected 'corrija lo que dice el mensaje y vuel…' not to contain 'corrija'`. **(3)** 2 rojas, una a cada lado del cable: `cliente.test.ts` y `escalera.test.ts`, las dos con `expected null to be '2f0f7f2e-9a1c-4f1e-9a55-1c3f5c2f0a11'`. **(4)** `Hay frases escritas DENTRO del codigo de «sesion», fuera de su saco: paquetes/sesion/escalera.ts:170 «Hay que volver a identificarse»` — y esa **misma** linea pasa en verde con las tres formas anteriores: medidas sobre el `escalera.ts` de `main`, con sus **26** frases dentro, dan **0 hallazgos**. **(5)** `titulo: «El estado actual no admite esta operacion»: expected false to be true`, nombrando el peldano. **(6)** `yarn typecheck` de `rentas` en rojo: `verificaciones/resuelve-el-clon-hermano.test.ts(52,21): error TS2554: Expected 2 arguments, but got 1` y lo mismo en `(68,21)`, con `RC=2` — la compatibilidad del AC8 esta vigilada y no supuesta. **(7)** `sin-suponer-un-sistema` en rojo: `paquetes/sesion/textos.ts:150 … /rentas/api/v1`. **Y los consumidores, medidos a mano con el metodo de la CI —el mismo arbol del consumidor, la libreria en `main` y luego en esta rama—**: `rentas` da `Test Files 64 passed (64) · Tests 948 passed (948) · Errors 2356` **identico en los dos**, y `normativa` `Test Files 2 failed \| 40 passed (42)` en los dos, con `Tests 3 failed \| 498 passed (501) · Errors 213` en la linea base y `2 failed \| 499 passed (501) · Errors 213` con esta rama —o sea **una roja MENOS**, y las dos que quedan son las dos mismas y son de reloj: `Test timed out in 5000ms` montando el Armazon—. Los `Errors` son la traza de `AbortSignal`/`undici` bajo Node 24 que #90 ya midio y anoto en este mismo registro; ninguno de los dos rojos es de esta rama, y con las dos pasadas en rojo el veredicto del trabajo avisa y no bloquea. **Y lo que midio el trabajo `consumidores` del PR, que es para lo que existe: CINCO verdes y UNO rojo.** Verdes `rentas` (`63 passed (63) · 933 passed (933) · Errors 2356`, identico en las dos pasadas), `catastro` (`45 · 310 · Errors 18`), `caja` (`35 · 382`), `pcf` (`50 · 1225`) y `normativa` (`2 failed \| 42 passed (44) · 2 failed \| 530 passed (532) · Errors 213`, **identico** en las dos, asi que el veredicto avisa y no bloquea). **Rojo `ciudadano`, y bloquea: linea base VERDE y esta rama ROJA.** Y no es un defecto de esta rama sino la guarda que `ciudadano` se puso a proposito: su `frontend/src/api/escalera.ts` traduce los peldanos al lenguaje del portal —«no hay soporte al que avisar, ni administrador que conozca»— con `TEXTOS_DEL_PORTAL: Readonly<Record<Peldano['clave'], TextosDelPeldano>>`, **completo por el tipo** y con su motivo escrito: «no compila si la libreria anade un octavo peldano y aqui no se decide que decir». Esta rama anade dos, asi que sale exactamente el rojo que ese docblock predice: `src/api/escalera.ts(74,14): error TS2739: … is missing the following properties …: conflicto, "orden-no-admitido"` y lo mismo en `src/api/escalera.test.ts(31,7)` sobre su `UN_FALLO_POR_CLAVE`, con `RC=2`. Le faltan ademas seis frases en su locale, que `el-locale-esta-completo` deriva de `clavesDeLaEscalera()`. **El AC8 del issue solo preveia a `rentas`** —y `rentas` no se rompe—: `ciudadano` entro en `consumidores.json` con #56, despues de que el issue se escribiera. Necesita un PR pareado suyo y una linea `consumidor: hneyra/ciudadano@<rama>` en el cuerpo, y **hasta entonces esto NO se mezcla**. **Remedido el 2026-09-22, y el bloqueo ya no esta:** `ciudadano` decidio los dos peldanos en su propio `main` (`fce3c64`, ciudadano#33): `frontend/src/api/escalera.ts:87` declara `ClaveDelPeldano = Peldano['clave'] | 'conflicto' | 'orden-no-admitido'`, con sus textos. Medido con su clon al lado de esta rama: lint, `tsc` e `i18n` en verde, y la unica roja de su `yarn verificar` es `el-puerto-del-arnes-sale-del-arbol.test.ts` con `listen EAFNOSUPPORT: address family not supported ::1`, que no importa nada de `@kamayuk` y sale igual con un `node -e` a secas: el entorno de la medida no tiene IPv6. **Se probo tambien la forma que no ensancha** —`peldanoDe` con siete y `peldanoAfinadoDe` con nueve, rama local `issue-52-forma-sin-ensanchar`, verde— y **se descarto**: con `ciudadano` al dia, dos funciones y un 409 que por omision sigue mandando a soporte no pagan nada. **Lo que se queda de ella es la barrera**: `LAS_NUEVE_CLAVES` en `paquetes/verificaciones/tipos/barreras-de-tipos.tsx`, un `Record<Peldano['clave'], true>` completo, la misma forma que la guarda de `ciudadano` puesta aqui. Rota dos veces: **(R8) una decima clave `'bloqueado'`** → `yarn typecheck` `barreras-de-tipos.tsx(182,14): error TS2741: Property 'bloqueado' is missing in type '{ 'sin-identidad': true; …`; **(R9) quitar `'conflicto'` de la union** → `barreras-de-tipos.tsx(188,3): error TS2353: Object literal may only specify known properties, and 'conflicto' does not exist in type 'Readonly<Record<…` (y los `TS2322` de `escalera.ts(240,7)` y `escalera.test.ts(234,5)`). Restaurado, `git diff --exit-code` limpio. **Tras mezclar `main` con #102 dentro**: `yarn verificar` RC=0 con `Test Files 55 passed (55) · Tests 1043 passed (1043)` mas `3 passed (3)` de capa |
| **Los campos, los actos y la prosa: los once huecos (#86, grupos A a F).** **Primera mitad: la hoja sucia, lo tecleado, los campos y los avisos (grupos A, B y C).** Ocho huecos de `normativa/frontend/diseno/HUECOS.md` —el encargo decía «siete», y los tres grupos nombran ocho—, **cada uno por un dato OPCIONAL**: una definición que no lo lleva se dibuja y se comporta como antes, y cada hueco lo prueba con y sin su dato. **Grupo A.** `la-hoja-se-marca-sucia-al-teclear` (H38): `definicion.hoja.suciaAlTeclear` → **cada** cambio que ensucia llama a `hoja.marcarSucia()` y guardar un acto vacía lo tecleado y llama a `marcarGuardada()`; hasta aquí el aviso salía sólo con la primera tecla y lo tecleado no se vaciaba nunca, así que una hoja guardada no podía volver a ensuciarse. El `limpiaAl` del issue **no entra**: su único valor con sentido es la lista entera. `lo-tecleado-y-la-negativa-sobreviven` (H35b), **opción C decidida**: `definicion.hoja.conservaLoTecleado: 'soloSiSucia'` → lo tecleado —campos, y valores, observación e intento del acto abierto— sube al `Armazon` en `tecleadoPorDestino`, con la misma clave que `sucias`; `limpiar` borra las dos cosas y dejar un destino que no está en `sucias` olvida su entrada. La `key` por destino (normativa#58) sigue desmontando y lo tecleado ya no muere mientras el árbol diga «SIN GUARDAR»; la negativa del servidor (`datos.lecturas`) es del sistema y no se toca. `HojaDelMarco` gana `marcarSucia?`, `marcarGuardada?`, `tecleado?` y `alTeclear?` —opcionales: una hoja escrita a mano sigue cabiendo— y `LoTecleado` vive en `hoja.ts`, así que `@kamayuk/ui` sigue sin importar `@kamayuk/shell`. `descartar-lo-escrito` (H48): `acto.descartar: { rotulo, dicho? }` → un secundario con `BotonConMotivo` (impedido sólo en vuelo, con el párrafo del primario) que vacía valores, observación, intento y rechazo, lo dice en un `role="status"` que existe vacío desde que el acto se abre, y deja la hoja limpia **sólo si lo del acto era lo único tecleado** —el encargo decía «limpia» a secas, y así daría por guardado un bloque que sigue escrito—. **Grupo B.** `obligatorio-u-opcional-por-campo` (H05b): `opcional?` y `mensajes?: { obligatorio? }` en los campos del bloque; «(opcional)» sale del dato y **se borra la regex** `/opcional/i` sobre la ayuda. **Es el único cambio sin opt-in, y es deliberado**: una ayuda que diga «opcional» sin `opcional: true` pierde la marca, y un campo de acto con `opcional: true` la gana. `errores-tras-el-primer-intento` (H07): `acto.errores: 'trasElPrimerIntento'` y `CampoDelBloque` gana `error?` hacia la `Etiqueta`. `ayuda-en-un-campo-de-solo-lectura` (H50): `CampoDeSoloLectura.ayuda?`, y el caso `'r'` la pasa. **Grupo C.** `aviso-efimero-tras-un-acto` (H37): `acto.alTerminar`/`alFallar: { aviso }` → `avisar()`/`avisar.error()`; descartar **no** avisa por ahí, porque su región viva ya lo dice. `insignias-fijas-en-la-cabecera` (H42): `bloque.insignias` y `aLaDerecha: { codigo }` en un hueco `junto` de `TarjetaCabecera`, **fuera** del `<h2>`. Dos palabras nuevas en `TextosDeLasPiezas` (`loEscritoSeDescarto`, `campoObligatorio`); `TextosDelInterprete` y los textos del armazón, ninguna. **1041 pruebas en 56 archivos** (987 en 54) más las **3 de capa** **Segunda mitad: la prosa, el filtro y el archivo (grupos D, E y F).** `texto-con-marcas` (H43, N6): `notaConMarcas` —en el bloque y en el acto, y gana a `nota`— es una lista de tramos `{ texto }`/`{ codigo }`/`{ fuerte }` (`TextoConMarcas`, con `?: never` en cada rama) que `ProsaConMarcas` dibuja dentro de la misma `TarjetaNota` con `<code>` y `<strong>`; la regla pura es `resolverMarcas`, y **el tramo `codigo` no pasa por `traducir`**. **Va en un campo aparte, y medido**: con `nota: T | TextoConMarcas<T>` —y `nota?: Texto | TextoConMarcas` en el acto— dejan de compilar los dos sitios que la leen como `Texto`, que son lo que escribe una pieza del consumidor con el `resolverTexto` publicado; con el tipo condicional (`[T] extends [string] ? T : …`) el de por omisión sigue siendo `string`, pero se rompe `DefinicionDeBloque<Texto>`, que es la que recorren los sistemas de las piezas. Tres barreras de tipo leen `nota` como la lee un sistema. `datosQueLee` baja de `acciones.ts` a `componer.ts`, junto a `resolverTexto`, recorre también los tramos y sale por el índice. **`instruccion` se devuelve a `normativa`**: la dibuja `@kamayuk/shell` desde `Destino.instruccion` (`CabeceraDePantalla.tsx`), y darle marcas ensancha el catálogo de los seis sistemas y pide texto nuevo del armazón con rama pareada. `filtro-en-el-cliente-con-conteo` (H02): `tabla.filtroLocal: { buscador?: { rotulo, marcador?, columnas? }, chips?: [{ rotulo, si: Condicion }], total?, sinCoincidencias? }`. **Decidido**: filtra las filas que LLEGARON —la página del servidor; en cliente, todas las recibidas **antes** de cortar la página—, con lo elegido en el estado de la tabla, con hoja o sin ella: ni a la ruta ni al servidor, y no ensucia; en cliente, cambiarlo devuelve a la primera página, y es lo único que se mueve. Los chips leen los `datos` de la fila con `seCumple` —los del mismo dato se suman, los de datos distintos se cruzan— y el buscador ignora mayúsculas y tildes. Puesto, «N de M» en un `role="status"` montado de antemano y «· T en total» **sólo si el sistema dio `total`**; el conteo de siempre se calla. Si no deja ninguna, `sinCoincidencias` o `textos.ningunaPasaElFiltro`, **nunca el `vacio`**; sin dato o con `[]` no se ofrece. Reglas puras `filtrarLasFilas` y `conteoDelFiltro`. `guardar-como-archivo` (H30a): **una cuarta clase de acción**, `{ guarda: { texto: { desde }, tipoDeMedio, nombre }, sinDescarga? }`: un `Blob` con el texto de `nombrados` **tal cual** —sólo `desde`: ni plantilla ni `traducir`— entregado con `entregarAlNavegador`, importado de `../../api/entregar.ts`. Es el primer import de `ui` hacia `api` y no hizo falta declarar nada en `paquetes/ui/package.json` —`sesion` ya importa `../api/index.ts` así—: `lo-que-exports-promete-existe`, `sin-suponer-un-sistema`, `el-texto-visible-es-dato` (con sus `PEER_DECLARADAS` intactas), `sin-nombre-publico-entre-paquetes` y `el-marco-no-decide-permisos` siguen verdes —esta última mira sólo los imports directos de `paquetes/shell`: el marco arrastra `entregar.ts` a través de la `ui`, y ahí no hay `fetch`—. Impedida con `BotonConMotivo`: si el navegador no sabe `download` —se sabe antes de pulsar—, `sinDescarga` o `textos.sinDescarga`; si la entrega revienta al pulsar, lo mismo desde ese momento; sin el texto o sin el dato de su nombre, `textos.faltaParaGuardar`. **Lo que cuesta, medido**: una rama más en la unión es aditiva para quien escribe definiciones y no para quien las recorre estrechando por descarte —sin tocar las dos piezas que lo hacen, `tsc` da RC=2—; un consumidor que estreche igual saldrá con ese rojo en el trabajo `consumidores`. Cinco palabras nuevas en `TextosDeLasPiezas` (`filtrarLaTabla`, `filasQueDejaElFiltro`, `ningunaPasaElFiltro`, `faltaParaGuardar`, `sinDescarga`); `TextosDelInterprete` y el armazón, ninguna. **1077 pruebas en 57 archivos** (1043 en 57) más las **3 de capa** | **Doce roturas, una cada vez, restauradas con `git checkout` y comprobadas con `git diff --exit-code` RC=0.** (1) `limpiar` sin borrar lo tecleado. **Salió VERDE la primera vez**, con las diez pruebas del marco: el efecto del destino ya olvida lo de una hoja limpia al salir, así que «salir sin guardar» y «guardar y cerrar» no la necesitaban. Lo que sólo `limpiar` puede hacer es olvidarlo **guardando sin salir**, y esa prueba no existía: se escribió la (5) y la rotura se repitió. (2) El efecto sin el olvido: se conserva sin mirar `sucias`. (3) `marcarSucia` sólo con la primera tecla (`if (Object.keys(tecleado.campos).length === 0)`). (4) El acto ignorando `tecleadoDeLosActos`. (5) La región viva de descartar montada sólo al descartar. (6) `alTeclear` renombrado en `HojaAbierta`, contra la barrera. (7) La regex de vuelta. **Y antes, la medición de H05b**: quitada la regex y sin tocar nada más, `paquetes/ui` + `paquetes/shell` (581 pruebas) dan UNA roja, la única que dependía de ella. (8) Los errores sin `&& intentado`. (9) `ayuda={undefined}` de vuelta en el caso `'r'`. (10) `alTerminar` sin `avisar`. (11) Lo de `junto` dentro del `<h2>`. (12) El hueco `junto` siempre, sin dato. **Y una expectativa mía que estaba mal**: escribí que `alEnsuciar` saldría 2 veces tras guardar y re-teclear, y salió 3 —el acto avisa el suyo una vez por apertura desde #66—; se corrigió la prueba, no el código **Segunda mitad: catorce roturas más y dos mediciones, cada rotura restaurada desde su copia y comparada byte a byte; con los tres grupos comprometidos, `git diff --exit-code` RC=0.** Mediciones: (M1) la nota con marcas DENTRO de `nota`, antes de decidir el campo aparte; (M2) la cuarta rama de `DefinicionDeAccion` sin tocar las piezas que estrechan. Roturas: (13) el tramo `codigo` como texto plano (un `Fragment` en vez de `<code>`). (14) `datosQueLee` ignorando la forma nueva (`return []` para una lista): lo que se lee dentro de una marca no lo pediría nadie. (15) El tramo `codigo` por `traducir`. **Salió VERDE la primera vez**: la prueba sólo tenía un `codigo` con `{ desde }`, que nunca se traduce; se añadió uno escrito en la definición y la rotura se repitió. (16) La barrera: `nota` con el tipo condicional. (17) El filtro a la ruta (`sitio.fijar({ filtro })`). (18) El conteo inventando un total (`?? String(todas.length)`). (19) La frase del filtro vacío reusando `vacio`. (20) Cortar la página y filtrar después, en cliente. **Salió VERDE la primera vez**: con «bodega» las dos que casan caían, por casualidad, en la página que el recorte dejaba —la página se calculaba sobre las filtradas y se acotaba a la primera—; se buscó «almacen», la cuarta, y la rotura se repitió. (21) La frase del filtro vacío escrita a mano, contra la guarda del saco marcado. (22) Se guarda lo reserializado (`JSON.stringify(JSON.parse(texto))`), que es lo que daría volver a pedirlo. (23) `sinDescarga` mudo (`ofreceDescarga: true` siempre). (24) El texto con `trim()`. (25) El texto por `traducir`. (26) `entregarAlNavegador` importado por su nombre público | (1) `(5) guardada SIN salir … → guardada, la hoja sigue ensenando lo tecleado: \`limpiar\` no lo olvido: expected 'lo escrito' to be '' // Object.is equality` — `1 failed \| 10 passed (11)`. (2) `(2) NO sucia … → una hoja limpia conservo lo tecleado: la \`key\` por destino no sirve de nada: expected 'lo escrito' to be ''`. (3) **3 rojas**: `expected "spy" to be called 3 times, but got 1 times` (dos, la de teclado incluida) y `guardada una vez, la hoja ya no se ensucia: expected false to be true`. (4) **2 rojas**: `expected '' to be 'R-7'` y `el acto volvio vacio con la hoja SIN GUARDAR: expected '' to be 'R-7'`. (5) `la region viva tiene que existir ANTES de decir nada: expected null not to be null`. (6) `paquetes/shell/estado-en-la-ruta.test.tsx(441,8): error TS2322: Type 'HojaAbierta' is not assignable to type 'HojaDelMarco & Required<Pick<HojaDelMarco, "alTeclear" \| "marcarSucia" \| "marcarGuardada">> & { readonly tecleado: LoTecleado \| undefined; }'. Property 'alTeclear' is missing in type 'HojaAbierta'` RC=2. Medición: `EL AC4 … y el interprete, sin \`textos\` ni \`traducir\` … → expected 'explicacionbloquenotatextoayuda, opci…' to contain '(opcional)'` — `1 failed \| 580 passed (581)`; se pasa a `opcional: true`, igual que la muestra `filtro-en-la-ruta` de #94. (7) **3 rojas**: `expected false to be true`, `la ayuda volvio a decidir la marca: expected true to be false` y `expected 'Alias' to contain '(opcional)'`. (8) `ANTES del primer intento, nada en rojo → Codigo: expected 'true' to be null`; la de #66 que exige lo mismo sin el dato (`actos.test.tsx`, «no envia SIN observacion») siguió verde, porque sin el dato no hay errores de campo. (9) `expected '' to be 'La calcula el servidor con la tabla v…'`. (10) **2 rojas**: `Unable to find an element with the text: Grupo dado de alta.` (la de teclado incluida). (11) `expected 'Detalle del registroVigenteSolo lectu…' to be 'Detalle del registro'`. (12) `SIN el dato, la cabecera es la de antes BYTE A BYTE → expected '<div data-slot="tarjeta-cabecera" cla…' to be '<div data-slot="tarjeta-cabecera" cla…'`. **Lo que no se pudo medir aquí**: los seis consumidores no están clonados en este entorno, así que el riesgo de H05b —cuántas ayudas suyas dicen «opcional» sin el dato— lo dirá su CI contra esta rama **Revisado rompiendo por el revisor, no por quien lo implemento**: conservar sin mirar `sucias` → `una hoja limpia conservo lo tecleado: la \`key\` por destino no sirve de nada: expected 'lo escrito' to be ''`; `limpiar` sin olvidar → `guardada, la hoja sigue ensenando lo tecleado: \`limpiar\` no lo olvido`; `marcarSucia` solo con la primera tecla → 3 rojas (`guardada una vez, la hoja ya no se ensucia: expected false to be true` y dos `expected "spy" to be called 3 times, but got 1 times`); la regex de vuelta → 4 rojas, entre ellas `la ayuda volvio a decidir la marca: expected true to be false`. **Tras mezclar `main` con #102 dentro**: `yarn verificar` RC=0 con `Test Files 57 passed (57) · Tests 1043 passed (1043)` mas `3 passed (3)` de capa **Segunda mitad.** (M1) `tsc` RC=2: `paquetes/ui/interprete/ActoDeLaPantalla.tsx(254,81): error TS2345: Argument of type 'Texto \| TextoConMarcas' is not assignable to parameter of type 'Texto'.` y `paquetes/ui/interprete/BloqueDeLaPantalla.tsx(101,48): error TS2345: Argument of type 'Texto \| TextoConMarcas<Texto>' is not assignable to parameter of type 'Texto'.` (M2) `tsc` RC=2: `paquetes/ui/interprete/GrupoDeAcciones.tsx(84,45): error TS2538: Type 'undefined' cannot be used as an index type.`, `paquetes/ui/interprete/acciones.ts(134,35): error TS18048: 'accion.va' is possibly 'undefined'.` y `acciones.ts(135,31): error TS2345`. (13) **4 rojas**: `el codigo salio como texto plano: expected undefined to be 'fk_cita_registro' // Object.is equality`, `expected undefined to be '—'` y `expected undefined to be 'R-00042'` — `4 failed \| 46 passed (50)`. (14) `expected [] to deeply equal [ 'restriccion' ]` — `1 failed \| 49 passed (50)`. (15) primero `50 passed (50)`; después `el codigo paso por \`traducir\`: expected 'FK_CITA' to be 'fk_cita' // Object.is equality`. (16) `tsc` RC=2: `paquetes/verificaciones/tipos/barreras-de-campos-y-tablas.tsx(120,98): error TS2322: Type 'Texto \| TextoConMarcas<Texto>' is not assignable to type 'Texto'.`; `laNotaDeHoySigueSiendoCadena` NO sale en el rojo: con el condicional, el de por omisión sigue siendo `string`. (17) **2 rojas**: `el filtro viajo a la ruta: \`?estado=\` seria un 422: expected "spy" to not be called at all, but actually been called 2 times` y `expected [ [ { parametros: { …(1) } } ], …(1) ] to deeply equal [ Array(1) ]` — `2 failed \| 63 passed (65)`. (18) `el conteo invento un total que no llego: expected '2 de 4 · 4 en total' to be '2 de 4' // Object.is equality`. (19) `expected 'El servidor no devolvio ningun regist…' to be 'Ningun registro de esta pagina pasa e…' // Object.is equality`. (20) primero `65 passed (65)`; después `se filtro la pagina ya cortada, y no todas las recibidas: expected [] to deeply equal [ 'R-004' ]`. (21) `«Pantalla con el filtro local de #86», con el filtro que no deja ninguna deja llegar texto que NO pasó por el saco` — `1 failed \| 17 passed (18)`. (22) **3 rojas**: `se guardo otro texto que el verificado: expected '{"valor":1,"nombre":"José"}' to be '{"valor":1.0,"nombre":"Jos\u00e9"}\n' // Object.is equality`. (23) **2 rojas**: `expected null to be 'true' // Object.is equality` (el `aria-disabled` que no salió), en «un navegador sin descarga lo DICE…» y «si la entrega revienta al pulsar…». (24) **3 rojas**: `expected '{"valor":1.0,"nombre":"Jos\u00e9"}' to be '{"valor":1.0,"nombre":"Jos\u00e9"}\n'`. (25) `expected '«{"valor":1.0,"nombre":"Jos\u00e9"}\n»' to be '{"valor":1.0,"nombre":"Jos\u00e9"}\n'`. (26) `sin-nombre-publico-entre-paquetes`: `Un import por el nombre publico resuelve AQUI … Usa una ruta relativa: "../api/index.ts".` — `1 failed \| 3 passed (4)`. **Lo que no se pudo medir aquí, otra vez**: los consumidores no están clonados ni se pueden leer desde esta sesión (la búsqueda de código sólo encontró que `caja` reexporta `DefinicionDeAccion as Accion`), así que el riesgo de la cuarta rama lo dirá su CI. **Al cerrar**: `yarn verificar` RC=0 con `Test Files 57 passed (57) · Tests 1077 passed (1077)` más `3 passed (3)` de capa **La segunda mitad, rompiendo el revisor (2026-09-23)**: el tramo `codigo` como `Fragment` → `el codigo salio como texto plano: expected undefined to be 'fk_cita_registro'`; el conteo con `total: String(recibidas)` → `el conteo invento un total que no llego: expected '2 de 4 · 4 en total' to be '2 de 4'` (2 rojas); la frase del filtro vacío reusando `vacio` → `expected 'El servidor no devolvio ningun regist…' to be 'Ningun registro de esta pagina pasa e…'`; guardar `JSON.stringify(JSON.parse(texto))` en vez del texto → `se guardo otro texto que el verificado: expected '{"valor":1,"nombre":"José"}' to be '{"valor":1.0,"nombre":"Jos\u00e9"}\n'` (3 rojas). Restauradas, `git diff --exit-code` RC=0. **Tras mezclar `main` con #96 dentro**: `yarn verificar` RC=0 con `Test Files 57 passed (57) · Tests 1131 passed (1131)` mas `3 passed (3)` de capa **Tras mezclar `main` con #105 dentro** (el Node del consumidor, la ultima 24): `yarn verificar` RC=0 con `Test Files 57 passed (57) · Tests 1133 passed (1133)` mas `3 passed (3)` de capa |
| **El Node del consumidor es la ÚLTIMA 24, no la que el corredor tenga en caché (2026-09-23).** Medido en el trabajo `consumidores` de #104 sobre `4b2cf40`: `hneyra/rentas` salió rojo en 15 s, **antes de medir nada** —en la línea base igual que en la rama—, con `error @kamayuk/rentas-web@0.1.0: The engine "node" is incompatible with this module. Expected version "^24.21.0". Got "24.20.0"` en `yarn install --frozen-lockfile`: el `main` de `rentas` (`8f6dbd7`) subió su `engines` y el `actions/setup-node@v7` con `node-version: "24"` se quedó con la 24.20.0 que el corredor traía en caché (`Found in cache @ /opt/hostedtoolcache/node/24.20.0/x64`, `check-latest: false`). No era un fallo del PR que lo midió: lo habría dado cualquiera. Se arregla con `check-latest: true` en el `setup-node` que instala al consumidor —lo que el consumidor exige lo decide él; esta CI sólo tiene que no quedarse atrás— | `los-consumidores-se-miran.test.ts` gana dos pruebas: el `setup-node` que cachea el `yarn.lock` del consumidor lleva `check-latest: true` **como orden** —se leen las líneas del paso sin comentarios, porque una guarda satisfecha por un `# check-latest: true` ya se cazó una vez aquí— y su muestra con los tres casos (orden, comentario, ausente) | **(R1) quitar la línea** → `AssertionError: el \`setup-node\` del consumidor no pide la ultima 24: expected [ …(5) ] to include 'check-latest: true'` — `1 failed \| 12 passed (13)`. **(R2) dejarla en comentario** → el mismo rojo, `1 failed \| 12 passed (13)`. Restaurado desde copia. `yarn verificar` RC=0 con `Test Files 55 passed (55) · Tests 1045 passed (1045)` más `3 passed (3)` de capa. **El rojo que lo destapó y el verde que lo arregla los da la CI de este mismo PR**: el trabajo de `rentas` tiene que pasar de caer en la instalación a medir |
| **Una fila de tabla se elige por sí misma, y el detalle de un maestro se dibuja sin elección (#95).** Medido al cerrar `caja`#99: la elección con estado en la ruta sólo existía en `maestroDetalle`, cuyo maestro es un `listbox` de `titulo`/`linea`/`insignia` donde una tabla de ocho columnas no cabe, y cuyo `detalle.sinEleccion` sustituía el detalle entero; una tabla sólo actuaba por botón (#65), y hubo que rodearlo con una acción `va` a la misma hoja. **Dos datos OPCIONALES**, y sin ellos lo de antes. `DefinicionDeTabla.eleccion?: { enLaRuta, desde }` (`EleccionDeLaFila`): pulsar la fila —el clic sube desde la celda— o Intro/Espacio con el foco en ella escribe `datos[desde]` **de la fila** con un solo `moverLaRuta` por `sitio.fijar` —`cambiosEn` de un sitio, que es `cambioEn`—, y la que casa con la ruta sale con `aria-selected="true"`, fondo `bg-azul-suave` y filo azul en la primera celda. **Patrón ARIA decidido: `role="grid"` en la `<table>`**, porque `aria-selected` en una `row` sólo significa algo dentro de un `grid`/`treegrid` (ARIA 1.2) —en una tabla de datos no se anuncia— y `table`→`grid` es la conversión que la configuración recomendada de `jsx-a11y` admite (`yarn lint` RC=0 con `onClick`/`onKeyDown`/`tabIndex` escritos en línea sobre el `<tr>`, no esparcidos, para que la regla los vea). Tabulador itinerante **por filas** —entra por la elegida o la primera elegible; ↑/↓/Inicio/Fin mueven el foco— y la elección **no sigue al foco**, como el maestro de #67. **Desviación declarada del APG**: el `grid` del APG mueve el foco por celdas y saca del tabulador los botones de dentro; aquí `accionesPorFila` sigue en el tabulador, porque lo otro cambia el teclado de una tabla que ya funciona. Un botón de la fila **no la elige**: el clic que nace en un mando (`closest` de `button, a[href], input, …` dentro de la fila) y la tecla cuyo `target` no es la fila se ignoran, también sobre la acción impedida. Una fila sin el dato (ausente, `null` o `''`) no es elegible: sin `tabIndex`, sin `aria-selected`, sin clic, con `data-no-elegible`. Página y orden no la tocan: son otros sitios. Sin `hoja`, en el `sinMarco` de la tabla. Regla pura `valorDeLaFila`, publicada con el tipo. `detalle.sinEleccionSeDibuja?: boolean` en el `maestroDetalle`: sin elección el detalle se dibuja —cabecera, piezas, cada lectura en su estado— con `sinEleccion` en un `Alerta` `data-sin-eleccion` encima. **Campo aparte y no unión, medido** (R-g). Muestras en un archivo propio, `muestras-de-la-eleccion.ts` —`fila-elegible-en-la-ruta` y `detalle-sin-eleccion-que-se-dibuja`—, y **no** en `muestras-de-las-tablas.ts` ni en `muestras-de-la-composicion.ts`: sus centinelas exigen las claves de su serie, ni una más. Ninguna palabra nueva en ningún saco; `paquetes/shell`, sin tocar | `la-fila-es-elegible.test.tsx`, **20 pruebas**: recargar conserva (montada con `?movimiento=M-2`); elegir escribe `[{ parametros: { movimiento: 'M-1' } }]` y elegir la elegida no mueve nada; `ruta.movimiento` lo lee otra pieza; en el sujeto; el teclado con `userEvent` (Tab desde el último mando de la barra llega a la fila, las flechas no eligen, Intro y Espacio sí, la fila sin dato no es parada); la acción de la fila no elige (clic, Intro y Espacio; atendida e impedida); la fila sin el dato; página y orden no borran; sin hoja; **sin el dato, la `<table>` BYTE A BYTE** contra la medida con el `TablaDelBloque.tsx` de `origin/main` (`a8b1902`), con los `id` de `useId` igualados; `valorDeLaFila` pura; el maestro con el dato sin y con elección, y sin el dato el todo o nada; una **barrera de tipo** que lee `sinEleccion` como `Texto`; el centinela de dos claves, ninguna local de `catastro`, y las dos se dibujan sin avisos de costura. Cada rotura, con un guion del borrador que aplica, corre, restaura el archivo y comprueba `diff --exit-code` RC=0 | **(R-a) la elección en el estado local aun con hoja** (`fijarSinMarco` en vez de `sitio.fijar`) → `AssertionError: la eleccion no llego a la ruta: expected [] to deeply equal [ Array(1) ]`, `TestingLibraryElementError: Unable to find an element with the text: Elegido: M-1.` y `AssertionError: expected [] to deeply equal [ { sujeto: 'M-1' } ]` — `Tests 4 failed \| 16 passed (20)`. **(R-b1) sin la guarda del clic en un mando** → `AssertionError: la accion de la fila eligio la fila: expected [ Array(1) ] to deeply equal []` — `1 failed \| 19 passed (20)`. **(R-b2) sin la guarda de la tecla** → `AssertionError: Intro o Espacio en la accion eligieron la fila: expected [ Array(1) ] to deeply equal []` — `1 failed \| 19 passed (20)`. **(R-c) sin `aria-selected`** → `AssertionError: expected null to be 'true' // Object.is equality` — `6 failed \| 14 passed (20)`. **(R-d) Intro y Espacio no eligen** → en «EL TECLADO…», `AssertionError: expected [] to deeply equal [ Array(1) ]` — `1 failed \| 19 passed (20)`. **(R-e) el detalle se sustituye aunque declare dibujarse** (`if (elegido === null)`) → `AssertionError: expected undefined to be 'Nada elegido todavia: el detalle se l…' // Object.is equality` — `1 failed \| 19 passed (20)`. **(R-f) sin el dato la tabla cambia**: `role="grid"` siempre → `AssertionError: expected <table data-slot="tabla" …(3)>…(2)</table> to be null` — `1 failed \| 19 passed (20)`; y la sutil, `data-no-elegible` también sin `eleccion`, que sólo ve la comparación byte a byte → `expected '<table data-slot="tabla" class="w-ful…' to be '<table data-slot="tabla" class="w-ful…' // Object.is equality` — `1 failed \| 19 passed (20)`. **(R-g) `sinEleccion: Texto \| { dibuja, dice }`** → `tsc --noEmit` RC=2: `paquetes/ui/interprete/MaestroDetalle.tsx(200,25): error TS2345: Argument of type 'Texto \| { readonly dibuja: true; readonly dice: Texto; }' is not assignable to parameter of type 'Texto'.`, lo mismo en `(211,27)` y en la barrera, `la-fila-es-elegible.test.tsx(306,21)`. Tres más que el encargo no pedía: **(R-h) la elección sigue al foco** → `AssertionError: expected [ Array(1) ] to deeply equal []`; **(R-i) cambiar de página borra la elección** → `expected [ { parametros: { …(2) } } ] to deeply equal [ { parametros: { pagina: '1' } } ]`; **(R-j) todas las filas en el tabulador** → `expected +0 to be -1 // Object.is equality`; las tres `1 failed \| 19 passed (20)`. **Ninguna salió verde.** `yarn verificar` RC=0 con `Test Files 58 passed (58) · Tests 1153 passed (1153)` (1133 en 57) más `3 passed (3)` de capa; `paquetes/ui`, **533 en 25** (513 en 24). **Los seis consumidores no se midieron aquí** —no están clonados—: lo mide el trabajo `consumidores` de la CI del PR |
