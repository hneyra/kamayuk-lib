# Registro de «Verificar antes de afirmar»

Cada issue deja aquí una fila con qué se implementó, **con qué rotura se demostró que la
verificación muerde** y qué rojo produjo. Es lo que impide volver a descubrir el mismo hallazgo por
tercera vez.

La doctrina —qué es una fila y qué tiene que demostrar— vive en
[`CLAUDE.md`](../../CLAUDE.md); la historia, aquí. Que la fila **exista** lo comprueba
`docs/00-gobierno/verificar-fila-del-registro.mjs`; lo que **diga**, la revisión.

**La tabla nace con una fila, y es la del repositorio mismo.** No hereda nada: la historia de los
paquetes que llegan aquí vive en el repositorio del que salieron, y copiarla sería el registro de un
trabajo que aquí no se hizo.

| Verificación | Cómo se demostró que puede fallar | Resultado |
|---|---|---|
| **Nacen las tres primeras librerías de frontend (#2).** `@kamayuk/formato` (de `rentas/frontend/src/dominio/`, verbatim), `@kamayuk/api` (**diseñado**, no extraído: los tres clientes tenían clases de error incompatibles) y `@kamayuk/sesion` (`crearIdentidad` parametrizado por realm, retorno, destino y **prefijo de claves**). Más `sin-suponer-un-sistema`, que barre el código de producción de los cinco paquetes que viajan a un navegador, omitiendo comentarios. **134 pruebas, 0 fallos** | **Cuatro roturas.** (1) `const colado = '/rentas/api/v1';` en `paquetes/api/cliente.ts`. (2) `\b` inicial en el patrón de vocabulario tributario, que hacía que `totalDeArbitrios` **no casara** — el límite de palabra no existe dentro de camelCase. (3) La guarda barriendo también `paquetes/verificaciones/`. (4) La guarda en entorno `jsdom` en vez de `node` | (1) `AssertionError: ADR-0030 §2 pone el sistema delante de la ruta… Donde aparece: paquetes/api/cliente.ts:161`. (2) `expected 0 to be greater than 0` en «y sobre la muestra, «vocabulario-tributario» encuentra lo que tiene que encontrar» — **la regla no habría mordido la forma en que el vocabulario se cuela de verdad, que es un identificador**. (3) La guarda **se delataba a sí misma**: el texto de su propia regla cita «arbitrio», porque es el ejemplo con el que ADR-0030 §4 la escribe, y ese texto es código y no comentario. (4) `TypeError: The URL must be of scheme file` en `texto.ts:6` |
| **El verde de los tres paquetes resolvía por una vía que ningún consumidor usa (#4).** Los imports entre paquetes pasan a **relativos**; se retiran el `paths` del `tsconfig` y el `alias` de `vitest`, que enmascaraban el fallo; y las **nueve prohibiciones** de ESLint se mudan a `@kamayuk/verificaciones` con sus nueve muestras, porque el código enlazado no lo lintaba nadie. **160 pruebas en 9 archivos, 0 fallos** | **Cuatro roturas.** (1) El import desnudo `from '@kamayuk/api'` con `node_modules/@kamayuk` apartado. (2) Al revés: apartado `node_modules/@kamayuk` **entero**, con los imports ya relativos. (3) `**/node_modules/**` escrito dentro de un docblock. (4) `includes('muestras')` como comprobación de directorio | (1) `paquetes/sesion/escalera.ts(45,30): error TS2307: Cannot find module '@kamayuk/api'` con **RC=2** — el rojo que sólo sale en CI, dentro de un archivo de otro repositorio. Y la guarda nueva lo caza sobre el árbol de verdad, nombrando `paquetes/sesion/escalera.ts:3` y diciendo qué escribir. (2) `tsc` **RC=0** y **160 pruebas en verde**: la resolución ya no depende de los enlaces de workspace, que es la situación del consumidor. (3) `ReferenceError: node_modules is not defined` en `prohibiciones.mjs:6` — el `*/` del glob **cierra el comentario antes de tiempo**. (4) La guarda salió roja nombrando `otras-muestras.ts`, que lleva la palabra en el nombre y no está en el directorio |
| **UI-2 · `@kamayuk/ui` capa 1 (#6).** Los **42** tokens del artboard —38 colores, 2 radios, 2 sombras— en un `@theme` de Tailwind v4, **derivados del archivo y no transcritos**; `Importe`, `Insignia`, `FechaDeCalculo` e `Icono`; los trazos con nombre GENERICO —el mapeo modulo→icono es negocio de un contexto y la guarda lo prohibe—; y las barreras de tipo. **11 archivos, 175 pruebas** (eran 9 y 160) | **Cuatro roturas.** (1) `fechaCalculo` pasada a opcional. (2) Un quinto tono en `Tono`. (3) `Importe` con tres decimales. (4) El arnes, sin querer: cuatro pruebas rojas nada mas escribirlas | (1) `TS2578: Unused '@ts-expect-error' directive` en dos sitios — la barrera de tipos es una prueba DEL COMPILADOR y falla cuando la propiedad se pierde, sin ejecutar nada. (2) `TS2741: Property 'verde' is missing in type …` — el `Record<Tono, string>` de las clases obliga a exhaustividad, asi que un tono nuevo no entra sin sus dos clases. (3) `Importe con una forma que el backend no sirve` — no redondea: revienta. (4) `Found multiple elements with the text: Un estado`. **Con `globals: false`, Testing Library NO registra su limpieza**, asi que cada `render` se acumulaba en el mismo documento. El rojo hablaba de un selector ambiguo y no de que faltara limpiar: mandaba a mirar la prueba en vez del arnes. Arreglado con un `afterEach(cleanup)` explicito en `vitest.setup.ts` |
| **UI-3 · Los tres temas por dos modos, con el oscuro derivado en OKLCH (#8).** Seis paletas completas —228 declaraciones— **derivadas de la del artboard con reglas** (unos ocho numeros por tema), no escritas a mano; `temas.css` se regenera y la guarda exige que salga igual; contraste WCAG medido en las seis, con las excepciones declaradas CON SU CIFRA; `ProveedorDeTema` con sus dos ejes; y el primer componente de shadcn con el `--radius` atado. **16 archivos, 217 pruebas** (eran 13 y 202) | **Seis roturas, cuatro de ellas defectos mios que solo aparecieron al MIRAR el archivo generado.** (1) El orden del papel invertido. (2) `--azul-oscuro` clasificado como accion. (3) El croma del sepia multiplicado. (4) El tono de la accion del sepia rotado. (5) Un valor tocado a mano en `temas.css`. (6) El renombrado del radio, contra `rentas` | (1) `--superficie` salia **#090909** y `--fondo` **#0f1012**: la tarjeta mas OSCURA que el lienzo. En claro la tarjeta es blanca sobre lienzo gris; en oscuro tiene que seguir siendo la mas clara, porque **la elevacion se lee con luz en los dos modos**. El contraste NO lo caza: las dos parejas pasaban. (2) En oscuro una accion se aclara, asi que **la barra global entera se volvia clara**. No es una accion: es una superficie. (3) `--superficie` es `#ffffff`, croma 0, y 0 × 1.6 sigue siendo 0 — el papel del sepia salia gris. **Un tono no tiñe lo que no tiene color**: hay que anadir croma. (4) Con el tono en 10° el azul del sepia salia **#5f1d2c**, granate, o sea del color del error. El sepia entibia el papel y la tinta; la semantica se queda quieta. (5) `El archivo de temas dejo de salir de las reglas`, con el comando para regenerarlo. (6) El `@theme` paso de `--radius-radio` a `--radius`, y la guarda de `rentas` salio roja: `expected 44 to be 42`. **Nada en la CI de esta libreria lo habria dicho** |
| **UI-4 · Las once piezas de shadcn que el interprete de V8 pide en cada pantalla (#11).** `Tarjeta` —filo AZUL y cabecera azul maciza, que NO es la `Card` de shadcn—, `Etiqueta`, `Campo`/`Area`/`Dato`, `Casilla`, `Desplegable`, `Emergente`, `Calendario`, `Separador`, `Tabla`, `Alerta`, `Avance` y `Formulario` sobre `react-hook-form`+`zod`; mas `campos.ts`, que pone por escrito **que pieza dibuja cada uno de los siete tipos de campo** en vez de esconderlo en un `switch`. La forma es la de shadcn —Radix, `cva`, `cn()`, `data-slot`— y los valores son los del artboard. **21 archivos, 253 pruebas** (eran 16 y 217) | **Seis roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` sin sorpresas al terminar): **(R1)** `Tarjeta` con `rounded-[0.625rem]` escrito a mano; **(R2)** `s: 'Desplegabe'` en la tabla de tipos; **(R3)** `Opcion` como `<div>` en vez de `Select.Item`; **(R4)** `Etiqueta` sin su `aria-describedby`; **(R5)** un archivo de capa fuera de `test:capas`; **(R6)** la indulgencia de `test:capas` colada en `test` | **6 de 6 en rojo.** (R1) `Hay piezas con el radio escrito a mano… tarjeta.tsx: «rounded-[0.625rem]»` — `Boton` ya se vigilaba a si mismo, pero con doce piezas vigilarlo pieza a pieza es como se olvida una. (R2) `La tabla nombra piezas que el paquete no exporta: Desplegabe` — que el nombre este escrito no basta. (R3) `Unable to find an accessible element with the role "option"`: el desplegable se abria y no habia nada que elegir. (R4) DOS rojos a la vez, `expected null to be '_r_4_-ayuda'` y `expected null to be '_r_9_-error'`. (R5) `Hay archivos de capa que NO los corre nadie`. (R6) `expected 'vitest run --exclude…' not to contain 'dangerouslyIgnoreUnhandledErrors'`.<br><br>**El defecto que la prueba caza y la vista no: el mensaje de error se comia el NOMBRE del campo.** La primera `Etiqueta` envolvia al control en el `<label>` para no tener que inventar un identificador por campo. Con ayuda o error puestos, envolver mete **todo** el contenido del `<label>` en el nombre accesible: el campo pasaba a llamarse «Ejercicio El ejercicio son cuatro digitos: 2026.», y el lector de pantalla lo anuncia ENTERO cada vez que se enfoca — el mensaje deja de ser un aviso y se vuelve parte del rotulo, que no se quita ni corrigiendo el campo. En pantalla se veia perfecto. Lo delato `getByLabelText('Ejercicio')` al dejar de encontrarlo. Y el miedo que motivo el envoltorio **no tenia fundamento**: `useId()` da uno unico por instancia montada, asi que un interprete que dibuje la misma definicion cuarenta veces da cuarenta identificadores distintos. Reescrita a `htmlFor` + `aria-describedby` + `Slot`, que es la forma de shadcn — y ahora se sabe por que es esa.<br><br>**Y el otro hallazgo, que es del ARNES y no del producto: montar una capa de Radix bajo jsdom cuesta ~6 s.** Reproducido con `radix-ui` pelado y cero lineas de esta libreria: el mismo `Popover` **cerrado** tarda 63 ms y el archivo 1.8 s; **abierto**, 6 026 ms y 40 s. Es acumulativo dentro del archivo, asi que un archivo con las cuatro pruebas de capa juntas tardaba **137 s** y hacia caducar la llamada del trabajador al proceso principal —`[vitest-worker]: Timeout calling "onTaskUpdate"`—, o sea **`yarn verificar` en ROJO con las 246 pruebas en VERDE**. Descartados uno a uno: los dos motores de Vitest (hilos y procesos), tres reporteros, `ResizeObserver` e `IntersectionObserver` puestos a mano, `pretendToBeVisual: false`, cerrar la capa antes de terminar, y un `requestAnimationFrame` sincrono. El documento quedaba limpio entre pruebas. **Aviso para quien mida esto**: el reloj de jsdom miente aqui —decia 6 ms donde el de Node decia 5 817— y `console.log` desde el trabajador viaja por la misma tuberia congestionada, asi que instrumentar con `console.log` mide la tuberia y no el codigo. Resuelto acotando: `test` los excluye, `test:capas` los corre con la indulgencia, `verificar` encadena los dos, y `las-capas-corren.test.ts` impide el modo de fallo silencioso —un archivo excluido de uno y no nombrado en el otro, que no daria ningun rojo: daria menos pruebas—.<br><br>**Cifras**: `yarn verificar` **19 archivos / 251 pruebas** mas **2 / 2** de capa, rc=0. Y **el consumidor comprobado a mano ANTES de abrir el PR**, que es lo que #10 pide y lo que no se hizo en #8: `rentas@main` con esta rama enlazada, **46 archivos, 1 128 pruebas, 0 fallos** — las tres `peerDependencies` nuevas no lo rompen |
| **UI-6 · `@kamayuk/shell`: el armazón de V8, escrito nuevo y sin saber de qué sistema es (#13).** Las **siete piezas** del armazón entran en `@kamayuk/ui` con la convención de #11 —`Miga`, `Plegable`, `Menu`, `Confirmacion`, `Cajon`, `PaletaDeMando` y `Avisos`, sobre Radix, `cmdk` y `sonner`— y sobre ellas se escribe **el armazón entero**: barra global, carril de módulos plegable con su filtro, paleta de mando, cabecera con miga, acciones al pie y el aviso de cambios sin guardar. **El catálogo entra por parámetro** (`catalogo.ts`) y el enrutado es por hash con `createHashRouter`, **con una ruta por destino ofrecido**. **24 archivos, 306 pruebas** (eran 19 y 251), más **3 / 3** de capa (eran 2 / 2) | **Doce roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` sin sorpresas al terminar): **(R1)** `const PREFIJO = '/rentas/api/v1'` en `shell/catalogo.ts`; **(R2)** «predial del contribuyente» en la barra de instrucción; **(R3)** el árbol pintando `casan.slice(0, 1)`; **(R4)** `destinoDeSlug` devolviendo el slug en vez de `null`; **(R4b)** la ruta `*` rindiendo `<Pantalla />`; **(R5)** `slugDe` ignorando el slug declarado; **(R6)** «Seguir editando» limpiando también la marca; **(R7)** el atajo mirando sólo `ctrlKey`; **(R8)** `accionesDelPie` devolviendo siempre las de escritura; **(R9)** `Cancelar` construido sobre `AlertDialog.Action`; **(R10)** un `capa-*.test.tsx` en `paquetes/shell` que no corre nadie; **(R11)** `capa-del-menu.test.tsx` fuera de `test:capas`; **(R12)** el carril ignorando `enCajon` y el plegado | **12 de 12 en rojo.** (R1) `ADR-0030 §2 pone el sistema delante de la ruta… paquetes/shell/catalogo.ts:71`. (R2) `ADR-0030 §4 lo escribe con este ejemplo exacto… CabeceraDePantalla.tsx:33`. (R3) `Unable to find an accessible element with the role "button" and name /Flota/`. (R4) `expected 'flo-turnos' to be null` y `expected 'alm-entradas' to be null`. (R4b) `Unable to find an element with the text: /no corresponde a ningun destino disponible/`. (R5) `expected '#/alm-entradas' to be '#/entradas'`, y ocho derivados detrás. (R6) `Unable to find an element with the text: esta sucia`. (R7) `Unable to find an element with the placeholder text of: /Un modulo o un destino/`. (R8) `expected [ 'Limpiar', 'Guardar' ] to deeply equal [ 'Exportar', 'Imprimir' ]`. (R9) `expected null to be 'cancelar'`. (R10) y (R11) `Hay archivos de capa que NO los corre nadie: 'test' los excluye y 'test:capas' no los nombra`, nombrando el archivo. (R12) `Unable to find an accessible element with the role "dialog"` y `expected <aside …> to be null`.<br><br>**R4 y R4b son la misma regla por dos puertas, y sólo mirándolas juntas se ve cuál cierra.** Con R4 puesta —`destinoDeSlug` aceptando cualquier slug— las pruebas puras salieron rojas y **la de integración siguió VERDE**: el hash de un destino que el catálogo no trae seguía sin abrir nada. El motivo es que el cierre de verdad no es esa función, sino **la tabla de rutas**, que se construye recorriendo el catálogo: lo que no está en él no tiene ruta que lo reciba. R4b lo confirma al revés, rindiendo la pantalla desde la ruta `*`. O sea que el AC4 no depende de que alguien se acuerde de comprobar el hash en cada pantalla: depende de la forma de la tabla.<br><br>**El hallazgo del arnés, que corrige la regla de #11 con números: lo caro NO es «una capa», es el POSICIONADOR.** #11 apartó a `test:capas` las pruebas que abren una capa, midiendo `Popover` y `Select`. Las siete del armazón se midieron de nuevo, una por archivo y con las librerías peladas: **`DropdownMenu` 12 409 ms de archivo (39 s de reloj de pared); `AlertDialog` 208 ms; `Dialog` 233 ms; `Command` (cmdk) 227 ms; `Sonner` 151 ms** — y las cuatro baratas **juntas en un archivo, 305 ms**. Lo que separa a unas de otras es `@radix-ui/react-popper`, que `DropdownMenu` lleva dentro igual que `Popover` y `Select`; `AlertDialog`, `Dialog` y el diálogo de `cmdk` se colocan con CSS y no lo cargan. Y el gasto **no está en abrir**: cronometrado por tramos con el reloj de Node, montar 34.8 ms + buscar el disparador 87.6 ms + abrir 79.1 ms + la consulta 8.9 ms = **213 ms dentro del `it`**, con el `it` reportando 4 775 ms. Está en **desmontar el posicionador al limpiar**, fuera del cuerpo de la prueba, que es por lo que no se ve instrumentando lo que uno escribe. Resultado: sólo `capa-del-menu.test.tsx` va apartado, y las otras cuatro piezas caben en `piezas-del-armazon.test.tsx` con el resto.<br><br>**Y la guarda de las capas tenía un agujero, que R10 mide en las dos direcciones.** `las-capas-corren.test.ts` miraba `paquetes/ui/shadcn` mientras el `--exclude` de `test` es **global**: un `capa-*.test.tsx` en cualquier otro paquete quedaba excluido de `test` igual que los demás y la guarda **no lo veía** para exigir que `test:capas` lo nombrara — o sea, no lo corría nadie, que es exactamente el modo de fallo que esa guarda existe para impedir, escondido donde la guarda no miraba. Medido con el archivo puesto: con la guarda acotada, **6 pruebas en verde**; con la de #13, que barre `paquetes/` entero, roja nombrando `paquetes/shell/capa-inventada.test.tsx`.<br><br>**Tres cosas más que se midieron en vez de suponerse.** (a) **jsdom no trae `matchMedia`** —además de `ResizeObserver` y `scrollIntoView`, que `cmdk` pide—: `TypeError: window.matchMedia is not a function` en `sonner/dist/index.mjs:1072`, al resolver el modo `system`. Por eso `useEsEstrecho()` del armazón pregunta por ella antes de usarla en vez de darla por hecha. (b) **El rótulo de la región viva de `sonner` venía en inglés**: sin `containerAriaLabel`, `aria-label="Notifications alt+T"`. No se dibuja en ninguna parte —es el nombre accesible del contenedor— así que una pantalla entera en castellano se anunciaba con una palabra en inglés y nadie lo ve mirándola. (c) **`AlertDialog` NO se cierra pulsando fuera, pero `Esc` SÍ lo cierra** —probado sobre la pieza, y el foco al abrir cae en el `Cancel`—. La primera mitad es la diferencia de verdad con un `Dialog`; la segunda obliga a que **`Esc` signifique la salida inocua**, y por eso el aviso conecta su cierre a «seguir editando» y no a «salir». El javadoc decía lo contrario antes de medirlo.<br><br>**Cifras**: `yarn verificar` **24 archivos / 306 pruebas** más **3 / 3** de capa, rc=0. Y **el consumidor comprobado a mano ANTES de abrir el PR**, que es lo que #10 pide: **`rentas` sale ROJO, y se dice aquí en vez de arreglarlo callando**. `yarn typecheck` da `TS2307: Cannot find module 'sonner'` y `Cannot find module 'cmdk'` dentro de `node_modules/@kamayuk/ui`, más dos derivados que parecen defectos de la librería y no lo son; `yarn test`, **3 archivos fallidos de 53 y 1 prueba de 1 234**, con `Failed to resolve import "cmdk"`. **No es un defecto de este PR: es su consecuencia declarada.** Desde `rentas`#88 el clon enlazado resuelve sus dependencias **desde `rentas`** (`preserveSymlinks: true`), así que las dos `peerDependencies` nuevas de `@kamayuk/ui` hay que instalarlas allí: `cmdk@^1.1.1` y `sonner@^2.0.8` en `frontend/package.json`, dos líneas, y `resolucion.ts` las deduplica sola porque deriva su lista de las `peerDependencies`. Este PR **no toca `rentas`** a propósito |
| **La barra de instruccion no se dibujaba NUNCA (#15).** `CabeceraDePantalla` aceptaba `instruccion?` y sabia dibujar la barra gris de V8; `Armazon` **no se la pasaba**, y no podia: ni `Destino` ni `ModuloDelCatalogo` la llevaban. Asi que la barra no salia en ningun sistema y no habia forma de que saliera sin tocar la libreria. `Destino` gana `instruccion?: string` —dato del SISTEMA, no del marco: son cuarenta frases distintas en `rentas`— y `Armazon` la baja a la cabecera. **24 archivos, 308 pruebas** (eran 24 y 306) | **La rotura es reintroducir el defecto**: `<CabeceraDePantalla hoja={hoja} />` sin su `instruccion`, que es exactamente como estaba | **Roja**: `Unable to find an element with the text: /revise lo que entro y lo que salio hoy/`, 1 de 24.<br><br>**Por que este defecto no se veia, que es lo unico interesante de el.** La cabecera se dibujaba **entera y correcta** —miga, titulo, nota— y solo le faltaba la barra. Una pantalla a la que le falta la instruccion **parece una pantalla**, no un defecto: no hay hueco, ni filo vacio, ni error en consola. Las pruebas del armazon comprobaban la cabecera y pasaban, porque comprobaban lo que si se dibujaba. Lo destapo `rentas`#90 al montar la aplicacion de verdad y exigir de cada una de las 40 pantallas su instruccion: **cuarenta rojos pidiendo un texto que nadie dibujaba**. Es el argumento entero a favor de que el recorrido corra contra la aplicacion montada y no contra el componente suelto.<br><br>**Y se anade la mitad negativa**, que es la que impide que el arreglo sea peor que el defecto: un destino SIN instruccion **no dibuja la barra**, en vez de dibujarla vacia. Un filo que encierra nada se lee como un hueco donde falta algo |
| **La CI mira a sus consumidores, y la lista es dato (#10).** `paquetes.yml` gana un trabajo que clona cada consumidor declarado en `consumidores.json`, le corre su propia orden de verificacion **dos veces sobre el mismo arbol** —una con esta libreria en `main` y otra con la rama del PR— y falla solo si la segunda empeora a la primera. `los-consumidores-se-miran.test.ts` impide que el guardian se quede sin sujeto. **25 archivos, 314 pruebas** (eran 24 y 308) | **Dos mediciones en CI de verdad, en una rama desechable** (`rotura-reintroducir-el-renombrado-de-8`, cerrada y borrada): **(M1)** `--shadow-sombra-1` cambiado de `0 1px 2px rgba(22,35,44,.06)` a `0 1px 3px rgba(22,35,44,.09)`; **(M2)** la orden del consumidor puesta a `yarn verificar && false`, que falla en las dos pasadas | **M1 — el trabajo caza lo que la libreria no puede ver.** `Lint, tipos y pruebas` **VERDE con 314 pruebas**, y `hneyra/rentas sigue en verde con esta rama` **ROJO**: `linea base (libreria en main): success` · `##[error]ESTA RAMA ROMPE A «hneyra/rentas».` · `Con la libreria en «main» su suite sale en VERDE; con esta rama, en ROJO.`<br><br>**M2 — el AC4, que es lo que separa un guardian util de uno que se ignora.** Con las dos pasadas rojas, el trabajo sale **VERDE** con aviso: `linea base (libreria en main): failure` · `::warning::«hneyra/rentas» ya estaba rojo con la libreria en «main». Esta rama NO es la causa, asi que este trabajo no la bloquea.`<br><br>**El primer intento de medida fue el equivocado, y eso es un hallazgo.** Se empezo reintroduciendo el renombrado de #8 —`--radius-radio`— y **la libreria lo caza ella sola**: #11 anadio `radio.test.ts`. Bueno para la libreria y malo como medida, porque el trabajo de consumidores ni llega a correr (`skipping`). Hubo que buscar una rotura que la libreria **no tenga con que comparar**, que es exactamente el hueco que #10 describe: la paleta se compara contra el artboard, y **el artboard vive en `rentas`** — esa direccion es deliberada, porque al reves seria `@kamayuk/ui` dependiendo de un sistema (ADR-0030 §4). O sea que el hueco no es un descuido: es la sombra que proyecta una decision correcta.<br><br>**El coste, medido** (AC5): el trabajo del consumidor tarda **1m14s** y **1m22s** en las dos corridas —clonar los dos repositorios, un `yarn install` y **dos** pasadas de la suite de `rentas`—, y corre en paralelo con el propio de la libreria (1m33s). El PR pasa de ~1m35s a ~3min de reloj de pared.<br><br>**Lo que NO cierra.** El trabajo corre sobre `main` del consumidor, no sobre la rama que a lo mejor lo esta arreglando en paralelo: dos cambios que se necesitan a la vez —una `peerDependency` nueva aqui y su declaracion alli— siguen exigiendo el mismo orden de siempre, mezclar el del consumidor primero. Es lo que se hizo en #14/`rentas`#96 a mano, y esto no lo automatiza |
| **UI-11 · Los textos del armazon entran como DATO, no como `i18next` (#19).** Las **32** palabras que `@kamayuk/shell` decia por su cuenta y las **6** de `@kamayuk/ui` salen a dos sacos —`shell/textos.ts` y `ui/textos.tsx`— y entran por `<Armazon textos={…} />` (un `Partial`, con el castellano por omision) y por `props` en las piezas sueltas. **Cero `peerDependencies` nuevas**, que es el coste que el issue rechaza: `i18next` como `peer` obligaria a los cuatro sistemas a montarlo antes de dibujar un boton. El saco viaja por su propio contexto, y ese contexto tiene los valores por omision **como valor por defecto** —a diferencia de `useArmazon()` y `useHoja()`, que revientan a proposito—: las siete piezas se publican sueltas y una pieza que exigiera proveedor de idioma para dibujar un arbol convertiria la traduccion en requisito. **28 archivos, 343 pruebas** (eran 25 y 314), mas **3 / 3** de capa | **Ocho roturas, cada una aplicada SOLA y restaurada por copia** (`git diff --stat` identico al de partida despues de cada una): **(R1)** `Volver` escrito otra vez dentro de `AccionesAlPie`; **(R2)** `aria-label="Mostrar u ocultar el menu"` escrito otra vez en `BarraGlobal`; **(R3)** `ListaDeLaPaleta` sin su `label`; **(R4)** `Miga` ignorando su `rotulo` y volviendo a `aria-label="Ruta"`; **(R5)** una pieza NUEVA con texto dentro que **nadie monta**; **(R6)** `react-i18next` anadido a las `peerDependencies` de `@kamayuk/ui`; **(R7)** `marcarElSaco` devolviendo el saco sin marcar; **(R7b)** el arnes dejando de recorrer los atributos; **(R8)** `Armazon` dejando de fundir el saco con los valores por omision | **8 de 8 en rojo.** (R1) `El armazon con una hoja de consulta deja llegar texto que NO pasó por el saco: «Volver» (por texto, en button[boton])` y `paquetes/shell/AccionesAlPie.tsx:36 «Volver» (por texto JSX)` — **4 de 21**. (R2) `«Mostrar u ocultar el menu» (por aria-label, en button[boton])` en las OCHO pantallas, mas `paquetes/shell/BarraGlobal.tsx:57 «Mostrar u ocultar el menu» (por atributo «aria-label»)` — **8 de 21**. (R3) `La paleta con resultados deja llegar texto que NO pasó por el saco: «Suggestions» (por aria-label, en div[lista-de-la-paleta])` — **1 de 20**. (R4) `«Miga» deja llegar texto que NO pasó por el saco: «Ruta» (por aria-label, en nav[miga])` — **2 de 17**. (R5) **las dos guardas montadas en VERDE, 20 de 20**, y solo el barrido rojo: `paquetes/ui/shadcn/pieza-inventada.tsx:3 «Sin resultados» (por atributo «aria-label»)` y `«No hay nada que ensenar todavia.» (por texto JSX)`. (R6) `Las «peerDependencies» de «ui» cambiaron…: expected [ 'class-variance-authority', …(11) ] to deeply equal [ …(10) ]` y `«react-i18next» entro por «peerDependencies»` — **2 de 9**. (R7) `«alternarElCarril» no se marco: expected 'Mostrar u ocultar el menu' to be '⟦alternarElCarril⟧'`, y detras las ocho pantallas. (R7b) `el recorrido no mira los atributos anunciados: expected [ 'Un literal que nadie tradujo' ] to include 'Un nombre accesible sin marcar'` — **2 de 20**. (R8) **23 de 36**, con `Unable to find an accessible element with the role "button" and name "Grabar"` y veintidos derivados de `armazon.test.tsx`.<br><br>**R5 es la medicion que justifica que haya DOS guardas y no una, y es lo unico realmente interesante de este issue.** El AC2 pide montar, y montar es lo correcto: un escaner de fuentes da rojos sobre cadenas que nunca se dibujan y se calla sobre las que si. Pero el montaje **solo caza lo que se monta**. En `@kamayuk/shell` eso lo cubre todo, porque el armazon es un solo arbol con una sola entrada: una pieza nueva del marco se dibuja dentro de `<Armazon>` o no se dibuja. En `@kamayuk/ui` NO: sus piezas son hojas que nadie monta hasta que un sistema las usa, asi que una pieza nueva con una palabra dentro pasa en verde — medido, 20 de 20. Por eso entra un segundo guardian que contesta **otra** pregunta, la de forma y no la de contenido: «el texto literal visible solo puede vivir en los dos sacos».<br><br>**Y ese barrido se escribio con el analizador de TypeScript, no con expresiones regulares, tambien por medida.** El primer intento con regex dio **249 aciertos sobre este mismo arbol, de los cuales 2 eran de verdad** —listas de importacion, destructuraciones, tipos `() => void`, cualquier linea suelta con pinta de prosa— y la eleccion era entre ruido y agujeros. Preguntandole a `ts.createSourceFile` que es un `JsxText` y que es un `JsxAttribute`, el barrido da **cero** falsos positivos y sus dos unicos aciertos son las dos frases del saco de la ui.<br><br>**El defecto que la guarda destapo al primer intento: `cmdk` montaba su lista EN INGLES.** Sin `label`, `Command.List` sale con `aria-label="Suggestions"`. Es exactamente el mismo defecto que #13 midio en `sonner` —`Notifications alt+T`— y de la misma familia: **no se dibuja en ninguna parte**, asi que una paleta entera en castellano se anunciaba con una palabra en ingles y mirar la pantalla no lo ensena. Corregido con `rotulo` en `ListaDeLaPaleta`, por omision «Sugerencias». Lo caza el recorrido **porque mira los atributos anunciados y no solo los nodos de texto**, que es la diferencia con el arnes de `rentas`#103 — y no es teorica: **cinco de las 32 palabras del armazon no se dibujan en ninguna parte**, y un recorrido de nodos de texto las da todas por buenas (R7b lo mide).<br><br>**Y una regla de la casa mordio sola, sin que nadie la invocara.** La primera version del saco declaraba `cuantosDestinos: (casan: number, total: number)`, y `yarn lint` salio rojo: `Un importe se declara «string», nunca «number»… (regla 1, RNF-055)` en `paquetes/shell/textos.ts:90`. En este producto `total…` es un nombre de dinero y solo `totalElementos` y `totalPaginas` estan exceptuados. Renombrado a `ofrecidos`, que ademas dice que se cuenta.<br><br>**Cifras**: `yarn verificar` **28 archivos / 343 pruebas** mas **3 / 3** de capa, rc=0. El archivo del armazon cuesta **1 472 ms** con la paleta, el cajon y el `AlertDialog` abiertos, asi que **no va a `test:capas`**: lo caro es el posicionador (#13) y el menu de sesion se queda cerrado a proposito. Y **el consumidor comprobado a mano ANTES de abrir el PR**, y medido DOS veces sobre el mismo arbol: `rentas@227467d` en un `git worktree` limpio, `yarn verificar` da **32 archivos / 507 pruebas, 0 fallos, rc=0** con la libreria en `main` **y exactamente lo mismo con esta rama**. Nada de `rentas` se toco: los textos son opcionales y con valor por omision, asi que su `Armazon` sigue compilando y dibujando lo mismo.<br><br>**El arbol de trabajo tuvo que apartarse para medir, y conviene que conste.** La primera pasada se hizo sobre `/home/jorge/ws/rentas` y salio con uno y luego dos rojos en `verificaciones/la-v6-no-esta.test.ts` —`e2e — el arnes, atado a las pantallas de V6` y `expected … to contain "import '@kamayuk/ui/estilos.css'"`—: **los dos de ficheros sin seguimiento que otra sesion estaba creando ahi en ese momento**, no de esta rama. Medir el consumidor sobre un arbol sucio da una cifra que no dice nada, asi que la buena es la del `worktree` |
| **UI-14 · El armazon sobrevive a que su catalogo cambie despues de montar (#20).** `crearEnrutador()` **deja de recibir el catalogo**: una sola ruta `*` bajo la raiz, y `DestinoDeLaRuta` elige entre `<Pantalla />` y `<DestinoNoOfrecido />` con `useHojaDeLaRuta`, que es el MISMO dato que dibuja el arbol, la paleta y la miga. El enrutador se construye **una vez** —`useState(crearEnrutador)`, inicializador perezoso y no `useMemo`, que es una pista que React puede tirar—. `Cascara` sigue poniendo el proveedor de la hoja solo cuando hay hoja, asi que `useHoja()` sigue reventando fuera de una pantalla, que es a proposito. La propiedad del AC4 de #13 pasa de sostenerla la tabla de rutas a sostenerla el render, y las cuatro pruebas que la vigilan siguen verdes **sin tocar una linea**.<br><br>**La causa NO era «el contexto se pierde», y medirla cambio la prueba que hay que escribir.** Leido en `react-router@7.18.3`: `RouterProvider` siembra el estado en un `useState(router.state)` **una sola vez** (`chunk-BV7QT456.mjs:6801`), asi que con un enrutador nuevo la pintada siguiente usa todavia las coincidencias del viejo; y `useRoutesImpl` las reapunta contra el manifiesto NUEVO **por el identificador de ruta** —`manifest[m.route.id] || m.route`, `:6130`—. Los identificadores son **posicionales** (`0-1`, `0-2`…), de modo que cambiar el catalogo reapunta cada coincidencia a la ruta que por accidente ocupe ese hueco en la tabla nueva. Donde caia una ruta de destino se dibujaba `<Pantalla />` **sin que hubiera hoja** y `useHoja()` reventaba; donde caia la `*`, no pasaba nada.<br><br>**Y de ahi sale un hallazgo que contradice la parafrasis del encargo, no el issue.** Medidas cinco variantes con el defecto puesto: `[]` -> un catalogo que **no** ofrece el destino del hash **revienta** (es el repro del issue, `#/tra-pap` con `SIN_TRANSITO`); `[]` -> uno que **si** lo ofrece **NO revienta** —sale la pantalla correcta, por accidente de los identificadores—; y de un catalogo lleno a uno menor depende de donde caiga el hueco: con «Flota» fuera y `#/flo-turnos` el hueco cae en la `*` y se salva, con «Almacen» fuera y `#/alm-panel` cae en una ruta de destino y revienta. O sea: «montar con `[]` y volver a pintar con uno lleno» **no muerde**. Las dos pruebas nuevas que llevan **EL REPRO** delante estan elegidas para caer del lado que revienta, y el javadoc del `describe` lo dice para que nadie las «simplifique» a un catalogo cualquiera.<br><br>**Cinco pruebas nuevas en `armazon.test.tsx`**, todas con el catalogo **llegando tarde** (AC3): el repro del issue, el repro al reves (AC2 — se pierde el permiso de la pantalla que se esta mirando), el destino que el catalogo si trae, el caso sin hash, y que el marco **no queda congelado** (se navega por el arbol con el catalogo que llego). Las tres ultimas no muerden este defecto y esta escrito cual si. | **Una rotura, la del propio defecto**: `crearEnrutador(catalogo)` con una ruta por destino ofrecido —`...catalogo.flatMap(...)` con `element: <Pantalla />`— y `useMemo(() => crearEnrutador(configuracion.catalogo), [configuracion.catalogo])` en `Armazon`. Es exactamente el codigo de `main`, restaurado despues por copia. Y antes de eso, la **medicion de las cinco variantes** en un archivo apartado —A, B, C, D y E— que es lo que dijo cuales de las cinco pruebas muerden y cuales no | **2 de 29 en rojo, y las dos son las del repro.** `AssertionError: expected <h2></h2> to be null` / `Received: <h2>Unexpected Application Error!</h2>` en `armazon.test.tsx:544` («EL REPRO del issue: con un hash puesto, el catalogo llega y no ofrece ese destino») y en `:561` («EL REPRO al reves (AC2)»), con `Error handled by React Router default ErrorBoundary: Error: useHoja() fuera de una pantalla del <Armazon>. Sin hoja abierta no hay nada que marcar sucio…` en la consola — **el mensaje literal del issue**. Las otras 27 verdes, incluidas las cuatro del AC4 de #13, que es la mitad que importa: el defecto no las tocaba. <br><br>**Cifras**: `yarn verificar` **28 archivos / 348 pruebas** (eran 343) mas **3 / 3** de capa, rc=0; `paquetes/shell` pasa de **60 a 65**. Ninguna prueba nueva abre capa, asi que el archivo se queda fuera de `test:capas` y `las-capas-corren` sigue conforme. <br><br>**Consumidor comprobado A MANO antes de abrir el PR**, sobre `rentas@6874c26` con el arbol limpio y la libreria enlazada por `link:`: `yarn verificar` **32 archivos / 508 pruebas, 0 fallos, rc=0**, y `yarn e2e` **47 pasadas en 50,4 s** en Chromium contra el bundle construido — que es la medicion que de verdad importa aqui, porque abre **los 40 destinos por el hash** y es donde una ruta unica mal resuelta se veria. `rentas` no se toco: su rodeo declarado (`frontend/src/aplicacion.tsx:99-109`) se queda, porque sigue siendo lo correcto por su primer motivo — no ensenar un arbol vacio durante un segundo—; lo que deja de ser es **necesario** |
